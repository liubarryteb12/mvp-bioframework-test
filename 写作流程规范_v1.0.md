# 写作流程层规范 v1.0（WP-1~9 + NC-1~7）

> 定位：v2.15 新增 §12「写作流程层」。**V1–V5 管分析，WP 管写作**。
> 不重定义任何现有判据，只调用。
> 一句话：**WP 管"从数据到稿件"的转换顺序，NC 管"各节连成一条链"。**

---

## §0 本轮实跑发现的两个真 bug（先记，避免复发）

### 0.1 `writing_spec.yaml` 词表被 `;` 拼成单字符串 → 判据恒 PASS（空规）

初版写成：

```yaml
upgrade_words:
  - 首次; - 揭示; - 阐明; - 驱动
```

YAML 解析结果是 **`['首次; - 揭示; - 阐明; - 驱动']`**（一个元素），
而非 4 个词。于是 `w in title` 恒为 False → **WP-1.1 永远 PASS**。

**这是第 11 条陷阱形态①（从未 FAIL = 空规）的实例，发生在配置层。**

已修：全部改用标准块序列；并加 `writing_guard._check_spec_types()`——
递归扫描所有 list 元素，含 `;` 或 ` - ` 即 `SystemExit`。

### 0.2 WP-5a.1 的 50 字符窗口**跨句** → 邻近句的 `[L-xxx]` 被误判为"已标注"

变异测试注入"删除 `[L-012]`"后守卫仍 PASS。根因：
结果节为 `...显著富集,部分通路不显著。未见明显 batch 效应 [L-015]。`
—— `显著富集` 与 `[L-015]` 距离 < 50 字符，虽**不在同一句**，仍被算作已标注。

已修：窗口由"固定 50 字符"改为**同一句内**（按 `。;换行` 切分）。

> 两条都是"看起来完备、实则拦不住"，且**都是变异测试抓出来的**——
> 再一次印证第 11 条：一直 PASS 不等于守卫在工作。

---

## §1 依赖图（顺序是结构，不是建议）

```
WP-0 素材就绪（台账·图件·产物·元数据）
   │ 必过 W-Gate
   ▼
WP-CORE 逻辑链 P0–P4   ← NC 叙事层的锚
   ├─► WP-4 方法 ─┐
   ├─► WP-5 结果 ─┤
   ├─► WP-6 讨论 ─┼─► 四节定稿
   └─► WP-7 参考 ─┘        │
                            ▼
                     WP-2 摘要（首句必须是 P1）
                            ▼
                     WP-3 背景（调 research_survey_skill）
                            ▼
                     WP-1 标题（最后定稿）
                            ▼
                     WP-8 去 AI 味（初步过滤）
                            ▼
                     WP-9 三格式导出
```

**两处反惯例，有意为之**：

1. **摘要在正文之后**：防"先写摘要再让正文凑数"（NC-6 倒挂）。
   摘要首句 P1 必须与正文 P1 **逐字同一**。
2. **标题在最后**：防"标题先定、正文向标题凑"（T1.1 越界）。

---

## §2 九节点判据

每节点给三样：**坑（锚定）· 判据（可判定）· 注入方式（可构造 FAIL）**。

### WP-1 标题

| # | 坑 | 判据 | 注入 |
|---|---|---|---|
| 1.1 | 主张超证据 | 无升级词（首次/揭示/阐明/驱动/机制/证明/证实） | 加"揭示" |
| 1.2 | 无信息词 | 黑名单：研究/分析/探索/初探/浅析 | 加"的研究" |
| 1.4 | 超限 | 中文 ≤25 字 / 英文 ≤20 词 | 加长 |
| 1.6 | 缺对象 | 含研究对象 + 主要发现 | 删对象 |

### WP-2 摘要

| # | 坑 | 判据 |
|---|---|---|
| 2.1 | 首句非 P1（NC-6） | 首句 == P1 |
| 2.2 | 结构缺段 | 背景/方法/结果/结论四段齐全 |
| 2.6 | 末句无边界 | 含"尚需/待验证/未来/局限" |
| 2.7 | 缺样本量 | `n = N` 或"共 N 例" |

### WP-3 背景（★ 与 research skill 的接口，防幻觉文献）

```
research_survey_skill:
  input : topic_keywords · field · year_range · max_results
  output: papers[{DOI,title,authors,journal,year}] + coverage_report
  failure:
    - 未调用 → WP-3 拒入
    - 返回 0 篇 → 判 N/A（第三态），不降级为 AI 生成
```

| # | 判据 |
|---|---|
| 3.2 | 引言引用 ∈ skill 输出集合；DOI 逐条可验证 |
| 3.4 | 含 gap statement（然而/尚未/目前缺乏/仍不清楚） |

### WP-4 方法

| 判据 | 阈值 |
|---|---|
| 4.1 版本/批号 | `v?\d+\.\d+` 或"批号" |
| 4.3 统计 | 单/双侧 **且** 多重校正（BH/Bonferroni/FDR） |
| 4.7 数据共享 | `GSE\d+\|SRR\d+\|DOI` |

### WP-5 结果（拆 4 子块）

| 子块 | 判据 |
|---|---|
| **5a** 文字 | 每个"显著"**同句内**有 `[L-xxx]`；不含机制词；阴性结果显式出现 |
| **5b** 排版 | 图号连续无缺 |
| **5c** 图表注 | 精确 n · 误差棒+中心 · 检验方法 · ≤250 字（对齐作图规范 §D） |
| **5d** 图表标 | 无信息词黑名单；≤15 字；≠ 图注首句 |

### WP-6 讨论

| # | 判据 |
|---|---|
| 6.1 | 首段首句 == P1（NC-3） |
| 6.2 | 含局限性（P3） |
| 6.3 | 含展望（P4） |
| 6.4 | 机制词**同句内**有 `(L\d)` 标注（NC-2 孤证） |

### WP-7 参考文献

| 判据 |
|---|
| 7c.1a 无"用而未引"（正文引用 ⊆ 文献表） |
| 7c.1b 无"引而未用" |
| 7b.2 每条含 DOI/PMID |

### WP-8 去 AI 味（**只是过滤，不追求完全去除**）

| # | 判据 | 阈值 |
|---|---|---|
| 8.1 | 模板句式（随着…的发展/近年来） | 0 出现 |
| 8.2 | 空洞表达（具有重要意义/发挥关键作用） | 0 出现 |
| 8.3 | 过渡词密度（然而/因此/此外） | ≤3/千字 |
| 8.4 | 段落长度标准差 | ≥30 字 |
| 8.9 | 元话语密度（值得注意的是） | ≤1/千字 |

阈值填槽可覆（按期刊/学科调）。

### WP-9 三格式导出

| 输出 | 内容 | 用途 |
|---|---|---|
| **docx-A** | 纯文本 + `[图N位置]` 占位符 | 编辑修改 |
| **docx-B** | 纯图片 + 图注/表注 | 排版核对 |
| **pdf** | 文图合一 | 投稿审阅 |

| 判据 |
|---|
| T9.1 三版本图号集合一致 |
| T9.2 docx-A 占位符数 == docx-B 图表数 |
| T9.3 docx-B 图注 ⊂ pdf（逐字） |
| T9.5 三版本参考文献逐字一致 |
| T9.6 图片 ≥300 dpi |

---

## §3 叙事层 NC-1~NC-7（贯穿，非节点）

| 编号 | 检查 | 落点 |
|---|---|---|
| **NC-1** 悬空问题 | 引言问句 → 讨论有回应 | WP-3 / WP-6 |
| **NC-2** 孤证结论 | P1 支撑证据 ≥2 | WP-CORE / WP-6 |
| **NC-3** 倒挂 | 讨论首段 = P1 | WP-6.1 |
| **NC-4** 平行叙事 | 结果间有递进连接词 | WP-5a |
| **NC-5** 图文脱节 | "如图 X" 后有目的词 | WP-5a/b |
| **NC-6** 摘要双头 | 摘要首句 = P1 | WP-2.1 |
| **NC-7** 新发现注入 | 讨论引用 ⊆ 结果引用 | WP-6 |

**NC-3 与 NC-6 是同一约束的两次检查**（P1 同时是摘要首句与讨论首段首句）
—— 与 G-12 的 rid-all 同构，是"同一真相两处一致"的对称守卫。

---

## §4 与主框架的接口

| 现有 | WP 如何调用 |
|---|---|
| V4 证据分级 L1–L4 | WP-5a / WP-6 的 L 级标注 |
| 台账 §8.5 | WP-5a 的 `[L-xxx]` 来源 |
| 作图规范 §D | WP-5c 图注必含项 |
| R4 数字纪律 | WP-2 / WP-5c |
| R5 措辞升级 | WP-1.1 / WP-6.4 |
| G-7.3 / figure_kit | WP-5b / WP-9 |
| 上机总纲第三态 N/A | WP-3 skill 返 0 篇 → N/A，不是 FAIL |
| **guard_selftest 先红后绿** | 每个 WP 守卫的强制要求 |

---

## §5 诚实边界（不立的规则）

按框架铁律"**无具体坑的规则不立**"，以下**不立**——立了就是空规：

| 不立 | 原因 |
|---|---|
| "故事是否吸引人" | 无具体坑 |
| "叙事是否顺畅" | 无具体坑 |
| "讨论是否深刻" | 无具体坑 |
| "引言是否优雅" | 无具体坑 |
| "去 AI 味是否彻底" | 边界模糊，人工完成 |

**WP 只抓可判定的断裂，不假装能评判好不好看。**

---

## §6 验证状态（v2.15 实跑）

| 项 | 结果 |
|---|---|
| 合规样例稿 | **43/43 通过，不适用 0**（v2.25 实测） |
| 变异测试 | **66/66 守卫生效**（WP-1~9 各有用例，G-COV 齐全；v2.25 实测） |

> ★ **v2.25 基线变更（三处旧数字作废）**：本表原写「36/36 通过（1 项 N/A）」，
> 而 `云端服务器执行提示词` CP-10 写「38 通过 + 4 N/A」——**两处都不等于实测**。
> 根因是两处"不给输入 = 自动通过"：
>
> 1. 构建流程**从不传 `--p1`** → `WP-CORE / WP-2.1 / WP-6.1` 判 N/A；
> 2. `--skill` 恒缺 → `WP-3.1` 判 N/A（而 N/A **不是失败**）。
>
> 于是「4 项从未执行」被汇总掩盖。v2.25 已修：
> 构建从稿件**派生** P1（不硬编码）、提供 demo skill 夹具、
> `WP-3.1` 硬化为「未提供 `--skill` → FAIL」，并把汇总分母改为三态之和。
> 现测：**43/43 通过, 失败 0, 不适用 0**。
| WP-9 合规/违规 | 合规 rc=0、违规 rc=1（双向验证） |
