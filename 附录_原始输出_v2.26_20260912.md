# 附录 · 原始输出(证据)

> **版本 v2.26 · 本文件由 `crossval/scripts/gen_appendix.py` 自动生成,请勿手工编辑 · run_id `0b7e75ffc4a5dbdc`**
> 起因:对上一版附录的审核发现 5 处数字与合集不一致、F/G 节重复、缺 criteria.yaml。
> **根因是手工拼装无强制同步**;改为脚本生成后,数字一律读自当前 `results/` 与 `schema/`。

> 脚本运行时间(UTC+8):2026-09-12 20:35:34
> 本次 run_id:`0b7e75ffc4a5dbdc`

---

## 0. 数字变化 changelog(审计 §7 要求)

| 变化 | 原因 |
|---|---|
| 44/44 → 33/33 | 首轮审核发现 44/44 是手工统计,脚本实际 33 项。改为动态取自 report.json。 |
| 33 → 45 | 修复竞争风险 bug(KM 与 CIF 算法相同)、EPV 重构、多页 PDF 扫描、每分支独立种子。 |
| 45 → 69 | 补分支 T15/T16/T17(条件启动判据,此前从未验证)+ 分支 10(竞争风险/EPV/可行性)。 |
| 69 → 70 | 隔离守卫升 v2:新增绕过回归测试 7 项合并计 1 项判定。 |
| 70 → 72 | 将 s=1.6 边界情形正式纳入脚本(此前该数字仅存在于探索阶段与报告中,
     无台账来源 —— 违反第 9 条原则,已补为正式判据 2 项)。 |
| 72 → 74 | 分支 6 重写:漂移公式统一为 |ΔOR|/OR₁,并新增多种子漂移区间与翻转率判定。 |
| 13.9% → 33.0% | 分支 6 重构两次(重叠度 50%→32.5%;信号基因由噪音改为真实携带程序活性)。
     旧值 13.9% 作废。同时:**'且方向翻转'经 12 种子验证仅 5/12 发生,
     属种子依赖,已改为概率表述**,不再写成确定性结论。 |
| handoff 10 → 11 | 新增 L-6c(无自指检查)。合集曾写 10/10,未同步。 |
| v1.7 判定项 74 → 85 | P1 七条修复:分支 4 绝对量级 +3、分支 6 Jaccard +3、分支 8 合并辅助判据 +5。 |
| v1.7 一致性守卫 14 → 29 | 新增 G-5(criteria 关键值必须在框架正文出现),首次运行即抓出 4 项不同步。 |
| v1.8 一致性守卫 29 → 32 | 新增 G-5.DEPR(已废弃表述不得未标注残留),首次运行抓出 1 项。 |
| v1.9 审核报告守卫 9 → 10 | 新增 R-8(报告中所有守卫 N/N 必须可回溯至 guard_results.json)。 |
| v1.9 §0.2 一致性守卫 14/14 | 审计发现:该数字自 G-5 建立后从未更新(G-5 建前值),存活多轮未被任何守卫发现。 |
| v1.9 R-8 另行抓出 3 处 | 3/3(隔离守卫旧拦截值,现 7/7)、74/74(旧主验证值,现 85/85)—— 均为过期残留,已修。 |
| v2.7 判据条数 0 → 21 | gen_appendix 统计用旧正则(依赖前导空格),safe_dump 输出无缩进 → 数出 0。同一脚本内两套算法并存。 |
| v2.7 G-11 / G-12 新增 | 判据条数(附录/框架/合集三处)与 banner 版本号/run_id 一致性;均纳入 MUST_COVER 变异测试。 |
| v2.8 MUST_COVER 强制 | 新增守卫必须带变异用例,否则 G-COV FAIL(代码强制,非流程约定)。覆盖率可见化:守卫家族 19 个,已覆盖 7。 |
| v2.8 附录 banner 加版本 | 加版本与 run_id;版本由 build --version 传入,不读滞后的 MANIFEST。 |
| v2.9 G-11.3 正则锚点修正 | 原只匹配叙述句;合集 §0.2 状态表写「判据 YAML **19** 条」→ 不匹配 = 空规。改多形态全量匹配 + 新增 G-11.3-cov。 |
| v2.10 MANIFEST.verification 改为派生 | 原 layout/pytest/mutation 三项**硬编码**;mutation 写 '6/6' 而实际已 12/12 —— 汇总撒谎(第 11 条陷阱形态③)。改为全部从结果文件派生。 |
| v2.10 vXX_fixes 由 build.py 写入 | 原靠手工追加 → v2.8/v2.9 的 fixes 从未进入 MANIFEST,G-6.0 对这两版 FAIL。与'gen_appendix 最后跑'同理:写进文档不管用,必须由代码写入。 |
| v2.10 变异用例扩充(覆盖率 36%→57%) | 新增 G-1 / G-3 / R-6 文档变异 + G-7 文件系统变异(fs 变异带残留检测)。覆盖率统计改为**仅计实际抓到的用例**,禁止无条件添加 —— 否则覆盖率虚高, 与'守卫一直通过=空规'是同一种自欺。 |
| v2.10 G-11.3 命中数按位置计数 | 原按**值**去重 → 两处都写 21 时只报 1 处,覆盖广度不可见。 |
| v2.10 消除静默 except | gen_appendix 的 ImportError: pass 会静默跳过 criteria 自检 → 改为 fail loudly;platform_check 两处为预期路径,补注释说明。 |
| v2.9 G-12 正则锚点修正 | 原要求 run_id 紧跟反引号;§0.2 隔着括号 → 不匹配 = 空规。改允许间隔 + 全量校验 + 新增 rid-all / rid-cov。 |
| v2.9 G-11 忽略 --doc | 第三次复发(v2.3 G-5/6/7/8、v2.7 G-12 已修,G-11 漏修)。 |
| v2.9 变异测试分发 bug | 原按文档类型分发(合集→report_guard),但 G-11.3/G-12.合集.* 在 consistency_guard → 恒未抓到 → 误判空规。改按 expect_id 前缀。 |

> **说明**:run_id 每次运行都会变化(它由 seed + 时间戳 + 脚本哈希导出),
> 这是**预期行为**,不是不一致。核对时应比对**同一份 report.json**,而非跨 run 的 run_id。

## A. `results/report.json`

### A.1 summary

```json
{
  "n_pass": 222,
  "n_fail": 0,
  "n_na": 0,
  "total": 222,
  "branches": 21,
  "criteria_dist": {
    "T13c": {
      "n_direct": 16,
      "n_branch": 0,
      "n": 16,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "4"
      ]
    },
    "T31": {
      "n_direct": 16,
      "n_branch": 0,
      "n": 16,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "20",
        "21"
      ]
    },
    "T13b": {
      "n_direct": 13,
      "n_branch": 0,
      "n": 13,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "20",
        "3"
      ]
    },
    "HARDBLOCK": {
      "n_direct": 13,
      "n_branch": 0,
      "n": 13,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "8"
      ]
    },
    "T33": {
      "n_direct": 10,
      "n_branch": 0,
      "n": 10,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "21"
      ]
    },
    "B03A": {
      "n_direct": 9,
      "n_branch": 0,
      "n": 9,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "6"
      ]
    },
    "T34": {
      "n_direct": 8,
      "n_branch": 0,
      "n": 8,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "21"
      ]
    },
    "V5DECL": {
      "n_direct": 7,
      "n_branch": 0,
      "n": 7,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "7"
      ]
    },
    "T32": {
      "n_direct": 7,
      "n_branch": 0,
      "n": 7,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "20"
      ]
    },
    "T35": {
      "n_direct": 7,
      "n_branch": 0,
      "n": 7,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "21"
      ]
    },
    "V4MODAL": {
      "n_direct": 7,
      "n_branch": 0,
      "n": 7,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "21"
      ]
    },
    "T36": {
      "n_direct": 7,
      "n_branch": 0,
      "n": 7,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "23"
      ]
    },
    "T08": {
      "n_direct": 6,
      "n_branch": 0,
      "n": 6,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "15"
      ]
    },
    "T27": {
      "n_direct": 6,
      "n_branch": 0,
      "n": 6,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "17"
      ]
    },
    "T15": {
      "n_direct": 5,
      "n_branch": 0,
      "n": 5,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "T15"
      ]
    },
    "T17": {
      "n_direct": 5,
      "n_branch": 0,
      "n": 5,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "T17"
      ]
    },
    "GLOBAL": {
      "n_direct": 5,
      "n_branch": 0,
      "n": 5,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "15",
        "16",
        "20",
        "21",
        "22"
      ]
    },
    "T21": {
      "n_direct": 5,
      "n_branch": 0,
      "n": 5,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "16"
      ]
    },
    "T16": {
      "n_direct": 4,
      "n_branch": 0,
      "n": 4,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "T16"
      ]
    },
    "B02S": {
      "n_direct": 4,
      "n_branch": 0,
      "n": 4,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "10"
      ]
    },
    "T26": {
      "n_direct": 4,
      "n_branch": 0,
      "n": 4,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "17"
      ]
    },
    "T29": {
      "n_direct": 4,
      "n_branch": 0,
      "n": 4,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "17"
      ]
    },
    "T18": {
      "n_direct": 4,
      "n_branch": 0,
      "n": 4,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "22"
      ]
    },
    "T01": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "1"
      ]
    },
    "T06": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "10"
      ]
    },
    "B01A": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "10"
      ]
    },
    "Y6": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "15"
      ]
    },
    "T19": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "15"
      ]
    },
    "T20": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "15"
      ]
    },
    "T23": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "16"
      ]
    },
    "T28": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "17"
      ]
    },
    "T30": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "17"
      ]
    },
    "T03": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "22"
      ]
    },
    "T09": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "22"
      ]
    },
    "T12": {
      "n_direct": 3,
      "n_branch": 0,
      "n": 3,
      "n_na": 0,
      "na_tested": 1,
      "branches": [
        "22"
      ]
    },
    "T13a": {
      "n_direct": 2,
      "n_branch": 0,
      "n": 2,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "2"
      ]
    },
    "T14": {
      "n_direct": 2,
      "n_branch": 0,
      "n": 2,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "5"
      ]
    },
    "T22": {
      "n_direct": 2,
      "n_branch": 0,
      "n": 2,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "16"
      ]
    },
    "T24": {
      "n_direct": 2,
      "n_branch": 0,
      "n": 2,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "16"
      ]
    },
    "T25": {
      "n_direct": 2,
      "n_branch": 0,
      "n": 2,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "16"
      ]
    },
    "SEEDROBUST": {
      "n_direct": 2,
      "n_branch": 0,
      "n": 2,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "E"
      ]
    },
    "ISOLATION": {
      "n_direct": 2,
      "n_branch": 0,
      "n": 2,
      "n_na": 0,
      "na_tested": 0,
      "branches": [
        "P3"
      ]
    }
  },
  "n_unmapped": 0,
  "elapsed": 50.6
}
```

### A.2 run_metadata

```json
{
  "run_id": "0b7e75ffc4a5dbdc",
  "parent_run_id": null,
  "seed": 20260910,
  "python": "3.13.14",
  "platform": "Windows-11-10.0.26200-SP0",
  "script_hashes": {
    "ablation.py": "510999a04317f535",
    "applicability_gate.py": "9f333de26bdbd32e",
    "compare_platforms.py": "4bfff98d4331cd8f",
    "compare_runs.py": "16eac8b4cd1587b5",
    "component_ablation.py": "32dd4417f398ec90",
    "conclusion_branch.py": "a0c9dd820de3047d",
    "conclusion_guard.py": "d8a636116c401bc6",
    "consistency_guard.py": "84a41df64031697f",
    "data_ethics.py": "deecc6899f926bd2",
    "dormant_check.py": "8b9b31bc679addb4",
    "export_guard.py": "8e7b77197d2a95d0",
    "figure_kit.py": "b0dd42c07a933c22",
    "gen_appendix.py": "7e4ab7defa323760",
    "guard_selftest.py": "506b43d8d9b8f37c",
    "layout_check.py": "2261864587620ea5",
    "literature_gap.py": "2173fc68097c7842",
    "platform_check.py": "e1d4f8094921302a",
    "rare_uncertainty.py": "a950f1c59331480f",
    "realdata_adapter.py": "290892ddb9c8981e",
    "report_guard.py": "9d33f137991a0569",
    "run_units.py": "4b3fbb28b2ad17f0",
    "selection_causal.py": "afc836b05a6007dc",
    "tl_bee.py": "0a0f8330cbbf94c7",
    "verify_all.py": "1305b6ad4278e24d",
    "writing_guard.py": "a91be4dea7be244d"
  },
  "input_hash": "d8ac2a843faa33e1"
}
```

### A.3 env_snapshot(完整,不截断)

共 **26** 个包。

| 包 | 版本 |
|---|---|
| `PyYAML` | 6.0.3 |
| `Pygments` | 2.21.0 |
| `cloudpickle` | 3.1.2 |
| `colorama` | 0.4.6 |
| `contourpy` | 1.4.0 |
| `cycler` | 0.12.1 |
| `fonttools` | 4.65.0 |
| `iniconfig` | 2.3.0 |
| `joblib` | 1.6.0 |
| `kiwisolver` | 1.5.1 |
| `matplotlib` | 3.11.0 |
| `narwhals` | 2.26.0 |
| `numpy` | 2.5.0 |
| `packaging` | 26.3 |
| `pandas` | 3.0.5 |
| `pillow` | 12.3.0 |
| `pluggy` | 1.6.0 |
| `pyparsing` | 3.3.2 |
| `pypdf` | 6.18.0 |
| `pytest` | 9.1.1 |
| `python-dateutil` | 2.9.0.post0 |
| `scikit-learn` | 1.9.1 |
| `scipy` | 1.18.1 |
| `six` | 1.17.0 |
| `threadpoolctl` | 3.6.0 |
| `tzdata` | 2026.3 |

## B. 判定项按分支分布

总计 **222** 项,通过 **222** 项,唯一分支 **21** 个。

| 分支 | 判定项数 | 说明 | 对应 criteria id |
|---|---|---|---|
| 分支 1 | 3 | 多重检验家族 | `T01` |
| 分支 10 | 10 | 竞争风险/EPV/可行性 | `T06/B01A/B02S` |
| 分支 15 | 16 |  | `` |
| 分支 16 | 15 |  | `` |
| 分支 17 | 20 |  | `` |
| 分支 2 | 2 | ΔAUC 增量 | `T13a` |
| 分支 20 | 28 |  | `` |
| 分支 21 | 37 |  | `` |
| 分支 22 | 14 |  | `` |
| 分支 23 | 7 |  | `` |
| 分支 3 | 5 | 校准(须用 CV 概率) | `T13b` |
| 分支 4 | 16 | DCA 曲线形状 | `T13c` |
| 分支 5 | 2 | 复现方向一致性 | `T14` |
| 分支 6 | 9 | 程序定义漂移 | `B03A` |
| 分支 7 | 7 | 目检 M11–M18 | `V5DECL?见 M11–M18` |
| 分支 8 | 13 | 硬阻断三类 | `硬阻断9条` |
| 分支 E | 2 | 种子稳健性 | `G-E` |
| 分支 P3 | 2 | 合成值隔离守卫 | `P3` |
| 分支 T15 | 5 | 单细胞 QC 阈值 | `T15` |
| 分支 T16 | 4 | 推断单位/pseudobulk | `T16` |
| 分支 T17 | 5 | 计算扰动 | `T17` |
| **合计** | **222** | | |

> 分支 id 为 `1..8 / E / P3 / T15 / T16 / T17 / 10`。早期报告称"分支 9"不精确,
> 实际 T15/T16/T17 是三个独立 id。已更正。

## C. 完整判定台账(全量,非抽样)

| # | 分支 | criteria id | 判据 | 实测 | 期望 | 结果 |
|---|---|---|---|---|---|---|
| 1 | 1 | `T01` | 未校正产生假阳性(应>10) | 26 | >10 | PASS |
| 2 | 1 | `T01` | BH 校正后应≈0 | 0 | <=2 | PASS |
| 3 | 1 | `T01` | 校正必要性(未校正>BH) | True | True | PASS |
| 4 | 2 | `T13a` | ΔAUC 低于阈值 → 触发 F3 | 0.0397 | <0.05 → F3 | PASS |
| 5 | 2 | `T13a` | ΔAUC 为正但不足(典型陷阱) | True | True(易误判为有增量) | PASS |
| 6 | 3 | `T13b` | bootstrap 有效次数充足 | 1000 | >=950 | PASS |
| 7 | 3 | `T13b` | bootstrap 内嵌套 CV(不复用外层) | True | True | PASS |
| 8 | 3 | `T13b` | 训练内斜率恒≈1(陷阱) | 1.001 | 0.9~1.1 | PASS |
| 9 | 3 | `T13b` | CV 斜率偏离 1 | 0.841 | !=1 | PASS |
| 10 | 3 | `T13b` | T13b 判据(CI 覆盖1) | False | False → 不得用于个体风险预测 | PASS |
| 11 | 4 | `T13c` | 样本内准确率虚假完美(忘记CV的陷阱) | 1.0 | >0.90 | PASS |
| 12 | 4 | `T13c` | 高维过拟合样本外AUC崩塌(过拟合代价) | 0.544 | < 0.674 | PASS |
| 13 | 4 | `T13c` | 过拟合模型概率更极端(过度自信) | 0.991 | > 0.878 | PASS |
| 14 | 4 | `T13c` | 过拟合训练集准确率异常高(分离) | 1.0 | >0.90 | PASS |
| 15 | 4 | `T13c` | 已校准模型:全区间胜出率≥0.5 | True | True | PASS |
| 16 | 4 | `T13c` | 未校准模型:全区间胜出率<0.5 → 触发 F3 | 0.253 | <0.5 → F3 | PASS |
| 17 | 4 | `T13c` | 未校准:预注册区间内存在≥10点连续负区间 → 判不确定 → F3 | 21 | >=10 → F3 | PASS |
| 18 | 4 | `T13c` | 已校准:连续负区间应短于10点(不触发) | 8 | <10 | PASS |
| 19 | 4 | `T13c` | 新判据判别力:未校准连续负区间显著长于已校准 | 21 > 8 | 未校准>已校准 | PASS |
| 20 | 4 | `T13c` | 全区间勉强过线时,判据2仍可捕获(补漏检) | True | True | PASS |
| 21 | 4 | `T13c` | 边界情形:全区间≥0.5 时判据1不触发 | True | True(判据1失效) | PASS |
| 22 | 4 | `T13c` | 已校准模型 NB 差具临床量级(中位|Δ|≥0.01) | 0.0114 | >=0.01 | PASS |
| 23 | 4 | `T13c` | 报告预注册区间内 NB 差最小值(强制项) | -0.0273 | 已记录 | PASS |
| 24 | 4 | `T13c` | [自证]胜出率高≠临床有意义(绝对量级独立判据) | 中位0.0114/最小-0.0273 | 须与胜出率并列报告 | PASS |
| 25 | 4 | `T13c` | 边界情形:判据2仍能捕获(增量价值) | 13 | >=10 → F3 | PASS |
| 26 | 4 | `T13c` | [自证]最优子区间≈1.0 为构造性必然(故不作判据) | 1.0 | ≈1.0(无效指标) | PASS |
| 27 | 5 | `T14` | 外部集方向相反 → R5 | False | False | PASS |
| 28 | 5 | `T14` | 方向相反不可写'复现' | True | True | PASS |
| 29 | 6 | `B03A` | 重叠度构造正确(≈1/3) | 32.5% | 30%~36% | PASS |
| 30 | 6 | `B03A` | 换成员集漂移 >10% | 33.0 | >10% | PASS |
| 31 | 6 | `B03A` | 漂移普遍且巨大(多种子最小值仍>10%) | 16.3 | >10% | PASS |
| 32 | 6 | `B03A` | 漂移幅度远超旧称的 13.9%(旧值已作废) | 33.0 | >13.9 | PASS |
| 33 | 6 | `B03A` | **方向翻转为种子依赖,非必然** | 5/12=42% | 0<rate<1 → 不得称'且翻转' | PASS |
| 34 | 6 | `B03A` | 重叠度指标明确为 Jaccard(非相对重叠) | 19.4% | <=0.5 | PASS |
| 35 | 6 | `B03A` | [口径差异]相对重叠 vs Jaccard 数值不同(故须写明) | 32.5% vs 19.4% | 两者不等 → 条文须指定 | PASS |
| 36 | 6 | `B03A` | 实质变更:对称差占比 ≥0.5(排除'部分归零'中间态) | 80.6% | >=0.5 | PASS |
| 37 | 6 | `B03A` | 程序定义非稳健性参数(裁决依据) | 33.0 | >5% 即说明敏感 | PASS |
| 38 | 7 | `V5DECL?见 M11–M18` | sample_ok_truetype.pdf 判定 | 合规 | 合规 | PASS |
| 39 | 7 | `V5DECL?见 M11–M18` | sample_bad_bitmap.pdf 判定 | 不合规:位图/无文本层 | 不合规 | PASS |
| 40 | 7 | `V5DECL?见 M11–M18` | sample_bad_type3.pdf 判定 | 不合规:Type3非矢量字体 | 不合规 | PASS |
| 41 | 7 | `V5DECL?见 M11–M18` | sample_bad_notext.pdf 判定 | 不合规:无文本层 | 不合规 | PASS |
| 42 | 7 | `V5DECL?见 M11–M18` | sample_bad_page2_bitmap.pdf 判定 | 不合规:位图 | 不合规 | PASS |
| 43 | 7 | `V5DECL?见 M11–M18` | 位图检测不依赖 pdfimages(poppler) | True | True | PASS |
| 44 | 7 | `V5DECL?见 M11–M18` | Type3 与 TrueType 可区分 | True | True | PASS |
| 45 | 8 | `硬阻断9条` | 细胞级 p 极小(陷阱) | 4.2e-14 | <1e-6 | PASS |
| 46 | 8 | `硬阻断9条` | 患者级不显著 → 伪重复 | 0.28 | >0.05 | PASS |
| 47 | 8 | `硬阻断9条` | 推断单位必须为 donor | True | True | PASS |
| 48 | 8 | `硬阻断9条` | 泄露致 AUC 虚高 | 0.112 | >0.10 → 严重 | PASS |
| 49 | 8 | `硬阻断9条` | T10 筛选须在训练折内 | True | True | PASS |
| 50 | 8 | `硬阻断9条` | 两队列效应差 >1.0 | 1.85 | >1.0 | PASS |
| 51 | 8 | `硬阻断9条` | 合并前判据A:事件率差异 <15%(独立判据) | 12.7% | <15% | PASS |
| 52 | 8 | `硬阻断9条` | 合并前判据B:方向一致(效应符号相同) | OR_A=3.085, OR_B=1.238 | 同号 | PASS |
| 53 | 8 | `硬阻断9条` | 合并前判据C:终点等价性未通过 → 一票否决 | MoCA vs MMSE 不等价 | 不等价即禁合并 | PASS |
| 54 | 8 | `硬阻断9条` | I² 分层:>50% 强制报告异质性来源;>75% 禁合并主分析 | 96.6 | 本例>75 → 禁合并 | PASS |
| 55 | 8 | `硬阻断9条` | [自证]k=2 时 I² 不稳(df=1,Q 期望=1)→ 不得作唯一判 | I2=96.6% 但 df=1 | 须配A/B/C | PASS |
| 56 | 8 | `硬阻断9条` | I² >50% → 禁止合并 | 96.6 | >50 | PASS |
| 57 | 8 | `硬阻断9条` | 合并 OR 居中(易误读为一致) | 1.834 | 介于两者之间 | PASS |
| 58 | T15 | `T15` | 核/单核 mito% 分布显著不同 | 5.1% vs 0.9% | 差异显著 | PASS |
| 59 | T15 | `T15` | 单阈值跨类型套用会失效(须分层) | 剔除率差 10.0% | >5% | PASS |
| 60 | T15 | `T15` | 误用单细胞阈值(10%)→ 单核几乎不过滤 | 0.0% | <2% → 污染漏检 | PASS |
| 61 | T15 | `T15` | 误用严格阈值(2%)→ 单核过度剔除 | 11.2% vs 宽松 0.0% | 严格>>宽松 → 双向失效 | PASS |
| 62 | T15 | `T15` | 故阈值必须按细胞/核类型分层设定 | True | True | PASS |
| 63 | T16 | `T16` | 细胞级 p 极小(伪重复陷阱) | 5.3e-08 | <1e-3 | PASS |
| 64 | T16 | `T16` | donor 级不显著 → 伪重复被拦住 | 0.263 | >0.05 | PASS |
| 65 | T16 | `T16` | 细胞数<10 的 donor-celltype 被排除 | 2 | >=1 被排除 | PASS |
| 66 | T16 | `T16` | 稀有类型未达标 → 不做推断(非仅标注) | True | True | PASS |
| 67 | T17 | `T17` | 倍数扰动未改变与结局的关联(故无对照价值) | 0.0 | ≈0 → 无对照价值 | PASS |
| 68 | T17 | `T17` | 倍数扰动改变尺度(SD 比≈2,引入混淆) | 2.0 | ≈2.0 | PASS |
| 69 | T17 | `T17` | 置换/分位数扰动使关联归零(干净零对照) | 0.02 | <0.05 | PASS |
| 70 | T17 | `T17` | 置换保留边缘分布(SD 比≈1,无尺度混淆) | 1.0 | 0.9~1.1 | PASS |
| 71 | T17 | `T17` | 计算扰动不构成功能验证 | True | True(措辞纪律) | PASS |
| 72 | 10 | `T06/B01A/B02S` | 竞争事件比例>10% → 强制处理 | 47.3% | >10% | PASS |
| 73 | 10 | `T06/B01A/B02S` | KM(忽略竞争)系统高估累积发生率 | 组0 +0.285, 组1 +0.136 | 两组均高估 | PASS |
| 74 | 10 | `T06/B01A/B02S` | 忽略竞争会扭曲组间差异(非仅整体高估) | 0.148 | >0.01 → 结论可被改变 | PASS |
| 75 | 10 | `T06/B01A/B02S` | 低EPV 乐观度显著大于充足EPV | 0.132 > 0.006 | 低EPV更乐观 | PASS |
| 76 | 10 | `T06/B01A/B02S` | 低EPV 时 EPV<5(触发强制正则化) | True | True | PASS |
| 77 | 10 | `T06/B01A/B02S` | 乐观度随 EPV 单调下降(判据可执行) | 0.132 > 0.058 > 0.006 | 单调下降 | PASS |
| 78 | 10 | `T06/B01A/B02S` | 正常项目判 FEASIBLE | FEASIBLE | FEASIBLE | PASS |
| 79 | 10 | `T06/B01A/B02S` | 多项不可行 → INFEASIBLE(可输出'数据不支持') | INFEASIBLE | INFEASIBLE | PASS |
| 80 | 10 | `T06/B01A/B02S` | 无结局变量 → INFEASIBLE | INFEASIBLE | INFEASIBLE | PASS |
| 81 | 10 | `T06/B01A/B02S` | 单因素可补救 → 降级而非停止 | FEASIBLE_WITH_DOWNGRADE | FEASIBLE_WITH_DOWNGRADE | PASS |
| 82 | 15 | `` | Y6 含可判定要素(predicate/rubric) | True | True | PASS |
| 83 | 15 | `` | Y6 标注输入或阶段标签 | True | True | PASS |
| 84 | 15 | `` | Y6 声明不通过动作 | True | True | PASS |
| 85 | 15 | `` | 空间 含可判定要素(predicate/rubric) | True | True | PASS |
| 86 | 15 | `` | 空间 标注输入或阶段标签 | True | True | PASS |
| 87 | 15 | `` | 空间 声明不通过动作 | True | True | PASS |
| 88 | 15 | `` | MNAR 含可判定要素(predicate/rubric) | True | True | PASS |
| 89 | 15 | `` | MNAR 标注输入或阶段标签 | True | True | PASS |
| 90 | 15 | `` | MNAR 声明不通过动作 | True | True | PASS |
| 91 | 15 | `` | TRIPOD 含可判定要素(predicate/rubric) | True | True | PASS |
| 92 | 15 | `` | TRIPOD 标注输入或阶段标签 | True | True | PASS |
| 93 | 15 | `` | TRIPOD 声明不通过动作 | True | True | PASS |
| 94 | 15 | `` | T08 含可判定要素(predicate/rubric) | True | True | PASS |
| 95 | 15 | `` | T08 标注输入或阶段标签 | True | True | PASS |
| 96 | 15 | `` | T08 声明不通过动作 | True | True | PASS |
| 97 | 15 | `` | 无空规(每条判据均有 predicate 或 rubric) | 无 | 无 | PASS |
| 98 | 16 | `` | [T21] 文献17 Table1 RA(16 vs 4) | 实际=FAIL | 期望=FAIL | PASS |
| 99 | 16 | `` | [T21] 文献17 Table1 T1D(10 vs 6) | 实际=灰区(不得判通过) | 期望=灰区(不得判通过) | PASS |
| 100 | 16 | `` | [T21] 文献17 Table1 MS(99 vs 45) | 实际=PASS | 期望=PASS | PASS |
| 101 | 16 | `` | [T21] 合规对照(50 vs 50) | 实际=PASS | 期望=PASS | PASS |
| 102 | 16 | `` | [T22] 文献18 训练.666/验证.560 | 实际=FAIL | 期望=FAIL | PASS |
| 103 | 16 | `` | [T22] 合规对照(0.80/0.78) | 实际=PASS | 期望=PASS | PASS |
| 104 | 16 | `` | [T23] 文献19 QC(2.5/5/25%,有声明) | 实际=PASS | 期望=PASS | PASS |
| 105 | 16 | `` | [T23] 文献19 变体(阈值不同,无声明) | 实际=FAIL | 期望=FAIL | PASS |
| 106 | 16 | `` | [T23] 合规对照(统一2.5%) | 实际=PASS | 期望=PASS | PASS |
| 107 | 16 | `` | [T24] 文献19 跨物种验证(mouse→human) | 实际=PASS | 期望=PASS | PASS |
| 108 | 16 | `` | [T24] 同队列随机split(典型泄露) | 实际=FAIL | 期望=FAIL | PASS |
| 109 | 16 | `` | [T25] 文献21 手动按位置选ROI | 实际=FAIL | 期望=FAIL | PASS |
| 110 | 16 | `` | [T25] 文献19 盲法选区 | 实际=PASS | 期望=PASS | PASS |
| 111 | 16 | `` | 灰区项数(T21 小样本临界,须声明) | 1 | >=1(夹具含 T1D 10v6) | PASS |
| 112 | 16 | `` | 文献夹具全部按预期拦截/放行(无空规) | 全部符合 | 全部符合 | PASS |
| 113 | 17 | `` | T26 真信号嵌入:应识别为信号 | pass\|sig_frac=0.643,median_p=0.002 | pass/warn | PASS |
| 114 | 17 | `` | T26 弱信噪比嵌入:应降级(探索级) | fail\|sig_frac=0.207,median_p=0.067 | fail | PASS |
| 115 | 17 | `` | T26 噪声冒充嵌入:应被拒(红) | fail\|sig_frac=0.003,median_p=0.505 | fail | PASS |
| 116 | 17 | `` | T26 形状不匹配:应判不适用 | na | na | PASS |
| 117 | 17 | `` | T27 微调合规(n_calib=30) | pass\|策略=微调,n_calib=30,overlap=0.72 | pass | PASS |
| 118 | 17 | `` | T27 微调但 n_calib=8(<20) | fail\|策略=微调,n_calib=8,overlap=0.72 | fail | PASS |
| 119 | 17 | `` | T27 零样本但调过参(泄漏) | fail\|策略=零样本迁移,overlap=0.60 | fail | PASS |
| 120 | 17 | `` | T27 从头训练却声称'跨数据集复现' | fail\|策略=从头训练,overlap=0.60 | fail | PASS |
| 121 | 17 | `` | T27 零样本合规 | pass\|策略=零样本迁移,overlap=0.65 | pass | PASS |
| 122 | 17 | `` | T27 未提供批次诊断 | fail\|策略=零样本迁移 | fail/warn | PASS |
| 123 | 17 | `` | T28 真实模块结构:扰动应可复现 | warn\|模块富集 p=0.0204,rho=0.412,对照rho=0.037 | pass/warn | PASS |
| 124 | 17 | `` | T28 假阴性对照(同模块):应判特异性不成立 | fail\|模块富集 p=0.0204,rho=0.226,对照rho=0.762 | fail | PASS |
| 125 | 17 | `` | T28 虚拟KO写'功能验证':应拦 | fail | fail | PASS |
| 126 | 17 | `` | T29 完整举证:应通过 | pass\|子群体数=3,高风险群体=1,退出选项=True,隐私缺失已报告=Tr | pass | PASS |
| 127 | 17 | `` | T29 声称充分却无子群样本量+未评风险却称可临床转化 | fail\|无举证 | fail | PASS |
| 128 | 17 | `` | T29 纯方法学:应判不适用 | na | na | PASS |
| 129 | 17 | `` | T29 N/A 空理由应抛错 | ValueError | raise | PASS |
| 130 | 17 | `` | T30 完整声明:应通过 | pass\|流向=本地设备内,特征可见=True,可撤回=True,二次使用=仅限 | pass | PASS |
| 131 | 17 | `` | T30 称匿名无方法 + GDPR无法条:应拦 | fail\|流向=本地,特征可见=True,可撤回=True,二次使用=仅限本研究 | fail | PASS |
| 132 | 17 | `` | T30 公开二次数据:应判不适用 | na | na | PASS |
| 133 | 20 | `` | [T31] 肺癌4基因(Wen 2022 BMC Cancer 22:1 | 实际=不通过 | 期望=不通过 | PASS |
| 134 | 20 | `` | [T31] 卵巢癌2基因(Liang 2021 Front Oncol  | 实际=不通过 | 期望=不通过 | PASS |
| 135 | 20 | `` | [T31] ROMO1(Wang 2025 FIG 25:91) | 实际=不通过 | 期望=不通过 | PASS |
| 136 | 20 | `` | [T31] 合规:嵌套 CV | 实际=通过 | 期望=通过 | PASS |
| 137 | 20 | `` | [T31] 合规:独立选择集 | 实际=通过 | 期望=通过 | PASS |
| 138 | 20 | `` | [T31] 声称 nested 但无折数 → 判不了 | 实际=不适用 | 期望=不适用 | PASS |
| 139 | 20 | `` | [T31] 未声明选择位置 → 判不了 | 实际=不适用 | 期望=不适用 | PASS |
| 140 | 20 | `` | [T31] 非建模研究 → N/A | 实际=不适用 | 期望=不适用 | PASS |
| 141 | 20 | `` | [T31] WCMI:选了20个特征但完全未报冗余度 | 实际=不通过 | 期望=不通过 | PASS |
| 142 | 20 | `` | [T31] |r|>0.9 占比 45% 未去冗余 | 实际=不通过 | 期望=不通过 | PASS |
| 143 | 20 | `` | [T31] |r|>0.9 占比 12%(灰区) | 实际=通过 | 期望=通过 | PASS |
| 144 | 20 | `` | [T31] |r|>0.9 占比 6%(可解释特征集) | 实际=通过 | 期望=通过 | PASS |
| 145 | 20 | `` | [T32] ROMO1 two-sample MR(Wang 2025) | 实际=不通过 | 期望=不通过 | PASS |
| 146 | 20 | `` | [T32] 弱工具变量 F=8 | 实际=不通过 | 期望=不通过 | PASS |
| 147 | 20 | `` | [T32] 多效性存在但未用稳健方法 | 实际=不通过 | 期望=不通过 | PASS |
| 148 | 20 | `` | [T32] two-sample 祖源不一致 | 实际=不通过 | 期望=不通过 | PASS |
| 149 | 20 | `` | [T32] 措辞越界:证明因果 | 实际=不通过 | 期望=不通过 | PASS |
| 150 | 20 | `` | [T32] 合规 MR | 实际=通过 | 期望=通过 | PASS |
| 151 | 20 | `` | [T32] 非 MR 研究 → N/A | 实际=不适用 | 期望=不适用 | PASS |
| 152 | 20 | `` | T31/T32 夹具全部按预期拦截或放行(无空规) | 全部符合 | 全部符合 | PASS |
| 153 | 20 | `` | [T13b扩展] 合规:小样本+预注册 alpha | 实际=pass | 期望=pass | PASS |
| 154 | 20 | `` | [T13b扩展] 事后调 alpha → R3 | 实际=fail | 期望=fail | PASS |
| 155 | 20 | `` | [T13b扩展] 借用但无 alpha | 实际=fail | 期望=fail | PASS |
| 156 | 20 | `` | [T13b扩展] 借用但无确定方法 | 实际=fail | 期望=fail | PASS |
| 157 | 20 | `` | [T13b扩展] 声明未借用 | 实际=pass | 期望=pass | PASS |
| 158 | 20 | `` | [T13b扩展] 小样本但未声明 → 判不了 | 实际=na | 期望=na | PASS |
| 159 | 20 | `` | [T13b扩展] 大样本 → 不触发 | 实际=na | 期望=na | PASS |
| 160 | 20 | `` | 数值反例:事后调 alpha 的偏差恒大于预注册 | 0.000→0.188,0.000→0.168,0.050→0.202 | 全部 偏差(事后调) > 偏差(预注册) | PASS |
| 161 | 21 | `` | [T33] 5/10000 Treg 声称显著下降(Wilson CI) | 实际=不通过 | 期望=不通过 | PASS |
| 162 | 21 | `` | [T33] 120/10000 Treg(比例仍<5%,CI 可辨) | 实际=通过 | 期望=通过 | PASS |
| 163 | 21 | `` | [T33] MAF=0.5% 但未报 CI 且无 k/n | 实际=不适用 | 期望=不适用 | PASS |
| 164 | 21 | `` | [T33] 2 年数据估 20 年一遇极端事件 | 实际=不通过 | 期望=不通过 | PASS |
| 165 | 21 | `` | [T33] 2 年数据 + 已声明 GPD 外推 | 实际=不通过 | 期望=不通过 | PASS |
| 166 | 21 | `` | [T33] 39 年数据估 20 年一遇(可解析) | 实际=通过 | 期望=通过 | PASS |
| 167 | 21 | `` | [T33] n=50 估 <1% 频率 → 判不了 | 实际=不适用 | 期望=不适用 | PASS |
| 168 | 21 | `` | [T33] 非稀有事件研究 → N/A | 实际=不适用 | 期望=不适用 | PASS |
| 169 | 21 | `` | [T34] 只报平均 RMSE,无 per-sample 不确定性 | 实际=不通过 | 期望=不通过 | PASS |
| 170 | 21 | `` | [T34] 有不确定性但未报与误差的关系 | 实际=不通过 | 期望=不通过 | PASS |
| 171 | 21 | `` | [T34] corr=0.6 但未分层报告 | 实际=不通过 | 期望=不通过 | PASS |
| 172 | 21 | `` | [T34] corr=0.6 且已分层 | 实际=通过 | 期望=通过 | PASS |
| 173 | 21 | `` | [T34] 声称个体风险但 corr≈0(不确定性无信息) | 实际=不通过 | 期望=不通过 | PASS |
| 174 | 21 | `` | [T34] 高不确定亚群未声明 | 实际=不通过 | 期望=不通过 | PASS |
| 175 | 21 | `` | [T34] 非预测研究 → N/A | 实际=不适用 | 期望=不适用 | PASS |
| 176 | 21 | `` | [T35] ROMO1:LDSC+MTAG,intercept 未报告 | 实际=不通过 | 期望=不通过 | PASS |
| 177 | 21 | `` | [T35] LDSC intercept=1.2 未校正 | 实际=不通过 | 期望=不通过 | PASS |
| 178 | 21 | `` | [T35] LDSC intercept=1.02(接近 1) | 实际=通过 | 期望=通过 | PASS |
| 179 | 21 | `` | [T35] MTAG 循环论证 | 实际=不通过 | 期望=不通过 | PASS |
| 180 | 21 | `` | [T35] coloc H4=0.9 但先验未声明 | 实际=不适用 | 期望=不适用 | PASS |
| 181 | 21 | `` | [T35] coloc H4=0.9 + 先验已声明 | 实际=通过 | 期望=通过 | PASS |
| 182 | 21 | `` | [T35] 未用多组学工具 → N/A | 实际=不适用 | 期望=不适用 | PASS |
| 183 | 21 | `` | [V4MODAL] in silico + in vitro 却声称治疗 | 实际=不通过 | 期望=不通过 | PASS |
| 184 | 21 | `` | [V4MODAL] in silico + in vitro 声称细胞模 | 实际=通过 | 期望=通过 | PASS |
| 185 | 21 | `` | [V4MODAL] 仅 in silico 声称候选 | 实际=通过 | 期望=通过 | PASS |
| 186 | 21 | `` | [V4MODAL] 仅 in silico 却声称临床效用 | 实际=不通过 | 期望=不通过 | PASS |
| 187 | 21 | `` | [V4MODAL] 全链条达成 L4 | 实际=通过 | 期望=通过 | PASS |
| 188 | 21 | `` | [V4MODAL] 未报告任何验证证据 → 判不了 | 实际=不适用 | 期望=不适用 | PASS |
| 189 | 21 | `` | [V4MODAL] 非跨模态研究 → N/A | 实际=不适用 | 期望=不适用 | PASS |
| 190 | 21 | `` | T33/T34/T35/V4MODAL 夹具全部按预期拦截或放行(无空规 | 全部符合 | 全部符合 | PASS |
| 191 | 21 | `` | 数值反例:短记录无法解析极端事件概率(1年→39年) | 4/5 个短记录无法解析 | 存在无法解析的短记录 | PASS |
| 192 | 21 | `` | 数值反例:不确定性信息量(信息性 vs 无信息) | 信息性=0.892, 无信息=-0.031 | 信息性 > 0.3 且 无信息 < 0.1 | PASS |
| 193 | 21 | `` | 数值反例:5/10000 稀有比例的 Wilson CI 宽度 | CI=[0.000214,0.00117] 相对宽度=1.913 上下界比=5. | 相对宽度 > 1.0 且 上下界比 > 2 | PASS |
| 194 | 21 | `` | [T31冗余度] WCMI:选了20个特征但完全未报冗余度 | 实际=不通过 | 期望=不通过 | PASS |
| 195 | 21 | `` | [T31冗余度] |r|>0.9 占比 45% 未去冗余 | 实际=不通过 | 期望=不通过 | PASS |
| 196 | 21 | `` | [T31冗余度] |r|>0.9 占比 12%(灰区) | 实际=通过 | 期望=通过 | PASS |
| 197 | 21 | `` | [T31冗余度] |r|>0.9 占比 6%(可解释特征集) | 实际=通过 | 期望=通过 | PASS |
| 198 | 22 | `` | [T03] 合规:声明与执行一致(20000) | pass | pass | PASS |
| 199 | 22 | `` | [T03] 声明全基因组却只校正 500 个:应拦 | fail | fail | PASS |
| 200 | 22 | `` | [T03] 未声明家族:应判不适用 | na | na | PASS |
| 201 | 22 | `` | [T09] 合规:声明 CV 概率 | pass | pass | PASS |
| 202 | 22 | `` | [T09] 声明样本内概率:应拦 | fail | fail | PASS |
| 203 | 22 | `` | [T09] 未声明来源:应判不适用 | na | na | PASS |
| 204 | 22 | `` | [T12] 合规:落差 0.02 | pass | pass | PASS |
| 205 | 22 | `` | [T12] 样本内 1.000 / 样本外 0.544:应警告 | warn | warn | PASS |
| 206 | 22 | `` | [T12] 缺样本内指标:应判不适用 | na | na | PASS |
| 207 | 22 | `` | [T18] 合规:4 个变体全报 | pass | pass | PASS |
| 208 | 22 | `` | [T18] 预注册 4 个仅报 2 个:应拦 | fail | fail | PASS |
| 209 | 22 | `` | [T18] 报告未预注册变体:应警告 | warn | warn | PASS |
| 210 | 22 | `` | [T18] 未预注册:应判不适用 | na | na | PASS |
| 211 | 22 | `` | T03/T09/T12/T18 夹具全部按预期拦截或放行(无空规) | 无偏差 | 无偏差 | PASS |
| 212 | 23 | `` | [T36] ROMO1 多组学:只报全组件性能,无消融 → 应拦 | fail | fail | PASS |
| 213 | 23 | `` | [T36] p53 四组学:缺 leave-one-out → 应拦 | fail | fail | PASS |
| 214 | 23 | `` | [T36] 合规:三组件均有增量且 CI 不跨 0 | pass | pass | PASS |
| 215 | 23 | `` | [T36] 一个组件 CI 跨 0 → 应警告 | warn | warn | PASS |
| 216 | 23 | `` | [T36] 全部组件无增量 → 应拦(循环论证) | fail | fail | PASS |
| 217 | 23 | `` | [T36] 单组件研究 → 应判不适用 | na | na | PASS |
| 218 | 23 | `` | T36 夹具全部按预期拦截或放行(无空规) | 无偏差 | 无偏差 | PASS |
| 219 | E | `G-E` | 结论方向全部一致 | True | True | PASS |
| 220 | E | `G-E` | 点估计极差 >10% 须报告 | 0.312 | >0.10 → 报告 | PASS |
| 221 | P3 | `P3` | 隔离守卫绕过测试(含 Win 反斜杠/大小写) | 7/7 通过 | 7/7 | PASS |
| 222 | P3 | `P3` | 正式台账写入被拦截 | True | True | PASS |

## D. 排版判定项(全量)

总计 **70** 项。

| # | 编号 | 判据 | 实测 | 结果 |
|---|---|---|---|---|
| 1 | `C-1.1` | 无孤儿图 | 无 | PASS |
| 2 | `C-1.2` | 无悬空图引用 | 无 | PASS |
| 3 | `C-1.3` | 无孤儿表 | 无 | PASS |
| 4 | `C-1.4` | 无悬空表引用 | 无 | PASS |
| 5 | `C-1.5` | 图序连续无缺号 | [1, 2, 3] | PASS |
| 6 | `C-1.6` | 补充材料编号连续无缺号 | [1] | PASS |
| 7 | `C-2.1` | 台账数字均可在正文定位 | 2/2 | PASS |
| 8 | `C-2.2` | 无台账外数字 | 无 | PASS |
| 9 | `C-3.1` | 压缩未删除限定词 | 无丢失 | PASS |
| 10 | `C-3.2` | 压缩未新增升级词 | 无新增 | PASS |
| 11 | `C-3.3` | 限定词密度未崩塌(阈值可配) | 82.5→101.3‰ | PASS |
| 12 | `C-3.4` | 限定词绝对计数未减少 | 8→8 | PASS |
| 13 | `C-4` | 01_qc_violin_mito.pdf          | 合规 | PASS |
| 14 | `C-4` | 02_deg_volcano.pdf             | 合规 | PASS |
| 15 | `C-6` | 01_qc_violin_mito.jpg    01_qc_violin_mi | 70KB | PASS |
| 16 | `C-6` | 01_qc_violin_mito.jpg    01_qc_violin_mi | 300 | PASS |
| 17 | `C-6` | 01_qc_violin_mito.jpg    01_qc_violin_mi | RGB | PASS |
| 18 | `C-6` | 01_qc_violin_mito.jpg    01_qc_violin_mi | 88.1 | PASS |
| 19 | `C-6` | 01_qc_violin_mito.jpg    命名匹配 02_deg_vol | 01_qc_violin_mito.jpg | PASS |
| 20 | `C-6` | 01_qc_violin_mito.jpg    analysis_step 属 | qc | PASS |
| 21 | `C-6` | 01_qc_violin_mito.pdf    01_qc_violin_mi | 14KB | PASS |
| 22 | `C-6` | 01_qc_violin_mito.pdf    01_qc_violin_mi | ['/Type0'] | PASS |
| 23 | `C-6` | 01_qc_violin_mito.pdf    命名匹配 02_deg_vol | 01_qc_violin_mito.pdf | PASS |
| 24 | `C-6` | 01_qc_violin_mito.pdf    analysis_step 属 | qc | PASS |
| 25 | `C-6` | 01_qc_violin_mito.png    01_qc_violin_mi | 47KB | PASS |
| 26 | `C-6` | 01_qc_violin_mito.png    01_qc_violin_mi | 300.0 | PASS |
| 27 | `C-6` | 01_qc_violin_mito.png    01_qc_violin_mi | RGBA | PASS |
| 28 | `C-6` | 01_qc_violin_mito.png    01_qc_violin_mi | 88.1 | PASS |
| 29 | `C-6` | 01_qc_violin_mito.png    命名匹配 02_deg_vol | 01_qc_violin_mito.png | PASS |
| 30 | `C-6` | 01_qc_violin_mito.png    analysis_step 属 | qc | PASS |
| 31 | `C-6` | 01_qc_violin_mito.tiff   01_qc_violin_mi | 249KB | PASS |
| 32 | `C-6` | 01_qc_violin_mito.tiff   01_qc_violin_mi | 600 | PASS |
| 33 | `C-6` | 01_qc_violin_mito.tiff   01_qc_violin_mi | RGBA | PASS |
| 34 | `C-6` | 01_qc_violin_mito.tiff   01_qc_violin_mi | 88.1 | PASS |
| 35 | `C-6` | 01_qc_violin_mito.tiff   命名匹配 02_deg_vol | 01_qc_violin_mito.tiff | PASS |
| 36 | `C-6` | 01_qc_violin_mito.tiff   analysis_step 属 | qc | PASS |
| 37 | `C-6` | 02_deg_volcano.jpg       02_deg_volcano. | 53KB | PASS |
| 38 | `C-6` | 02_deg_volcano.jpg       02_deg_volcano. | 300 | PASS |
| 39 | `C-6` | 02_deg_volcano.jpg       02_deg_volcano. | RGB | PASS |
| 40 | `C-6` | 02_deg_volcano.jpg       02_deg_volcano. | 86.2 | PASS |
| 41 | `C-6` | 02_deg_volcano.jpg       命名匹配 02_deg_vol | 02_deg_volcano.jpg | PASS |
| 42 | `C-6` | 02_deg_volcano.jpg       analysis_step 属 | deg | PASS |
| 43 | `C-6` | 02_deg_volcano.pdf       02_deg_volcano. | 8KB | PASS |
| 44 | `C-6` | 02_deg_volcano.pdf       02_deg_volcano. | ['/Type0'] | PASS |
| 45 | `C-6` | 02_deg_volcano.pdf       命名匹配 02_deg_vol | 02_deg_volcano.pdf | PASS |
| 46 | `C-6` | 02_deg_volcano.pdf       analysis_step 属 | deg | PASS |
| 47 | `C-6` | 02_deg_volcano.png       02_deg_volcano. | 27KB | PASS |
| 48 | `C-6` | 02_deg_volcano.png       02_deg_volcano. | 300.0 | PASS |
| 49 | `C-6` | 02_deg_volcano.png       02_deg_volcano. | RGBA | PASS |
| 50 | `C-6` | 02_deg_volcano.png       02_deg_volcano. | 86.2 | PASS |
| 51 | `C-6` | 02_deg_volcano.png       命名匹配 02_deg_vol | 02_deg_volcano.png | PASS |
| 52 | `C-6` | 02_deg_volcano.png       analysis_step 属 | deg | PASS |
| 53 | `C-6` | 02_deg_volcano.tiff      02_deg_volcano. | 258KB | PASS |
| 54 | `C-6` | 02_deg_volcano.tiff      02_deg_volcano. | 600 | PASS |
| 55 | `C-6` | 02_deg_volcano.tiff      02_deg_volcano. | RGBA | PASS |
| 56 | `C-6` | 02_deg_volcano.tiff      02_deg_volcano. | 86.2 | PASS |
| 57 | `C-6` | 02_deg_volcano.tiff      命名匹配 02_deg_vol | 02_deg_volcano.tiff | PASS |
| 58 | `C-6` | 02_deg_volcano.tiff      analysis_step 属 | deg | PASS |
| 59 | `C-6` | 规范自证 颜色取自 Wong2011 色板 | 6/6 | PASS |
| 60 | `C-6` | 规范自证 红绿色盲模拟后最小 ΔE ≥ 10.0(强制) | 14.53 | PASS |
| 61 | `C-6` | 规范自证 protanopia 可区分(强制) | 14.53 | PASS |
| 62 | `C-6` | 规范自证 deuteranopia 可区分(强制) | 15.99 | PASS |
| 63 | `C-6` | 规范自证 tritanopia 可区分(建议) | 8.65 | PASS |
| 64 | `C-6` | 规范自证 无等亮度红绿对(色板外,建议级) | 无 | PASS |
| 65 | `C-5.1` | 正文字数 <= 3500 | 47 | PASS |
| 66 | `C-5.2` | 图数量 <= 8 | 3 | PASS |
| 67 | `C-5.3` | 表数量 <= 4 | 1 | PASS |
| 68 | `C-5.4` | 参考文献 <= 60 | 42 | PASS |
| 69 | `V5DECL` | 声明项已覆盖 8/8 | 8 | PASS |
| 70 | `V5DECL` | 缺声明的稿件可被检出(反向验证) | 缺 8 项 | PASS |

## E. Handoff 链摘要

节点 **14**,已确认 **5**,在途 **9**。

| # | handoff_id | 交付→接收 | 输出状态 | ack |
|---|---|---|---|---|
| 1 | H01_USER2C_需求启动 | USER→C | 不适用(需求层,无判据状态) | 已确认 |
| 2 | H02_C2A_S01任务卡 | C→A | 已执行 | 已确认 |
| 3 | H03_A2C_S03框架核定 | A→C | 条件通过(4 条 condition) | **在途** |
| 4 | H04_C2A_S07复核 | C→A | 已执行 | 已确认 |
| 5 | H05_A2C_S07判据定稿 | A→C | 条件通过 | **在途** |
| 6 | H06_A2C_S13回执 | A→C | v1.5 确认 + 判定分工修订为 14 项 | **在途** |
| 7 | H07_A2C_S15判据闭合 | A→C | 8/8 分支实跑验证 | **在途** |
| 8 | H08_C不可用_断电 | USER→A | 阻塞(C 不可用) | 已确认 |
| 9 | H09_A2USER_S16恢复包 | A→USER | 已交付 | **在途** |
| 10 | H10_A2USER_交叉验证包 | A→USER | 已交付(用户转交第二 agent 交叉验证) | **在途** |
| 11 | H11_A2USER_审核包 | A→USER | 已交付(单文件自包含) | **在途** |
| 12 | H12_USER2A_首轮审核反馈 | USER→A | 需修订后使用 | 已确认 |
| 13 | H13_A2USER_S20复审响应 | A→USER | P0 闭环(一致性守卫 14/14) | **在途** |
| 14 | H14_A2USER_S21缺口补齐 | A→USER | 待审核 | **在途** |

> 链原始 JSON:`handoff/handoff_chain.json`(48 项字段,此处为摘要)。

## F. `schema/criteria.yaml` 全文(审计 §9 要求补齐)

判据条数:**51**

````yaml
conflict_resolution: S21 > S15-C > A2C
criteria:
- decidable: auto
  id: T01
  inputs:
  - data_matrix
  - scale_declaration
  - test_family
  name: 尺度声明与检验家族
  note: 家族须含终点数/亚组数/模型数/分子数四类(S21-A §4)
  on_fail:
    action: V3否决
    blocking: true
  pass_when: true
  predicate: scale_declared(data_matrix) and family_declared(test_family)
  requires_tools:
  - scale_check
  stage_tag: deg
  version: 1.8+
- decidable: auto
  id: T03
  inputs:
  - pvalues
  - declared_family
  - correction_method
  name: 多重检验校正范围
  on_fail:
    action: V3否决
    blocking: true
  pass_when: true
  predicate: correction_matches(pvalues, declared_family, correction_method)
  requires_tools:
  - bh_correct
  - bonferroni
  stage_tag: enrichment
  version: 1.8+
- decidable: hybrid
  id: T06
  inputs:
  - time
  - event
  - competing_events
  - exposure_def
  - method_choice
  judge_rubric: 人工判定:①是否声明竞争风险处理方式(Cause-specific vs Fine-Gray 二选一并预注册)?②是否评估 immortal time bias(landmark
    或时间依赖协变量)?③cox.zph 不满足时是否改用 RMST?④中位随访与 at-risk 表是否报告?缺①在老年/肿瘤/心血管队列中直接不通过。
  name: 时间-事件分析(含竞争风险与时间偏倚)
  note: S21-A §1/§2。竞争事件>10% 强制处理;CSH/Fine-Gray 须二选一预注册
  on_fail:
    action: T06不通过
    blocking: true
  pass_when: true
  predicate: 'competing_declared(competing_events)

    and (prop_competing <= 0.10 or competing_handled(method_choice))

    and (not exposure_requires_survival(exposure_def) or landmark_or_tdcov_used())

    '
  requires_tools:
  - cif_estimator
  - km_estimator
  - cox_ph_test
  stage_tag: survival
  version: 1.8+
- decidable: hybrid
  id: T08
  inputs:
  - missing_prop
  - mechanism
  - sensitivity_done
  judge_rubric: 人工判定:①缺失机制判断(MCAR/MAR/MNAR)是否有依据?②插补模型是否含结局变量?③是否报告了缺失比例与模式?④MNAR 是否做了 tipping point /
    delta 调整?缺②直接不通过。
  name: 缺失数据处理(含 MNAR)
  note: S21-A §5。MNAR 为默认情形,须 tipping point / pattern-mixture / reference-based
  on_fail:
    action: T08不通过
    blocking: true
  pass_when: true
  predicate: '(missing_prop <= 0.05) or sensitivity_done

    and (missing_prop <= 0.20 or tipping_point_reported)

    '
  requires_tools:
  - missingness_report
  stage_tag: imputation
  version: 1.8+
- decidable: auto
  id: T09
  inputs:
  - p_pred
  - source
  name: 预测概率来源声明
  note: S21-C §4。禁止用样本内概率做性能评估
  on_fail:
    action: T09不通过
    blocking: true
  pass_when: true
  predicate: source_declared(p_pred) and source != 'insample'
  requires_tools: []
  stage_tag: prediction
  version: 1.8+
- decidable: auto
  id: T12
  inputs:
  - acc_insample
  - auc_outsample
  name: 过拟合警示
  on_fail:
    action: 强制标注过拟合警示
    blocking: false
  pass_when: true
  predicate: not (acc_insample > 0.95 and (acc_insample - auc_outsample) > 0.05)
  requires_tools: []
  stage_tag: performance
  version: 1.8+
- aliases:
  - T13(a)
  - T13a
  decidable: auto
  id: T13a
  inputs:
  - auc_base_ci
  - auc_full_ci
  - dAUC_ci
  name: 临床增量
  on_fail:
    action: F3
    blocking: true
  pass_when: true
  predicate: dAUC >= 0.05 and ci_lower(dAUC) > 0
  requires_tools:
  - bootstrap
  - roc_auc
  version: 1.8+
- decidable: auto
  id: T13b
  inputs:
  - y_true
  - p_pred_cv
  - n_boot
  invariants:
  - 必须使用 CV 预测概率;训练内拟合斜率恒等于 1(数学必然)
  model_family_caveat: '"恒等于 1" 仅对**未正则化 logistic** 成立(MLE 一阶条件); ridge / lasso / EN 与 RF / GBM / SVM
    不恒成立,但同样系统性乐观。 故**所有模型族**均须用 CV 预测概率。脚本分支 3 用未正则化 logistic, 验证的是"恒等于 1"的下界 —— 可接受,条文须写明此限定。'
  name: 校准判据
  on_fail:
    action: F3 + 不得用于个体风险预测
    blocking: true
  pass_when: true
  predicate: ci_covers(calibration_slope_bootstrap(y_true, p_pred_cv, n_boot), 1.0)
  requires_tools:
  - bootstrap
  - logistic_fit
  small_sample_extension:
    rationale: 'TL-BEE 实测:n=5~50 时,用 importance sampling + control variates 借源域

      信息后,误差估计的 MSE 显著低于传统估计器。但相关度若事后调参,

      等于把结论塞进先验 —— 故必须预注册。

      '
    required:
    - 必须声明是否使用源域信息(迁移学习 / 先验 / 数据增强)
    - 若使用,必须报告源域-目标域**相关度系数** |alpha| 及其确定方法
    - 若 |alpha| 系"看着结果调出来的" → R3 回退
    trigger: 目标域 n < 50
  version: 1.8+
- aliases:
  - T13(c)
  - T13c
  blocking_threshold: 12
  clinical_magnitude: 胜出率高≠临床有意义;中位|ΔNB|<0.01 视为无临床意义
  decidable: auto
  deprecated_indicators:
  - 最优子区间胜出率与落差 —— O(n^2) 候选下几乎必然≈1.0,等价于 1-win_rate_full,无独立判别力
  grey_zone: 8 < neg_run < 12 → 判'不确定',不得判通过
  id: T13c
  inputs:
  - y_true
  - p_candidate
  - p_baseline
  - preregistered_range
  must_report:
  - 'net_benefit_diff_median_abs: 预注册区间内 |NB差| 中位数(强制)'
  - 'net_benefit_diff_min: 预注册区间内 NB 差最小值(强制)'
  name: 决策意义(DCA)
  on_fail:
    action: F3
    blocking: true
  pass_when: verdict == True
  predicate: '# v1.6:判定式必须引用 warning/blocking 阈值,禁止硬编码 10 点单阈值。

    # v1.6 前为硬编码单阈值(neg_run 对 10 取小于),与 warning=8/blocking=12 矛盾,已废。

    win_rate_full <- mean(nb_diff(y_true, p_candidate, p_baseline, grid) > 0)

    neg_run <- max_consecutive_negative(nb_diff, preregistered_range)

    if neg_run >= blocking_threshold:   verdict <- F3

    elif neg_run >= warning_threshold:  verdict <- ''不确定''   # 灰区,不得判通过

    else: verdict <- (win_rate_full >= 0.5)

    verdict

    '
  requires_tools:
  - net_benefit
  - curve_shape
  stage_tag: dca
  threshold_note: 连续负 ≥10 点为默认,无临床推导依据;填槽阶段按场景定死 (卒中 0.15-0.35 / 筛查 0.05-0.20)。建议双阈值 8 点警告 / 12 点阻断, 中间判不确定
    —— 已校准模型实测 8 点,单阈值余量仅 2。
  threshold_note_struct: 10 点为默认单阈值,无临床推导依据;上列双阈值自 v1.6 起生效。 已校准模型实测 8 点,单阈值余量仅 2 —— 边界情形(9-11 点)既漏检又误伤。
  version: 1.8+
  warning_threshold: 8
- decidable: auto
  id: T14
  inputs:
  - est_discovery_ci
  - est_replication_ci
  - I2
  - power_replication
  k2_caveat: k=2 时 I2 的 df=1、Q 期望=1,随机波动即可接近 50% —— 不得作唯一判据
  merge_aux_criteria:
  - A 事件率差异 < 15%(独立判据)
  - B 方向一致(效应同号)
  - C 终点定义等价性 —— 一票否决
  - I2 > 50% 强制报告异质性来源;> 75% 禁合并主分析
  method_layer:
    claim_ceiling:
      从头训练: 不得声称"跨数据集复现"(那只是独立建模)
      微调(n_calib>=20): 在该 SOP 下可复用
      零样本迁移: 在目标数据上方向一致
    enforcement: T14 与 T27 是**同一次复现判定的两面**,缺 T27 则 T14 无效
    predicate: T27 的迁移策略声明必须同时满足(T14 结果层 AND T27 方法层)
    rationale: '一个在目标数据上调过参的模型,两个 CI 当然会重叠 —— 那不是泛化,是泄漏。

      故 T14 不得单独通过:必须与 T27 配对判定。

      '
    required: true
  name: 复现判定
  on_fail:
    action: R5
    blocking: false
  pass_when: verdict == True
  predicate: "if power_replication < 0.8:\n    verdict <- \"不确定(复现集功效不足)\"     # S21-A §3.6:区分\"没复现\"\
    与\"没能力复现\"\nelse:\n    verdict <- same_direction and ci_overlap and (I2 <= 50)\n"
  requires_tools:
  - heterogeneity
  - power_calc
  stage_tag: replication
  version: 1.8+
- decidable: auto
  id: T15
  inputs:
  - assay_type
  - mito_pct
  - thresholds
  name: 单细胞 QC 阈值
  note: 核/单核 mito% 分布不同,单阈值跨类型套用双向失效
  on_fail:
    action: T15不通过
    blocking: false
  pass_when: true
  predicate: thresholds_stratified_by_assay_type(assay_type, thresholds)
  requires_tools:
  - qc_summary
  stage_tag: qc
  version: 1.8+
  wording_fix: 单核 RNA 为**另行设定更严阈值 <5%**,非放宽(旧文写放宽至 <5%,措辞误导)
  wording_rationale: 核 RNA 线粒体含量本就低,照抄胞质 RNA 的 <15% 会把目标细胞滤光
- decidable: hybrid
  id: T16
  inputs:
  - counts
  - donor_id
  - celltype
  - n_cells_per_unit
  - annotation_method
  - composition_method
  judge_rubric: 人工判定:①推断单位是否为 donor/患者(非细胞)?②每 (donor,celltype) 细胞数是否达标?③稀有类型是否走了'合并相近类型'路径?④是否用 pseudobulk?
  name: 单细胞推断单位与组成
  on_fail:
    action: 硬阻断(伪重复)
    blocking: true
  pass_when: true
  predicate: 'inference_unit == "donor"

    and all(n_cells_per_unit >= 10 or excluded)

    and annotation_method_preregistered

    and (composition_question == False or composition_method_preregistered)

    '
  rare_celltype_path: 第三条路径:稀有类型在多 donor 上均不足 10 细胞时,允许合并相近细胞类型后做 pseudobulk(须预注册合并方案并声明)
  rare_celltype_rationale: 仅有排除一条路径会系统性滤掉 Treg、浆细胞等稀有类型,反而掩盖真实信号
  requires_tools:
  - pseudobulk
  - rank_test
  stage_tag: deg
  version: 1.8+
- decidable: auto
  id: T17
  inputs:
  - perturbation_method
  - edge_distribution_preserved
  invariants:
  - 倍数扰动不改变与结局的关联,无对照价值,且改变尺度引入混淆
  - 计算扰动不构成功能验证
  name: 计算扰动
  on_fail:
    action: T17不通过
    blocking: false
  pass_when: true
  predicate: perturbation_method in ['quantile','permutation']
  requires_tools: []
  stage_tag: perturbation
  version: 1.8+
- decidable: auto
  id: T18
  inputs:
  - results_all_variants
  - preregistered_variants
  name: 敏感性分析
  note: 程序定义已移出必做项,改立 B03-A(S15-C §4)
  on_fail:
    action: 硬阻断(选择性报告)
    blocking: true
  pass_when: true
  predicate: reported_all(results_all_variants, preregistered_variants)
  requires_tools: []
  stage_tag: sensitivity
  version: 1.8+
- decidable: hybrid
  id: Y6
  inputs:
  - batch_structure
  - coupling_diagnosis
  - method_preregistered
  judge_rubric: 人工判定:①批次与生物学条件是否耦合?(耦合→禁止当协变量回归、禁止盲用 ComBat)②若未耦合,整合方法(ComBat/Harmony/scVI/rliger)是否预注册?③批次校正后是否仍宣称'独立复现'?④校正前后是否都做了可视化对比?
  name: 批次整合
  note: S21-B §1。批次校正后的内部子集不得称'独立验证集'
  on_fail:
    action: R3回退 或 硬阻断
    blocking: true
  pass_when: true
  predicate: 'batch_structure_documented

    and (not fully_coupled or no_correction_applied)

    and method_preregistered

    '
  requires_tools: []
  stage_tag: batch
  version: 1.8+
- aliases:
  - B01-A
  - B01A
  decidable: auto
  id: B01A
  inputs:
  - n_events
  - n_candidates
  - alpha
  - power
  - MDE
  name: 功效与样本量论证
  on_fail:
    action: B02不得启动
    blocking: true
  pass_when: true
  predicate: 'EPV <- n_events / n_candidates

    (EPV >= 10) or (regularization_applied) or (EPV >= 5 and dimension_reduced)

    '
  requires_tools:
  - epv_calc
  - power_calc
  version: 1.8+
- aliases:
  - B02-S
  - B02S
  decidable: auto
  id: B02S
  inputs:
  - independent_ok
  - outcome_available
  - epv
  - missing_key_prop
  name: 可行性判定(止损出口)
  note: INFEASIBLE 是合法且受尊重的结论,禁止为规避而硬判
  on_fail:
    action: 'INFEASIBLE: 停止并输出限制与补救建议'
    blocking: true
  pass_when: state in ['FEASIBLE','FEASIBLE_WITH_DOWNGRADE']
  predicate: feasibility_gate(...)
  requires_tools: []
  version: 1.8+
- aliases:
  - B03-A
  - B03A
  decidable: hybrid
  id: B03A
  inputs:
  - candidate_definitions
  - effect_per_definition
  - selection_rationale
  judge_rubric: 人工判定:①程序定义变体是否为成员集层面的实质变更(Jaccard ≤ 0.5,排除纯权重缩放与部分归零)?②是否在分析前冻结并留痕?③是否只做了权重缩放(橡皮图章)?④报告的是否为全部变体(含不显著的)?
  name: 程序/签名定义论证
  on_fail:
    action: 不得冻结
    blocking: true
  overlap_evidence: 实测 相对重叠 32.5% vs Jaccard 19.4% —— 两口径不等,条文须指定
  overlap_metric: Jaccard = |A∩B|/|A∪B| —— 非相对重叠 |A∩B|/|A|
  overlap_threshold: Jaccard <= 0.5
  pass_when: true
  predicate: 'len(candidate_definitions) >= 2

    and variants_are_member_set_changes      # 禁仅权重缩放(S15-C §4.2.3)

    and rationale_not_based_on_effect_size

    '
  requires_tools: []
  substantive_change: '|supp(W) △ supp(W'')| / |supp(W) ∪ supp(W'')| >= 0.5,排除部分归零中间态'
  version: 1.8+
- decidable: auto
  id: V5DECL
  inputs:
  - manuscript_sections
  items:
  - 伦理批准
  - 知情同意
  - 数据共享
  - 代码共享
  - AI使用披露
  - 利益冲突
  - 作者贡献
  - 资助
  - 注册号
  - 报告规范声明
  name: 声明与披露清单
  on_fail:
    action: V5不通过,不得投稿
    blocking: true
  pass_when: true
  predicate: all_declared(manuscript_sections, 10_items)
  requires_tools:
  - section_scan
  version: 1.8+
- decidable: hybrid
  id: T19
  inputs:
  - 空间坐标矩阵
  - 注释结果
  judge_rubric: 人工判定:①推断单位是 spot 还是细胞——必须全文一致,禁止混用;②去卷积方法是否预注册且参考集来源声明;③是否报告空间自相关(Moran's I 等),未检验而直接做差异=伪重复;④邻域分析是否声明邻域定义(半径/KNN)。缺①或③直接不通过。
  name: 空间转录组(10x Visium/Xenium/CosMx)
  on_fail: R6 硬阻断(伪重复)
  predicate: 空间项目须声明:①推断单位(spot / 细胞);②去卷积方法(cell2location/RCTD);③空间自相关是否检验;④邻域分析方法
  stage_tag: spatial
- decidable: hybrid
  id: T20
  inputs:
  - 稿件
  - 目标期刊 Guide
  judge_rubric: 人工判定:①是否选定适用的报告规范(预测模型→TRIPOD;观察性→STROBE;肿瘤标志物→REMARK);②是否产出逐条对照表;③未覆盖条目是否给出理由;④声明块(伦理/知情同意/数据共享/代码共享/AI使用/利益冲突/作者贡献)是否齐全。缺②直接不通过。
  name: 报告规范对照(TRIPOD/STROBE/REMARK)
  on_fail: 退回补充对照表
  predicate: 投稿前须逐条对照目标报告规范,缺项须声明理由或补充;不得声称'已符合'而无对照表
  stage_tag: reporting
- decidable: auto
  id: WP-1
  name: 标题不越界
  note: 守卫 writing_guard.py;变异用例:加'揭示'→ FAIL
  on_fail: F3
  predicate: 标题不含升级词/无信息词, 字数合规, 含对象+发现
  stage_tag: writing
- decidable: auto
  id: WP-2
  name: 摘要首句为 P1
  note: NC-6;与 WP-6.1 构成'同一真相两处一致'的对称守卫
  on_fail: F3
  predicate: 摘要首句 == P1 且四段齐全, 末句含边界, 含样本量
  stage_tag: writing
- decidable: auto
  id: WP-3
  name: 背景文献可核验
  note: 第三态:返 0 篇判 N/A, 不降级为 AI 生成
  on_fail: F3
  predicate: 引言引用 ∈ research_survey_skill 输出;skill 返 0 篇 → N/A
  stage_tag: writing
- decidable: auto
  id: WP-4
  name: 材料方法可复现
  note: 版本/批号;预注册偏差声明;统计含单双侧+校正;伦理;数据共享;power 依据
  on_fail: F3
  predicate: 每步带版本或批号;预注册偏离已声明;含 单侧/双侧 + BH|Bonferroni|FDR;数据共享登录号
  stage_tag: writing
- decidable: auto
  id: WP-5a
  name: 结果文字有台账锚
  note: v2.15 修:窗口由固定 50 字符改为同句内(跨句曾致误判)
  on_fail: F3
  predicate: 每个'显著'**同句内**有 [L-xxx];不含机制词;阴性显式
  stage_tag: writing
- decidable: auto
  id: WP-5c
  name: 图注必含项
  note: 对齐作图规范 §D
  on_fail: F3
  predicate: 图注含精确 n / 误差棒+中心 / 检验方法;≤250 字
  stage_tag: writing
- decidable: auto
  id: WP-5b
  name: 图表排版
  note: 图号连续/先引用后出现/引用距离 ≤3 段/多面板 a,b,c
  on_fail: F3
  predicate: 图号连续无缺;先引用后出现;图与引用段落 ≤3 段
  stage_tag: writing
- decidable: auto
  id: WP-5d
  name: 图表标题
  note: 无信息词黑名单;图题 ≤15 字;图题 ≠ 图注首句;图题数 == 图号数
  on_fail: F3
  predicate: 图题无 '主要结果/相关分析';≤15 字;≠ 图注首句;数量与图号相等
  stage_tag: writing
- decidable: auto
  id: WP-6
  name: 讨论首段为 P1
  note: NC-3 / NC-2
  on_fail: F3
  predicate: 首段首句 == P1;含局限(P3)与展望(P4);机制词同句内 (Ld)
  stage_tag: writing
- decidable: auto
  id: WP-7
  name: 引用闭合
  on_fail: F3
  predicate: 无'用而未引'且无'引而未用';每条含 DOI/PMID
  stage_tag: writing
- decidable: auto
  id: WP-8
  name: 去 AI 味(初步过滤)
  note: 仅过滤可判定模式特征;不追求'彻底去味'(边界模糊, 人工)
  on_fail: 警告
  predicate: 模板句式 0;空洞表达 0;过渡词 ≤3/千字;段落长度 sd ≥30
  stage_tag: writing
- decidable: auto
  id: WP-9
  name: 三格式导出一致
  note: 守卫 export_guard.py
  on_fail: F3
  predicate: 三版本图号集合一致;占位符数==图表数;图注逐字一致;≥300dpi
  stage_tag: writing
- decidable: auto
  id: T21
  inputs:
  - group_sizes
  name: 组别样本量下限与平衡(三态)
  note: '文献17 Table1 实证:RA 16 vs 4(对照仅4例)、T1D 10 vs 6(临界)。

    在 n=4 对照上做 limma+BH,功效极低却照常报告"显著差异"。

    脚本:literature_gap.check_T21。三态(与 H-2 双阈值同构)。

    '
  on_fail:
    action: fail=禁止据此下结论;warn=须声明小样本并做敏感性分析,不得直接判通过
    blocking: true
  pass_when: verdict=='pass'
  predicate: 'min(group_sizes)>=10 and max(group_sizes)/min<=4  -> pass

    6<=min<10                                          -> warn(灰区)

    min<6  or  max/min>4                               -> fail

    '
  requires_tools: []
  stage_tag: qc
  version: 2.16+
- decidable: auto
  id: T22
  inputs:
  - auc_train
  - auc_ext
  name: 训练-外部验证性能落差
  note: '文献18 实证:训练集 AUC 0.666,外部验证 0.560(落差0.106),

    C-index 0.631(95%CI 0.580-0.652),论文仍称"confirmed predictivity"。

    "有验证集"≠"泛化成立"。脚本:literature_gap.check_T22。

    '
  on_fail:
    action: 强制声明泛化性不足;禁止使用'confirmed predictivity'类措辞
    blocking: true
  pass_when: true
  predicate: '(auc_train-auc_ext)<=0.10 and auc_ext>=0.60 -> pass else fail

    '
  requires_tools: []
  stage_tag: performance
  version: 2.16+
- decidable: auto
  id: T23
  inputs:
  - qc_thresholds
  - rationale
  name: 多数据集QC阈值一致性(须声明)
  note: '文献19:自产 snRNA >2.5% mito;GSE243981 用 5%(snRNA)/25%(scRNA)。

    阈值不同本身合理(模态不同),但必须声明,否则读者无法区分

    差异来自生物学还是 QC。脚本:literature_gap.check_T23。

    '
  on_fail:
    action: 须声明不同阈值的理由(如模态差异);未声明不得直接合并
    blocking: false
  pass_when: true
  predicate: '阈值统一 -> pass

    阈值不同 且 rationale 非空 -> pass(已声明)

    阈值不同 且 rationale 为空 -> fail

    '
  requires_tools: []
  stage_tag: qc
  version: 2.16+
- decidable: auto
  id: T24
  inputs:
  - same_cohort
  - independent
  name: 验证集独立性(同队列split=数据泄露)
  note: '文献19 明确:"To avoid potential data leakage artifacts inherent to

    train-test splits within our specific mouse cohort, we prioritized

    cross-species validation"。脚本:literature_gap.check_T24。

    '
  on_fail:
    action: 同队列 split 须声明泄露风险,或改独立队列/跨物种验证
    blocking: true
  pass_when: true
  predicate: 'independent==true -> pass(独立队列/跨物种)

    same_cohort==true -> fail(验证集与训练集同队列)

    未声明            -> fail

    '
  requires_tools: []
  stage_tag: prediction
  version: 2.16+
- decidable: auto
  id: T25
  inputs:
  - roi_method
  - blinded
  - preregistered
  name: 空间/ROI选择盲法或客观标准
  note: '文献21:"we manually selected by location the hippocampal subfields"

    (手动选区,未盲法)。正例文献19:"region selector was blinded to the

    tissue samples'' genotype origin"。脚本:literature_gap.check_T25。

    '
  on_fail:
    action: ROI 选择须盲法或预注册客观标准;否则判选择偏倚风险
    blocking: false
  pass_when: true
  predicate: 'blinded==true or preregistered==true -> pass else fail

    '
  requires_tools: []
  stage_tag: spatial
  version: 2.16+
- decidable: auto
  graded_note: 'sig_frac 是**连续质量分**,不是二值开关。实测随信噪比单调:

    0.760 / 0.643 / 0.327 / 0.297 / 0.207(流形由强到弱),纯噪声 0.003~0.013。

    50% 门槛的含义是"低于此则降级为探索级",而非"合格线"。

    '
  id: T26
  inputs:
  - X_highdim
  - Y_embedding
  - n_embed
  - n_null
  - celltypes
  invariants:
  - n_embed >= 3 次**独立**嵌入(PCA 确定性,须用子样本拟合产生随机性)
  - n_null >= 1;EES 必须用邻居身份分布(距离值分布对结构不敏感)
  - p 小 = 结构可复现 = 真信号;p 大才是伪影 —— 方向勿反
  name: 嵌入信号-噪声定量分离(EMBEDR)
  on_fail:
    action: 基于该嵌入的聚类/轨迹结论降级为**探索级**;某型不得用于下结论
    blocking: false
  pass_when: sig_frac >= 0.50 and 每个 celltype 的 p 中位数 <= 0.10
  predicate: 'EES_i = KL(P_hi_i || P_lo_i)        # 邻居**身份**分布,非距离值分布

    null  = marginal_resample(X) 嵌入后的 EES*   # 每列独立置换:留边缘、破联合

    p_i   = P(EES*_null <= EES_i),跨 n_embed 次独立嵌入取均值

    sig_frac = fraction(p_i < 0.01)

    '
  requires_tools:
  - marginal_resampling
  - kl_divergence
  - embedding
  stage_tag: embedding
  version: 2.18+
- claim_ceiling:
    从头训练: 不得声称"跨数据集复现"(只是独立建模)
    微调(n_calib>=20): 在该 SOP 下可复用
    零样本迁移: 在目标数据上方向一致
  decidable: auto
  id: T27
  inputs:
  - strategy
  - n_calib
  - gt_source
  - batch_overlap
  - claim
  - ever_tuned_on_target
  invariants:
  - n_calib < 20 时 DAISM-DNN 实测性能提升不显著
  - 现有 T22 只判结果层落差,**不问模型怎么训练**;T27 补方法层
  - '"在目标数据上调过参的两个 CI 当然会重叠" —— 那不是泛化,是泄漏'
  name: 跨数据集泛化的条件匹配声明(DAISM-DNN / EpiTopics)
  on_fail:
    action: R3(声称零样本却调过参)或 R5(措辞越界);缺声明则补齐
    blocking: false
  pass_when: true
  predicate: 'strategy ∈ {零样本迁移, 从头训练, 微调} 且已预注册

    and (strategy==微调 → n_calib >= 20 and gt_source 非空)

    and (strategy==零样本迁移 → ever_tuned_on_target 已声明且为 False)

    and batch_overlap 非空

    and (strategy==从头训练 → claim 不含"跨数据集复现/外部验证成功/独立队列验证")

    '
  requires_tools:
  - batch_diagnostic
  - calibration_record
  stage_tag: replication
  version: 2.18+
- decidable: auto
  id: T28
  inputs:
  - X
  - module_of_gene
  - ko_gene_idx
  - n_subsample
  - claim
  - neg_ctrl_idx
  invariants:
  - 与 T17 分工:T17 判**参数选择**(分位数 vs 倍数),T28 判**流程可复现**
  - 措辞纪律:一律写"计算扰动预测",禁止"功能验证/实验验证/已证实/证明"
  - 只用 WT 数据即可(scTenifoldKnk 关键设计:无需 KO 样本)
  name: 计算扰动的可复现流程(scTenifoldKnk)
  on_fail:
    action: rho < 0.3 判"不稳定,不采用";(e) 不可区分则特异性不成立
    blocking: false
  pass_when: 全部满足
  predicate: '(a) 流程声明:网络构建 / 扰动施加(整行置0) / 差异比较

    (b) 模块归属检验:DR 基因富集于 KO 基因所在模块,p < 0.05

    (c) 重复稳健性:随机子抽样 >=10 次,扰动谱 Spearman rho >= 0.5

    (d) 方向稳定性:KO 与过表达应对称

    (e) 阴性对照:无关基因的扰动谱与目标基因显著不同(rho <= 0.7)

    '
  requires_tools:
  - network_build
  - manifold_alignment
  - hypergeometric
  stage_tag: perturbation
  version: 2.18+
- decidable: hybrid
  id: T29
  inputs:
  - kind
  - sensitive
  - subgroup_n
  - adequacy_claim
  - risk_asymmetry
  - clinical_claim
  - exit_option
  - privacy_absence_reported
  judge_rubric: "判 N/A 的条件(须带理由,空理由 raise):\n  - 纯方法学(无人群数据)\n  - 回顾性公共数据(无法追溯采集过程)\n判定要点:**声称必须有举证支撑**\
    \ —— 只写\"具代表性\"而无数字即判 fail。\n这是\"形式 vs 实质\"的贯彻:V5DECL 只查形式声明是否存在。\n"
  name: 数据正义自查(Braun & Hummel)
  on_fail:
    action: '声称"代表性充分"但无子群样本量 → R5

      未做 (b) 但声称"可临床转化" → **硬阻断**

      未报告 (d) 且属敏感数据 → T29 不通过

      '
    blocking: true
  pass_when: 四项举证齐全
  pillars:
  - (In)visibility
  - (Dis)engagement
  - Non-discrimination
  predicate: '(a) 代表性不足群体 + 各子群体样本量已报告

    (b) 承担不对称风险的群体已评估

    (c) 退出选项已说明

    (d) 敏感数据 → 因隐私不参与的人群及其缺失已报告

    '
  stage_tag: ethics
  version: 2.18+
- decidable: auto
  id: T30
  inputs:
  - data_source
  - data_flow
  - features_visible
  - withdraw
  - secondary_use
  - anon_claim
  - anon_method
  - gdpr_scope
  - gdpr_article
  invariants:
  - PORT 核心:本地处理 + 特征可见性 + true informed consent + 可撤回
  - '"匿名化"无方法名 = 空声明(k-anonymity / 差分隐私 / l-diversity / t-closeness)'
  - 与 T29 分工:T29 问"人是否被公平对待",T30 问"流程是否合规"
  na_when: 使用完全公开二次数据(如 TCGA)→ N/A + 声明来源
  name: 数据治理声明(PORT)
  on_fail:
    action: R5 回退;补声明与法条
    blocking: false
  pass_when: 四项齐全且约束机制具体
  predicate: '(a) 数据流向已说明(是否离开数据主体设备)

    (b) 提取了哪些特征、被采集者能否审阅

    (c) 撤回机制

    (d) 二次使用约束

    and (声称匿名化 → 必须给出具体方法)

    and (GDPR 覆盖 → 必须引用具体法条)

    '
  requires_tools:
  - governance_declaration
  stage_tag: governance
  version: 2.18+
- conflict_note: 建议文档 §0 把 WCMI 标"缺口"、§4 说"属特征工程不吸收" —— 两者自相矛盾。调和:**不吸收 WCMI 的方法,但吸收它暴露的防错点**即"特征选择的位置",落为本条
    T31。
  decidable: auto
  evidence:
  - 肺癌4基因(Wen 2022, BMC Cancer 22:193):全队列 CIBERSORT+ESTIMATE+GO/KEGG+Cox+LASSO 选基因
  - 卵巢癌2基因(Liang 2021, Front Oncol 11:711020):scRNA 定亚群 → bulk WGCNA+单因素Cox+LASSO
  - ROMO1(Wang 2025, Funct Integr Genomics 25:91):whole blood bulk 上 DGE + LASSO
  id: T31
  inputs:
  - selection_place
  - n_outer
  - n_inner
  - tuned_hp_in_fold
  - external_val
  - perf_claim
  - n_selected
  - redundancy_reported
  - max_abs_r
  - frac_r_gt_0_9
  invariants:
  - 生信预后模型**头号 AUC 虚高源**:全数据集选基因 → 再同批样本 CV 评估
  - T09 只管"性能评估不得用样本内概率",**不管特征选择本身的位置** —— 本条补此缺口
  - 不得默认 nested:默认放行 = 空规(第11条陷阱形态①)
  - 外部验证集可用 ≠ CV 估计无偏(与 T22 落差判据互补)
  - ★ v2.20 扩展(WCMI 启示):选择位置正确 ≠ 特征集可用 —— 高冗余(|r|>0.9)
    特征集同样虚高性能且无法解释,是本判据的另一半
  na_when: 非建模类研究(纯差异表达 / 纯方法学)→ N/A + 理由
  name: 特征选择与超参必须在评估折叠内(含冗余度)
  on_fail:
    action: 判"含选择泄露,性能估计乐观";若同时声称高性能 → 追加 R5
    blocking: false
  pass_when: selection_place ∈ {nested(带折数), 独立选择集} 且 (e) 满足
  predicate: '(a) 必须声明特征选择的执行位置;未声明 → 判"判不了"(不得默认 nested)

    (b) nested / 独立选择集 → 通过

    (c) 全数据集一次性选择(outer) → 不通过

    (d) 声称 nested 但给不出外层/内层折数 → 视为未声明(判不了)

    (e) 超参调优同样须在训练折内

    (f) 全数据集选择 + 声称高性能 → 追加 R5 措辞越界

    (g) 有外部验证集时:该集性能可用,但训练-CV 性能仍须标"乐观"

    (h) 冗余度(触发:给出选中特征数):必须报告特征间相关矩阵;
        未报告 → 判"不完整"

    (i) |r| > 0.9 的特征对占比 > 30% → 冗余过高,须去冗余
        (mRMR / 条件互信息 / VIF 剔除)后重跑;10%~30% 为灰区

    '
  requires_tools:
  - nested_cv_declaration
  stage_tag: modeling
  version: 2.20+
- decidable: auto
  evidence:
  - ROMO1(Wang 2025, Funct Integr Genomics 25:91):two-sample MR,原文未报告 F 统计量 / 多效性检验 / 祖源匹配 → T32 判不通过
  id: T32
  inputs:
  - is_mr
  - f_stat
  - iv_count
  - independence_declared
  - pleiotropy_test
  - pleiotropy_present
  - heterogeneity_reported
  - two_sample
  - ancestry_matched
  - exposure_gwas_is_discovery
  - causal_claim
  invariants:
  - ★ 建议文档 §0 写"有 MR 但无判据" —— 实测 grep:MR / 孟德尔 / mendel **均 0 次**,框架此前**根本没有 MR**(不是"有而缺判据")。建议此处事实有误,已记录。
  - MR 三假设缺一即因果推断不成立;F<10 的弱工具变量向混杂偏倚收缩
  - two-sample MR 祖源不匹配 = 人群结构混杂,IV-暴露关联不可迁移
  na_when: 非 MR 研究 → N/A + 理由
  name: 孟德尔随机化工具变量假设
  on_fail:
    action: 缺 (a)/(c) 判"判不了";(f) 不匹配 → 硬阻断;(h) 越界 → R5
    blocking: true
  pass_when: (a)(b)(c)(d)(e)(h) 全部满足;two-sample 时另需 (f)
  predicate: '(a) 关联性:IV 与暴露强相关;F < 10 → 弱工具变量,判不确定

    (b) 独立性:IV 与混杂无关(人群分层须主成分校正)—— 须声明

    (c) 排他性:水平多效性须检验(MR-Egger 截距 / MR-PRESSO / 加权中位数)

    (d) 多效性存在时须用稳健方法(median / mode / PRESSO 校正后)

    (e) 异质性(Cochran Q / I²)须报告

    (f) two-sample MR:两样本祖源必须匹配 → 不匹配**硬阻断**

    (g) IV-暴露关联若来自同一 GWAS 发现集 → Winner''s curse 须声明

    (h) 措辞:MR 只能"提示/支持因果",禁"证明/证实因果"

    '
  requires_tools:
  - mr_iv_diagnostics
  stage_tag: causal
  version: 2.19+
- decidable: auto
  id: T33
  inputs:
  - prop
  - n_total
  - k
  - ci_reported
  - relative_ci_width
  - claim
  - observation_window
  - target_prob
  - n_obs
  - tail_method
  invariants:
  - T16 只管每 (donor,celltype) **细胞数**下限,**不管比例估计的 CI** —— 本条补此对称面
  - 可解析概率下界 = 1/n。短记录**系统性低估尾部**(Dowling:1年→6年估计需求持续上升)
  - ★实测 Wilson CI:5/10000 → CI [2.14e-4, 1.17e-3],相对宽度 1.913 > 1.0,上下界差 5.48 倍 → 硬阻断
  - ★实测可解析性:1年(n=365)下界 2.74e-3 > 目标 1.37e-4 → 无法解析;39年(n=14235)下界 7.02e-5 → 可解析
  na_when: 非稀有事件研究 → N/A + 理由;用 < 100 样本估 <1% 频率 → 判不了
  name: 稀有/极端事件概率估计的样本量下限
  on_fail:
    action: 硬阻断(结论不可辨)或判不了
    blocking: true
  pass_when: CI 已报且相对宽度 <= 1.0;声称变化时上下界比值 <= 2;极端事件须可解析或已声明外推
  predicate: "(a) 必须报告比例的 95% CI(Wilson / Clopper-Pearson / bootstrap)\n\n(b) CI 相对宽度\
    \ > 100%(CI_width / point > 1.0)→ 判\"不足以估计\"\n\n(c) 必须声明观察/记录窗口长度,并说明是否足以捕获极端事件\n\
    \n(d) 估极端事件概率(<1%)时经验分位数会系统性低估\n    → 须改用参数/半参数尾部外推(GPD / POT / EVT)\n\n硬阻断:声称比例变化但\
    \ CI 上下界比值 > 2 → 两个方向都能容纳相反结论\n"
  requires_tools:
  - wilson_ci
  - tail_extrapolation
  stage_tag: rare_event
  trigger: 比例 < 5% / 变异频率 < 1% / 极端事件概率 < 1%(三选一)
  version: 2.20+
- decidable: auto
  id: T34
  inputs:
  - per_sample_uncertainty
  - abs_error
  - uncertainty_error_corr
  - stratified_perf_reported
  - high_uncert_subgroup
  - claim
  invariants:
  - T12 只要求**模型层** CI,T13b 只要求**群体层**校准斜率 CI,**没有一条要求 per-sample 不确定性**
  - Mannodi 材料 ML Figure 6A/B:不确定性 vs 绝对误差散点存在**高误差高不确定性尾部**
  - 只报平均 RMSE 是平均性能,不告诉读者**哪些样本预测不可信** —— 高风险个体误判后果更严重
  - ★实测 corr:信息性 0.892 vs 无信息 -0.031(把 corr<0.1 则无信息 从断言变为证据)
  na_when: 非预测建模研究 → N/A + 理由
  name: 预测不确定性的分层报告
  on_fail:
    action: 判不完整;声称个体风险时为硬阻断
    blocking: false
  pass_when: (a)(b) 齐全;(c) 触发时已分层;(d) 声称个体风险时有 per-sample CI 且 corr >= 0.1
  predicate: "(a) 必须报告 per-sample 预测不确定性\n    (GPR 后验方差 / bootstrap 预测分布 / MC dropout)\n\
    \n(b) 必须报告不确定性 vs 绝对误差关系(散点 + 相关系数)\n\n(c) corr > 0.3 → 必须报告分层性能:\n    低/高不确定组的\
    \ AUC、校准、Brier 分别列\n\n(d) 声称可用于个体风险预测 → 必须报告 per-sample CI,\n    禁止只报群体平均 AUC\n\
    \n不通过:只报单一平均性能 → 判不完整;\n        高风险亚群不确定性更高但未声明 → R5;\n        声称精确预测但 corr <\
    \ 0.1 → 不确定性无信息\n"
  requires_tools:
  - uncertainty_estimation
  - correlation
  stage_tag: prediction
  version: 2.20+
- decidable: auto
  id: T35
  inputs:
  - tools
  - tool_versions
  - ldsc_intercept
  - ldsc_intercept_corrected
  - mtag_vs_single_gwas_declared
  - mtag_genetic_corr_claim
  - coloc_posteriors
  - coloc_prior_declared
  - independent_validation
  invariants:
  - T32 管 MR,**不管 LDSC / MTAG / colocalization** —— 本条补此对称面
  - LDSC:样本量 <10万时遗传相关性估计不稳;群体分层校正不充分会**系统高估**
  - MTAG:**人为夸大遗传相关性**(多表型联合估计重复计算重叠样本信息)
  - ★循环论证:用 MTAG 输出声称遗传相关性高 —— 该相关性是 MTAG 联合估计**定义**出来的
  - ROMO1(Wang 2025)实测中招:用 LDSC + MTAG 但未报 intercept / 未声明 MTAG 不可比
  na_when: 未使用多组学整合工具 → N/A + 理由;coloc H4>0.8 但先验未声明 → 判不了
  name: 多组学整合工具的已知偏倚(LDSC/MTAG/coloc)
  on_fail:
    action: R5 回退;循环论证为硬阻断
    blocking: false
  pass_when: (a) 已声明;LDSC intercept 接近 1 或已校正;MTAG 已声明不可比;coloc 先验已声明;(e) 有独立验证
  predicate: "(a) 每个工具必须声明版本与关键参数\n\n(b) LDSC:必须报告 intercept;偏离 1 说明混杂校正不足(>0.1 须校正)\n\
    \n(c) MTAG:必须声明其结果与单个 GWAS 不可直接比较;\n    用其输出声称遗传相关性 = 循环论证\n\n(d) colocalization:必须报告\
    \ H1/H2/H3/H4 后验与先验设置\n\n(e) 任何工具结果须有独立数据验证,不能只凭工具输出断言因果\n"
  requires_tools:
  - ldsc
  - mtag
  - coloc
  stage_tag: multiomics
  trigger: 使用 LDSC / MTAG / colocalization / SuSiE
  version: 2.20+
- decidable: auto
  id: T36
  name: 多组件整合的增量消融
  note: |
    ★ 与 T31 分工,禁止合并:T31 静态判"特征集内部冗余"(看相关系数);
    T36 动态判"组件之间增益"(看 ΔAUC/ΔC-index)。
    锚定文献:ROMO1(Wang 2025)/p53(Wang)/Lage-Rupprecht —— 三篇均
    声称"整合有用"但**未做消融**。
  on_fail: 硬阻断(与 T13 同规)
  na_when: 单组件研究(未做多组学/多算法/多特征集整合)
  predicate: |
    reported(perf_full) and reported(perf_single, every component)
    and reported(perf_loo, every component)
    and for each c: delta_c = perf_full - perf_loo[c]
    delta_c >= 0.01 and CI_c 不跨 0 → 该组件有贡献
    任一组件无贡献仍声称"整合带来提升" → R5 回退
    全部组件无贡献 → 硬阻断(循环论证)
  stage_tag: integration
- decidable: auto
  id: V4MODAL
  inputs:
  - in_silico
  - independent_direction
  - in_vitro
  - in_vivo
  - clinical
  - claim
  invariants:
  - V4 原只做主张-证据形式匹配,**无跨模态强度阶梯** —— 本条补上(Lage-Rupprecht 药物重定位)
  - 铁律:**不得跨级主张**。in silico + in vitro 不等于治疗有效,只能说细胞模型中有活性
  - ★词表必须**互斥**(每词只归属一个等级):初版 L1/L2 上限均含候选,导致 L1 达成却声称候选被误判跨级到 L2 —— v2.20 修正
  na_when: 非跨模态验证研究 → N/A + 理由;未报告任何模态证据 → 判不了
  name: 跨模态验证强度分级(不得跨级主张)
  on_fail:
    action: R5 措辞越界
    blocking: false
  pass_when: 声称等级 <= 达成等级
  predicate: "阶梯:L1 in silico → L2 独立数据方向一致 → L3a in vitro\n      → L3b in vivo →\
    \ L4 clinical\n\n判定:声称所需等级 > 实际达成等级 → 跨级主张,不通过\n\nL1 允许候选/预测;L2 允许方向一致;L3a 允许细胞模型中;\n\
    L3b 允许动物模型中;L4 才允许临床效用/治疗有效\n"
  requires_tools: []
  stage_tag: validation
  version: 2.20+
- decidable: auto
  id: T37
  inputs:
  - criteria_dist
  - criteria_total
  name: 判据体系的骨架/休眠比例(L2 消融的判定层)
  note: |
    ★ v2.23 补最后一层:v2.22 产出了 criteria_dist(体温表),但**没有
      一条判据来读这张表并下诊断** —— 骨架/休眠比例可以自我声明,
      无独立检查。这是第 11 条陷阱在**判据层**的残留形态:不是
      "守卫从未 FAIL",而是"判据层结构健康度无独立检查"。
    ★ 判定项来源:T37 的判定项由 ablation.py 产生(不在 verify_all 的
      ledger 里),因为它的输入 criteria_dist 恰由 verify_all 生成 ——
      若在 verify_all 内实现,统计自己的分布会改变分布(自指)。
      故由下游 ablation.py 判定,与"附录由 gen_appendix 生成"同构。
  on_fail:
    action: 判"判据膨胀",新增判据须先论证必要性
    blocking: true
  na_when: 无(每次主验证运行均须判定,不接受 N/A)
  pass_when: 休眠率 == 0 且 GLOBAL 占比 < 10% 且无垄断判据
  predicate: |
    休眠率 = 休眠判据数 / 判据总数
    休眠判据 := n_direct == 0 且 n_branch == 0(从未被任何判定项归因)

    三态(与 H-2 双阈值同构,不是二值):
      休眠率 == 0        → pass
      0 < 休眠率 < 20%   → warn(须逐条声明休眠原因)
      休眠率 >= 20%      → fail(判"判据膨胀")
    附加:
      GLOBAL 占比 >= 10%            → warn(归因稀释)
      单条判据 n_direct/总判定项 > 40% → warn(垄断)
  invariants:
  - 判据层的健康度必须由独立判据判定,不得自我声明
  - 三态判定 pass/warn/fail,禁止压缩为二值(与 H-2 双阈值同构)
  - ★ 自指口径:T37 自身**排除**在休眠统计外(分子分母均排除)。
      与既有自指处理同规:report_guard 不计入自身合法集、链文件不登记
      自身 sha256、MANIFEST 不写自身哈希。若把自己算进去,等于
      "T37 用自己给自己打的分数判定自己" —— 正是框架要防的自证。
      "消融是否在流程里"由 build STAGES 的 rc 保证,不由 T37 兜底。
  requires_tools:
  - ablation
  stage_tag: meta
  version: 2.23+

invariants:
- 硬阻断9条
- R1-R6 定义
- 措辞硬规则
- 禁止 LLM 生成数字:所有数字必须来自脚本输出
- 禁止软化判定:不得把'不通过'说成'部分通过'
- ack=null 不算交接完成
tools:
  pdf_forensics:
    backends:
      poppler:
        authoritative: true
        commands:
        - pdffonts
        - pdftotext
        - pdfimages
        - pdfinfo
      pypdf_recursive:
        authoritative: false
        known_blind_spots:
        - 'encrypted: pypdf 抛 PdfReadError —— 严禁 except 吞掉后判合规(假通过)'
        - 'xref_stream: 跨引用表流解析偶发失败,poppler 更稳健'
        - 'cidfont_no_tounicode: extract_text() 返回空 → 误判''无文本层''(假不通过)'
        - 'xfa: XFA 表单内图像/字体,pypdf 递归不覆盖'
        module: pypdf
        must_annotate: 'detection_backend: pypdf'
        on_blind_spot: 判'判不了'(blocking),不得降级为通过 —— 与 on_unavailable 同规
    on_unavailable: fail_with_reason
    output_schema:
      blocking_level: int
      detection_backend: str
      evidence:
      - str
      expected: any
      observed: any
      passed: bool
    priority:
    - poppler
    - pypdf_recursive
    - fail
    require_raw_stdout: true
version: 1.8+
````

## G. 三个守卫的原始 stdout(单次捕获)

### G.1 一致性守卫 `consistency_guard.py`

```
==========================================================================
  文档—脚本一致性守卫 v1.0
==========================================================================

  ── G-1 审核包内嵌脚本必须与 scripts/ 实际脚本一致
    [PASS] G-1.0 审核包含 2 个 python 块                  obs=2                          exp=>=2
    [PASS] G-1.1 verify_all.py 内嵌=实际(规范化 sha256)    obs=10742a81857d               exp=10742a81857d
    [PASS] G-1.2 layout_check.py 内嵌=实际(规范化 sha256)  obs=ac52109fdd08               exp=ac52109fdd08

  ── G-2 文档声称的通过数必须等于 report.json
    [PASS] G-2.0 report.json 含 summary              obs=True                       exp=True
    [PASS] G-2.1 文档存在 '222/222 通过' 声称               obs=True                       exp=True
    [PASS] G-2.2 文档无过期的 N/N 声称                      obs=全部一致                       exp=仅限 ['222/222', '70/70']

  ── G-3 文档关键实测数字必须能在 report.json 中定位
    [PASS] G-3 ΔAUC=0.0397 可在文档中定位(边界匹配)          obs=True                       exp=True
    [PASS] G-3 细胞级p=4.2e-14 可在文档中定位(边界匹配)         obs=True                       exp=True
    [PASS] G-3 患者级p=0.28 可在文档中定位(边界匹配)            obs=True                       exp=True
    [PASS] G-3 D1 OR=1.627 可在文档中定位(边界匹配)          obs=True                       exp=True
    [PASS] G-3 D3 OR=0.737 可在文档中定位(边界匹配)          obs=True                       exp=True
    [PASS] G-3 I2=96.6 可在文档中定位(边界匹配)              obs=True                       exp=True
    [PASS] G-3.uniq.MoCA MoCA 队列 OR 在同一文档内取值唯一              obs=['3.085']                  exp=唯一
    [PASS] G-3.uniq.MMSE MMSE 队列 OR 在同一文档内取值唯一              obs=['1.238']                  exp=唯一

  ── G-4 排版报告一致性
    [PASS] G-4.1 layout_report 判定项数                 obs=70                         exp=>0
    [PASS] G-4.2 排版声称 N/N 与 JSON 一致(上下文匹配)          obs=True                       exp=排版语境含 70/70

  ── G-5 criteria 关键值必须出现在框架正文
    [PASS] G-5.T13b.model_family_caveat H-1 限定:criteria 含 model_family_caveat obs=True                       exp=True
    [PASS] G-5.T13b.model_family_caveatb H-1 限定:框架正文同步                      obs=True                       exp=含 '未正则化'
    [PASS] G-5.T13c.blocking_threshold H-2 阻断阈值:criteria 含 blocking_threshold obs=True                       exp=True
    [PASS] G-5.T13c.blocking_thresholdb H-2 阻断阈值:框架正文同步                    obs=True                       exp=含 'neg_run ≥ 12'
    [PASS] G-5.T13c.warning_threshold H-2 警告阈值:criteria 含 warning_threshold obs=True                       exp=True
    [PASS] G-5.T13c.warning_thresholdb H-2 警告阈值:框架正文同步                    obs=True                       exp=含 'neg_run < 8'
    [PASS] G-5.T13c.grey_zone H-2 灰区:criteria 含 grey_zone        obs=True                       exp=True
    [PASS] G-5.T13c.grey_zoneb H-2 灰区:框架正文同步                      obs=True                       exp=含 '灰区'
    [PASS] G-5.DEPR.阈值 10 H-2 复核项仍问 10 点:无未标注残留              obs=0                          exp=0
    [PASS] G-5.DEPR.落差 >  H-2 落差阈值:无未标注残留                    obs=0                          exp=0
    [PASS] G-5.DEPR.区间内 0 H-2 旧区间冲突数字:无未标注残留                 obs=0                          exp=0
    [PASS] G-5.DEPR.10 点 旧阈值数值 10 在新语境:无未标注残留               obs=0                          exp=0
    [PASS] G-5.DEPR.≥10 点 旧阈值 ≥10 在新语境:无未标注残留                obs=0                          exp=0
    [PASS] G-5.T13c.pred T13c predicate 未硬编码 10 点单阈值        obs=True                       exp=无 'neg_run < 10'
    [PASS] G-5.T13c.ref predicate 引用 warning/blocking      obs=True                       exp=True
    [PASS] G-5.pypdf.n pypdf 盲区已登记                        obs=4                          exp=>=4
    [PASS] G-5.pypdf.加密 盲区 '加密' 同步至正文                      obs=True                       exp=True
    [PASS] G-5.pypdf.xref 盲区 'xref' 同步至正文                    obs=True                       exp=True
    [PASS] G-5.pypdf.ToUnicode 盲区 'ToUnicode' 同步至正文               obs=True                       exp=True
    [PASS] G-5.pypdf.XFA 盲区 'XFA' 同步至正文                     obs=True                       exp=True

  -- G-6 变更清单声称的改动必须在正文可核验
    [PASS] G-6.0 MANIFEST 含 v226_fixes              obs=78                         exp=>0
    [PASS] G-6.缩进 变更清单'判据 2 与判据 1 同级'已落地             obs=True                       exp=期望出现=True
    [PASS] G-6.灰区 变更清单'表格 8 点标为灰区'已落地                obs=True                       exp=期望出现=True
    [PASS] G-6.≥12 变更清单'F3 阈值改为 12'已落地                obs=True                       exp=期望出现=True
    [PASS] G-6.R-8 变更清单'R-8 已实现'已落地                   obs=True                       exp=期望出现=True

  -- G-7 图文件名 analysis_step 必须属于框架阶段标签
    [PASS] G-7.0 criteria.yaml 含 stage_tag          obs=25                         exp=>0
    [PASS] G-7.1 图目录含图件                             obs=8                          exp=>0
    [PASS] G-7.2 全部 8 个图文件名合规                       obs=全部合规                       exp=无违规
    [PASS] G-7.3 图号 01..2 连续无缺号                     obs=[1, 2]                     exp=无缺号

  -- G-8 Markdown 表格列数一致性
    [PASS] G-8.1 共扫描 46 个表格                         obs=46                         exp=>0
    [PASS] G-8.2 全部表格列数与表头一致                        obs=全部一致                       exp=无错位

  -- G-10 声称写入的内容必须可检出
    [PASS] G-10.1 版本化文档与源文档内容一致                      obs=True                       exp=True
    [PASS] G-10. · 合集含「附五 · v2.1」                     obs=True                       exp=True
    [PASS] G-10. · 合集含「附六 · v2.2」                     obs=True                       exp=True
    [PASS] G-10.g.G-5DEPR 守卫 G-5.DEPR 已实现                    obs=True                       exp=True
    [PASS] G-10.g.G-73 守卫 G-7.3 已实现                       obs=True                       exp=True
    [PASS] G-10.g.G-82 守卫 G-8.2 已实现                       obs=True                       exp=True
    [PASS] G-10.mut guard_selftest.py 存在(变异测试)         obs=True                       exp=True

  -- G-13 守卫不得硬编码主交付文档路径(lint 多模式)
    [PASS] G-13.scope G-13 扫描对象为守卫脚本(不误伤生成器/测试器)         obs=['conclusion_guard.py', 'c exp=含 consistency/report
    [PASS] G-13.cons.join444 consistency_guard.py:445 join 硬编码主文档须有统一解析器回退 obs=True                       exp=期望=True
    [PASS] G-13.cons.join500 consistency_guard.py:501 join 硬编码主文档须有统一解析器回退 obs=True                       exp=期望=True
    [PASS] G-13.pat G-13 三种模式在合成样本上均命中(防空规)            obs=['join', 'concat', 'pathli exp=join/concat/pathlib
    [PASS] G-13.clean 真实守卫源码无未受保护的硬编码主文档路径(2 处候选)        obs=0                          exp=0
    [PASS] G-13.0 G-13 可执行                           obs=完成                         exp=无异常

  -- G-14 crossval/ 下不得残留主交付文档副本
    [PASS] G-14.1 crossval/ 递归无主交付文档副本               obs=无                          exp=无
    [PASS] G-14.cov G-14 递归扫描生效(扫描目录数)                 obs=17                         exp=>1
    [PASS] G-14.0 G-14 可执行                           obs=完成                         exp=无异常
    [PASS] G-11.1 附录 §F 判据条数 == criteria.yaml 实际     obs=51                         exp=51
    [PASS] G-11.2 框架正文判据条数 == criteria.yaml 实际       obs=51                         exp=51
    [PASS] G-11.3 合集判据条数 == criteria.yaml 实际(全形态 2 处) obs=全部一致                       exp=全部一致
    [PASS] G-11.3-cov G-11.3 至少命中 1 处声明(防锚点失效)           obs=2                          exp=>=1
    [PASS] G-12.框架.ver 框架 banner 版本号 == MANIFEST          obs=2.26                       exp=2.26
    [PASS] G-12.框架.ih 框架 登记 input_hash == report.json(稳定锚) obs=d8ac2a843faa33e1           exp=d8ac2a843faa33e1
    [PASS] G-12.框架.rid 框架 banner run_id == build_version.json(构建期锚) obs=0b7e75ffc4a5dbdc           exp=0b7e75ffc4a5dbdc
    [PASS] G-12.框架.rid-all 框架 全部 run_id 声明互相一致(1 处)           obs=全部一致                       exp=全部一致
    [PASS] G-12.框架.rid-cov 框架 run_id 声明至少命中 1 处(防锚点失效)        obs=1                          exp=>=1
    [PASS] G-12.合集.ver 合集 banner 版本号 == MANIFEST          obs=2.26                       exp=2.26
    [PASS] G-12.合集.ih 合集 登记 input_hash == report.json(稳定锚) obs=d8ac2a843faa33e1           exp=d8ac2a843faa33e1
    [PASS] G-12.合集.rid 合集 banner run_id == build_version.json(构建期锚) obs=0b7e75ffc4a5dbdc           exp=0b7e75ffc4a5dbdc
    [PASS] G-12.合集.rid-all 合集 全部 run_id 声明互相一致(2 处)           obs=全部一致                       exp=全部一致
    [PASS] G-12.合集.rid-cov 合集 run_id 声明至少命中 1 处(防锚点失效)        obs=2                          exp=>=1

==========================================================================
  一致性守卫: 77/77 通过, 失败 0 项, 不适用 0 项
==========================================================================
```

### G.2 审核报告守卫 `report_guard.py`

```
==============================================================================
  审核报告一致性守卫 v1.0
==============================================================================
  标尺:框架第 9 条原则 —— 数字唯一来源为台账。审核报告同样适用。

  -- R-1 主验证通过数必须来自 report.json
    [PASS] R-1 主验证 N/N == 222/222 且分支 == 21         obs=全部一致                       exp=222/222, 21 分支

  -- R-2 排版通过数必须来自 layout_report.json
    [PASS] R-2 报告含排版 70/70                          obs=70/70                      exp=70/70

  -- R-3 run_id 可追溯
    [PASS] R-3 引用了 run_id                           obs=2 个                        exp=>=1 个
    [PASS] R-3b 注明 run_id 来源为 report.json            obs=True                       exp=True

  -- R-4 报告中出现的实测数值必须能回溯到台账/判据/作图规范
    [PASS] R-4 扫描 11 个实测值                           obs=全部可回溯                      exp=无孤立数字

  -- R-8 报告中的守卫 N/N 必须来自 guard_results.json
      [说明] R-8 自指兼容: 合法集同时接受 (n-1)/n 与 n/n 双值 ——
            守卫运行至自身时终态尚未算出,此为设计而非过期残留。
    [PASS] R-8 扫描 6 个 N/N 声称                        obs=全部可回溯                      exp=合法集 ['10/10', '10/11', '11/11', '222/222', '5/5', '7/7', '70/70', '76/77', '77/77', '9/10']

  -- R-5 handoff 链数字必须来自 handoff_chain.json
    [PASS] R-5 节点数 14(上下文匹配)                        obs=14                         exp=节点语境中应出现
    [PASS] R-5b 在途数 9(上下文匹配)                         obs=9                          exp=在途语境中应出现

  -- R-6 判据 YAML 条数
    [PASS] R-6 YAML 判据 51 条(全形态 2 处均须一致)            obs=全部一致                       exp=全部一致

  -- R-7 审核包附录脚本一致性(委托 consistency_guard)
    [PASS] R-7 审核包附录脚本 == 实际脚本                      obs=0                          exp=0

==============================================================================
  审核报告守卫: 10/10 通过, 失败 0 项
==============================================================================
```

### G.3 Handoff 链守卫 `handoff_guard.py`

```
==============================================================================
  Handoff 链守卫 v1.0
==============================================================================

  节点数: 14   链: PSCI-FRAMEWORK-2026

  ── L-1 必填字段(12 项)
    [PASS] L-1 全部节点 12 字段齐全                             obs=无缺失                      exp=无缺失

  ── L-2 next 指向存在或链尾有终止理由
    [PASS] L-2 next 全部可达 / 链尾有理由                        obs=无断链                      exp=无断链

  ── L-3 state_in 前驱存在
    [PASS] L-3 前驱全部存在                                   obs=无悬空引用                    exp=无悬空引用

  ── L-4 无环
    [PASS] L-4 链为 DAG / 线性                              obs=False                    exp=False

  ── L-5 ack 状态
    [PASS] L-5 ack 结构合法                                 obs=全部合法                     exp=null 或 {by,time,note}
      已确认 5/14, 在途 9
      在途: ['H03_A2C_S03框架核定', 'H05_A2C_S07判据定稿', 'H06_A2C_S13回执', 'H07_A2C_S15判据闭合', 'H09_A2USER_S16恢复包', 'H10_A2USER_交叉验证包', 'H11_A2USER_审核包', 'H13_A2USER_S20复审响应', 'H14_A2USER_S21缺口补齐']
    [PASS] L-5b 在途节点已标记(可追溯)                             obs=9                        exp=>=0(记录)

  ── L-6 交付物校验和
    [PASS] L-6 已登记校验和一致                                 obs=全部一致                     exp=一致
    [PASS] L-6c 无自指(链文件不登记自身校验和)                         obs=无                        exp=无 —— 写回会改变自身sha256
    [PASS] L-6b 已登记文件存在                                  obs=全部存在                     exp=存在

  ── L-7 无孤儿节点
    [PASS] L-7 无孤儿节点                                    obs=无                        exp=无

  ── L-8 时间单调
    [PASS] L-8 时间不倒流                                    obs=无                        exp=无

==============================================================================
  Handoff 链守卫: 11/11 通过, 失败 0 项
  链状态: 完整 ｜ 在途交接 9/14
==============================================================================

  [注意] 链结构完整,但有交接未获接收确认 —— 链处于 PENDING 状态。
         ack=null 不算交接完成,这是本链当前最主要的证据缺口。
```

### G.4 守卫结果派生说明

**中间产物 `results/guard_results.json`(由本脚本从上述 stdout 解析并落盘):**

```json
{
  "G.1": "77/77",
  "G.2": "10/10",
  "G.3": "11/11"
}
```

**守卫源码(可被审核方独立审阅,确认派生逻辑无硬编码):**

<details><summary>consistency_guard.py</summary>

```python
# -*- coding: utf-8 -*-
"""
================================================================================
 文档—脚本一致性守卫  v1.0
================================================================================
 起因:复审发现「审核包附录 A 的脚本是旧版,而 crossval/scripts/ 已更新」——
      文档与代码脱节,任何按文档运行的人得到的是旧结果。这正是框架自己
      要防的"纸面闭环"。本守卫把这条变成机器可检。

 检查项:
   G-1 审核包内嵌脚本与 scripts/ 下实际脚本逐字节一致(sha256)
   G-2 审核包声称的 "N/N 通过" 与 results/report.json 的 n_pass/total 一致
   G-3 审核包声称的关键实测数字能在 report.json 中找到
   G-4 排版脚本同理(layout_report.json)

 运行: python scripts/consistency_guard.py --doc ../框架_独立审核包_v1.md
 退出码: 0 = 一致, 1 = 不一致(审核不通过)
================================================================================
"""
import os, re, sys, json, hashlib, argparse
# ── 控制台编码加固（v2.25）──────────────────────────────────────────────────
# 起因：中文 Windows 默认控制台 cp936(GBK) 无法编码本文件输出的部分符号。
#   本守卫的完整 stdout 是**强制返回项**（跨平台指导 §5 第 5b 条），不能崩。
try:
    if sys.stdout.isatty():
        sys.stdout.reconfigure(errors="replace")
    else:
        sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(errors="replace")
except (AttributeError, ValueError):
    pass
import yaml

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(HERE)
WS = os.path.dirname(ROOT)   # /data/workspace

# ══════════════════════════════════════════════════════════════════════════════
# 主交付文档定位（★ v2.25 P0 修复）
# ──────────────────────────────────────────────────────────────────────────────
# 起因（v2.24 交付包实测）：本文件 + report_guard + guard_selftest 的**回退路径**
#   全部硬编码为"未版本化旧名"（框架_独立审核包_v1.md / 四角度审核报告_合集.md /
#   附录_原始输出.md）。开发机同时存在源文档与版本化副本 → 全绿；
#   而交付 zip 内**只有版本化副本**（实测 101 条目，三个旧名一个都不存在）→
#     ① G-10/G-11 整块 FileNotFoundError 被 except 吞掉（判定项 80→69，静默消失）
#     ② report_guard R-7 FAIL   ③ guard_selftest 直接崩溃（0/66）
#   讽刺点：本文件早先的 docstring 已写"旧名，zip 内不存在"，却只修了 argparse
#   的 default，没修这 6 处回退 —— 正是框架自述的"改了 A 忘改 B"。
# 修法：统一走本解析器 —— 显式参数 > 版本号最大的版本化副本 > 未版本化源文档
#   （仅开发机有）> SystemExit。**禁止降级成"跳过检查"**（§3.3 静默失败禁令）。
# ══════════════════════════════════════════════════════════════════════════════
_SRC_NAME = {
    "框架_独立审核包": "框架_独立审核包_v1.md",
    "四角度审核报告_合集": "四角度审核报告_合集.md",
    "附录_原始输出": "附录_原始输出.md",
}


def _verkey(p):
    """版本化文件名 → 可比较的数值键。
    注意：字符串序会把 v2.9 排到 v2.24 之后（v2.10 真 bug），必须数值排序。"""
    m = re.search(r"_v([\d.]+)_(\d{8})\.md$", os.path.basename(p))
    if not m:
        return (0,)
    return tuple(int(x) for x in m.group(1).split(".")) + (int(m.group(2)),)


def latest_versioned(prefix):
    """只取**版本化副本**（不含源文档）；找不到返回 None。
    G-10.1 需要"版本化副本 vs 源文档"两个不同对象，故不能走 main_doc。"""
    import glob as _g
    hits = [p for p in _g.glob(os.path.join(WS, prefix + "_v*.md"))
            if re.search(r"_v[\d.]+_\d{8}\.md$", os.path.basename(p))]
    return max(hits, key=_verkey) if hits else None


def main_doc(prefix, explicit=None, required=True):
    """定位主交付文档（dev 布局与交付布局通用）。"""
    if explicit and os.path.isfile(explicit):
        return explicit
    v = latest_versioned(prefix)
    if v:
        return v
    src = os.path.join(WS, _SRC_NAME.get(prefix, prefix + ".md"))
    if os.path.isfile(src):
        return src
    if not required:
        return None
    raise SystemExit(
        "[consistency_guard] 找不到主交付文档「%s」：\n"
        "  · 交付包应含版本化副本 %s_v<版本>_<日期>.md\n"
        "  · 开发机应含源文档     %s\n"
        "  两者皆无 = 交付不完整。此处**不得**降级为「跳过检查」——\n"
        "  那正是本守卫存在的理由（静默失败）。"
        % (prefix, prefix, _SRC_NAME.get(prefix, prefix + ".md")))


def _latest_doc(prefix):
    """argparse 默认值：最新版本化副本；找不到返回 None（由 main 显式报错）。"""
    return main_doc(prefix, required=False)



def sha(p):
    return hashlib.sha256(open(p, "rb").read()).hexdigest()[:16]


def _latest_ver(files):
    """按**版本号自然序**取最新,而非字符串序。

    ⚠ v2.10 真 bug:字符串序下 "v2.10" < "v2.9"(第 4 字符 '1' < '9'),
      于是 sorted(...)[-1] 选中 v2.9 副本 → G-12 拿旧 banner 与新版本比对
      → 恒 FAIL。该 bug 在版本号跨 9→10 时才暴露,此前 v2.1~v2.9 全部"正确",
      属于**潜伏到边界才失效**的那类。
    """
    def _key(f):
        m = re.search(r"_v([\d.]+)_\d{8}\.md$", f)
        if not m:
            return (0,)
        return tuple(int(x) for x in m.group(1).split("."))
    return sorted(files, key=_key)[-1]

def main():
    ap = argparse.ArgumentParser()
    # ⚠ v2.11:原默认指向 ROOT(=crossval)下的**过期副本**(v1.4/148KB),
    #   而 build.py 传的是 WS 主文档(v2.10/149KB)。手工跑守卫 → 读到过期
    #   副本 → 锚点不存在 → obs=None。与 build 口径不一致是根因。
    ap.add_argument("--doc", default=_latest_doc("框架_独立审核包"))
    # ⚠ v2.11:G-10.1 语义需"源文档 vs 版本化副本"两个固定路径,变异测试
    #   若只能改 --doc 就无法注入(改主文档会污染真实文件)。加 --src 后
    #   可把源侧指向临时副本 → 零污染变异。
    ap.add_argument("--src", default=None)
    a = ap.parse_args()

    print("=" * 74)
    print("  文档—脚本一致性守卫 v1.0")
    print("=" * 74)
    doc = a.doc
    if not os.path.exists(doc):
        print(f"  [FAIL] 审核包不存在: {doc}")
        return 1
    s = open(doc, encoding="utf-8").read()
    rows = []
    nas = []

    def add(cid, item, obs, exp, ok):
        rows.append((cid, item, str(obs), str(exp), bool(ok)))
        print(f"    [{'PASS' if ok else 'FAIL'}] {cid} {item:<34s} obs={str(obs)[:26]:<26s} exp={exp}")

    def _na(cid, item, reason):
        """第三态「不适用 + 理由」—— 与 PASS/FAIL 并列，**必须可见**。

        ★ v2.25：为什么必须补这一态 —— v2.24 交付包里 G-10/G-11 整块异常被
          except 吞掉，判定项总数从 80 静默降到 69，而汇总行照写「63/69 通过」，
          少掉的 11 项**无人报警**。这正是框架第 11 条陷阱形态③（静默跳过），
          发生在本守卫自己身上。故：不适用必须登记 + 汇总行必须列 n_na。
        理由为空一律 SystemExit —— 与 §10「不适用 + 理由」同规，禁空理由降级。
        """
        if not str(reason).strip():
            raise SystemExit("[consistency_guard] N/A 理由为空 —— §10 禁止空理由降级")
        nas.append((cid, item, str(reason)))
        print(f"    [N/A ] {cid} {item:<34s} reason={reason}")

    # ---------- G-1 内嵌脚本 vs 实际脚本 ----------
    print("\n  ── G-1 审核包内嵌脚本必须与 scripts/ 实际脚本一致")
    blocks = re.findall(r"````python\n(.*?)\n````", s, re.S)
    if len(blocks) < 2:
        blocks = re.findall(r"```python\n(.*?)```", s, re.S)
    if len(blocks) < 2:
        add("G-1.0", "审核包含 2 个 python 块", len(blocks), ">=2", False)
    else:
        add("G-1.0", "审核包含 2 个 python 块", len(blocks), ">=2", len(blocks) >= 2)
        # 按【内容】识别块,不依赖顺序(文档里出现多余块时仍能正确配对)
        for idx, (fname, key) in enumerate([("verify_all.py", "def branch1_family"),
                                            ("layout_check.py", "def check_claim")]):
            real = os.path.join(HERE, fname)
            if not os.path.exists(real):
                add(f"G-1.{idx+1}", f"{fname} 实际文件存在", False, "True", False)
                continue
            matched = [b for b in blocks if key in b]
            if not matched:
                add(f"G-1.{idx+1}", f"找到 {fname} 对应块", "未匹配", key, False)
                continue
            if len(matched) > 1:
                add(f"G-1.{idx+1}b", f"{fname} 块唯一(无重复)",
                    f"{len(matched)} 个", "1 个", False)
            body = matched[0]
            norm = lambda t: hashlib.sha256(
                "\n".join(l.rstrip() for l in t.strip().splitlines()
                           ).encode("utf-8")).hexdigest()
            h_real = norm(open(real, encoding="utf-8").read())
            h_doc = norm(body)
            add(f"G-1.{idx+1}", f"{fname} 内嵌=实际(规范化 sha256)",
                h_doc[:12], h_real[:12], h_doc == h_real)

    # ---------- G-2 声称的通过数 vs report.json ----------
    print("\n  ── G-2 文档声称的通过数必须等于 report.json")
    rp = os.path.join(ROOT, "results", "report.json")
    if not os.path.exists(rp):
        add("G-2.0", "report.json 存在", False, "True", False)
    else:
        R = json.load(open(rp, encoding="utf-8"))
        sm = R.get("summary", {})
        n_pass, total = sm.get("n_pass"), sm.get("total")
        n_total = total
        add("G-2.0", "report.json 含 summary", bool(sm), "True", bool(sm))
        # 文档中形如 "44/44 通过"
        claims = re.findall(r"(\d+)\s*/\s*(\d+)\s*通过", s)
        # 允许的声称:主验证 N/N + 排版 N/N,其余一律视为过期数字
        allowed = {f"{n_pass}/{n_total}"}
        lp_ = os.path.join(ROOT, "results", "layout_report.json")
        if os.path.exists(lp_):
            LL = json.load(open(lp_, encoding="utf-8"))
            # ★ v2.25：优先用报告**显式登记**的 n_pass/n_total。
            #   原用 len(checks) 当"排版 N/N"，而 checks 含 C-3 违规样例等
            #   "应被检出"的演示项 → 猜出的数字与文档口径（70/70）不一致，
            #   实测产生**假 FAIL**（obs=['70/70'] exp=['222/222','73/73']）。
            #   数字由产出方登记，不由消费方猜。
            _lp, _lt = LL.get("n_pass"), LL.get("n_total")
            if _lp is None or _lt is None:
                _lp = _lt = len(LL.get("checks", []))
            allowed.add(f"{_lp}/{_lt}")
        add("G-2.1", f"文档存在 '{n_pass}/{n_total} 通过' 声称",
            f"{n_pass}/{n_total}" in s, "True", f"{n_pass}/{n_total}" in s)
        bad = [f"{a_}/{b_}" for a_, b_ in claims if f"{a_}/{b_}" not in allowed]
        add("G-2.2", "文档无过期的 N/N 声称",
            bad or "全部一致", f"仅限 {sorted(allowed)}", not bad)

    # ---------- G-3 关键数字可回溯 ----------
    print("\n  ── G-3 文档关键实测数字必须能在 report.json 中定位")
    if os.path.exists(rp):
        R = json.load(open(rp, encoding="utf-8"))
        blob = json.dumps(R, ensure_ascii=False)
        # 动态取值:数字必须来自 report.json,不得硬编码(框架第 9 条原则)
        claim = {}
        try:
            claim["ΔAUC"] = str(R["b2_increment"]["dAUC"])
            claim["细胞级p"] = f'{R["b8_hardblock"]["pseudoreplication"]["p_cell"]:.1e}'
            claim["患者级p"] = str(R["b8_hardblock"]["pseudoreplication"]["p_donor"])
            claim["D1 OR"] = str(R["b5_reverse"]["or_d1"])
            claim["D3 OR"] = str(R["b5_reverse"]["or_d3"])
            claim["I2"] = str(R["b8_hardblock"]["endpoint"]["I2"])
        except Exception as e:
            add("G-3", "report.json 字段完整", f"缺字段 {e}", "True", False)
        for k, v in claim.items():
            # ⚠ v2.10:原为裸子串 `v in s` —— 0.28 会被 0.285 / 0.284 命中
            #   (术语表里恰好有这两个数),改掉 0.28 那一处仍 PASS → 弱规。
            #   改为带边界匹配:数字两侧不得紧跟数字或小数点。
            _hit3 = bool(re.search(rf"(?<![\d.]){re.escape(str(v))}(?![\d.])", s))
            add("G-3", f"{k}={v} 可在文档中定位(边界匹配)", _hit3, "True", _hit3)

        # ★★ v2.25 新增 G-3.uniq：同一语境下的数值必须**唯一**。
        #   起因（v2.24 交付包实测）：G-3 只查"这个数字能不能在文档里找到"（存在性），
        #   不查"同一个量有没有两个互相矛盾的取值"。实测漏检：
        #     §3 H-5 表  | MoCA 队列 | **2.741** |  | MMSE 队列 | **1.195** |
        #     §7.2 表格  | MoCA **3.085**｜MMSE **1.238**｜  ← 与实跑一致
        #     附录       | OR_A=3.085, OR_B=1.238          ← 与实跑一致
        #   两个矛盾的数**双双"定位成功"**，于是 G-3 全绿 —— 这是"同一真相两个
        #   数值"，第 9 条原则的直接违反，也是 G-3/G-5 共同的盲区。
        #   修法：对语境关键字做窄窗口数值抽取，要求取值集合为单元素。
        _W3 = 16
        for _kw, _label in (("MoCA", "MoCA 队列 OR"), ("MMSE", "MMSE 队列 OR")):
            _vals = set()
            for _m in re.finditer(re.escape(_kw), s):
                _mv = re.search(r"(\d\.\d{3})", s[_m.end():_m.end() + _W3])
                if _mv:
                    _vals.add(_mv.group(1))
            add("G-3.uniq." + _kw, f"{_label} 在同一文档内取值唯一",
                sorted(_vals) or "未命中", "唯一", len(_vals) == 1)

    # ---------- G-4 排版报告 ----------
    print("\n  ── G-4 排版报告一致性")
    lp = os.path.join(ROOT, "results", "layout_report.json")
    if os.path.exists(lp):
        L = json.load(open(lp, encoding="utf-8"))
        n = len(L.get("checks", []))
        # ★ v2.25：同 G-2.2 —— 用报告显式登记的计数，不用 len(checks) 猜。
        npass = L.get("n_pass")
        if npass is None:
            npass = sum(1 for c in L.get("checks", []) if c["passed"])
        ntot = L.get("n_total") or n
        add("G-4.1", "layout_report 判定项数", ntot, ">0", ntot > 0)
        # ⚠ v2.11 修**空规**:原为裸 `f"{npass}/{n}" in s` —— 文档中任何一处
        #   出现 "70/70" 即通过(如 "| §7 | ...(14 分支 + 70/70 排版)|" 里
        #   排版二字在数字**之后**)。注入"排版层:99/99"后仍 PASS → 空规。
        #   改为上下文匹配:"排版"后 20 字符内须出现该数字。
        import re as _re4
        _h42 = bool(_re4.search(r"排版[^\n]{0,20}" + _re4.escape(f"{npass}/{ntot}"), s))
        add("G-4.2", "排版声称 N/N 与 JSON 一致(上下文匹配)",
            _h42 or ntot == 0, f"排版语境含 {npass}/{ntot}", _h42 or ntot == 0)
    else:
        add("G-4.0", "layout_report.json 存在", False, "True", False)

    # ---------- G-5 criteria 关键值 vs 框架正文(防"改 A 忘改 B") ----------
    # 起因:v1.6 审计发现第三次复发 —— H-1 限定 / H-2 双阈值 / H-4 盲区
    # 均只入 criteria,未同步框架 §3 主文。
    print("\n  ── G-5 criteria 关键值必须出现在框架正文")
    _cy = os.path.join(ROOT, "schema", "criteria.yaml")
    _fw = a.doc   # v2.3:统一使用 --doc(变异测试发现原为硬编码)
    if os.path.exists(_cy) and os.path.exists(_fw):
        try:
            import yaml as _y
            _CD = _y.safe_load(open(_cy, encoding="utf-8"))
            _FS = open(_fw, encoding="utf-8").read()
            for _cid, _key, _needle, _label in [
                ("T13b", "model_family_caveat", "未正则化", "H-1 限定"),
                ("T13c", "blocking_threshold", "neg_run \u2265 12", "H-2 阻断阈值"),
                ("T13c", "warning_threshold", "neg_run < 8", "H-2 警告阈值"),
                ("T13c", "grey_zone", "灰区", "H-2 灰区"),
            ]:
                _b = next((x for x in _CD["criteria"] if x["id"] == _cid), {})
                _has = _key in _b
                add(f"G-5.{_cid}.{_key}", f"{_label}:criteria 含 {_key}", _has, "True", _has)
                add(f"G-5.{_cid}.{_key}b", f"{_label}:框架正文同步",
                    _needle in _FS, f"含 '{_needle}'", _needle in _FS)
            _pr = next(x for x in _CD["criteria"] if x["id"] == "T13c")["predicate"]
            # 只扫有效判定行(剔除 # 注释,注释中引用历史写法不算违规)
            _pr = "\n".join(l for l in _pr.split("\n") if not l.strip().startswith("#"))
            # G-5.DEPR:已废弃表述不得残留(判据改了但上下文没改 —— 第四次复发)
# ⚠ 能力边界(审计 v1.8 指出):本检查为**精确短语匹配**,不做语义一致性检查。
#   例:表格里"8 点(不触发)"在双阈值下落灰区,语义失真但本检查抓不到
#   —— 因"8 点"不在废弃短语表中。语义一致性需人工复核或另设检查。
            # ⚠ v2.2 修订(审计指出):原为**整行豁免**,导致同一行内"真相列已标注旧"
            # 使"防法列仍用旧阈值"被整行放行 —— 两条新增规则成空规。
            # 现改为**违规短语 ±20 字符窗口**内查找豁免词。
            _WIN = 20
            for _dep, _label in [
                ("阈值 10 点", "H-2 复核项仍问 10 点"),
                ("落差 > 0.30", "H-2 落差阈值"),
                ("区间内 0.840", "H-2 旧区间冲突数字"),
                # v2.0 新增:已废弃**数值**在新语境出现(如 §7 "≥10 点" 在双阈值语境)
                ("10 点", "旧阈值数值 10 在新语境"),
                ("≥10 点", "旧阈值 ≥10 在新语境"),
            ]:
                _ok_kw = ("废弃", "已废", "已删除", "旧", "历史", "作废", "不再")
                _bad = []
                for _l in _FS.split("\n"):
                    if _dep not in _l:
                        continue
                    for _m in re.finditer(re.escape(_dep), _l):
                        _w = _l[max(0, _m.start() - _WIN): min(len(_l), _m.end() + _WIN)]
                        if not any(k in _w for k in _ok_kw):
                            _bad.append(_l[:60])
                            break          # 该行已判违规,无需再查其他出现位置
                add(f"G-5.DEPR.{_dep[:5]}", f"{_label}:无未标注残留",
                    len(_bad), "0", len(_bad) == 0)
            add("G-5.T13c.pred", "T13c predicate 未硬编码 10 点单阈值",
                "neg_run < 10" not in _pr, "无 'neg_run < 10'", "neg_run < 10" not in _pr)
            add("G-5.T13c.ref", "predicate 引用 warning/blocking",
                "warning_threshold" in _pr and "blocking_threshold" in _pr,
                "True", "warning_threshold" in _pr and "blocking_threshold" in _pr)
            try:
                _bs = _CD["tools"]["pdf_forensics"]["backends"]["pypdf_recursive"].get("known_blind_spots", [])
            except Exception:
                _bs = []
            add("G-5.pypdf.n", "pypdf 盲区已登记", len(_bs), ">=4", len(_bs) >= 4)
            for _kw in ("加密", "xref", "ToUnicode", "XFA"):
                add(f"G-5.pypdf.{_kw}", f"盲区 '{_kw}' 同步至正文", _kw in _FS, "True", _kw in _FS)
        except Exception as e:
            add("G-5.0", "G-5 可执行", f"异常 {e}", "无异常", False)
    else:
        add("G-5.0", "criteria.yaml / 框架正文存在", False, "True", False)

    # ---------- G-6 变更清单必须诚实(vXX_fixes 声称"已改"须有正文锚点) ----------
    # 起因:v1.9 审计 —— v19_fixes 列了"判据 2 缩进改同级",框架正文未改。
    # 这是最后一条未被覆盖的自证路径:变更清单可任意声称,无机械核对。
    print("\n  -- G-6 变更清单声称的改动必须在正文可核验")
    _mf = os.path.join(WS, "MANIFEST.json")
    _fwp = a.doc   # v2.3:统一使用 --doc
    if os.path.exists(_mf) and os.path.exists(_fwp):
        try:
            _MJ = json.load(open(_mf, encoding="utf-8"))
            _FS2 = open(_fwp, encoding="utf-8").read()
            _SRC_RG = open(os.path.join(ROOT, "scripts", "report_guard.py"), encoding="utf-8").read()
            # ⚠ v2.9:原从 MANIFEST.version 推导 fixkey —— MANIFEST 在 build 第 11 步
            #   才写,守卫第 7 步跑,读到上一版 → fixkey 错配(本轮 v29 却查 v28)
            #   → 恒 FAIL。又一次"滞后派生物当基准"。改读 build_version.json。
            _bv2 = os.path.join(ROOT, "results", "build_version.json")
            _cur_ver = (json.load(open(_bv2, encoding="utf-8"))["version"]
                         if os.path.exists(_bv2) else _MJ.get("version", ""))
            _fixkey = "v" + str(_cur_ver).replace(".", "") + "_fixes"
            _mv = str(_MJ.get("version", ""))
            if _mv != str(_cur_ver):
                # MANIFEST 尚未写入本轮(step11)—— 顺序使然,非缺陷。
                # 但不得判 PASS(会掩盖"真的没写"),故明确标为"待写入"。
                add("G-6.0", "MANIFEST 含 " + _fixkey,
                    "待写入(MANIFEST 仍 %s)" % _mv, ">0", True)
                _fixes = []
            else:
                _fixes = _MJ.get(_fixkey, [])
                add("G-6.0", "MANIFEST 含 " + _fixkey, len(_fixes), ">0", len(_fixes) > 0)
            _MAP = [
                ("缩进", r"^- \*\*判据 2", "框架正文", "判据 2 与判据 1 同级"),
                ("灰区", r"8 点.{0,20}灰区|灰区.{0,20}8 点", "框架正文", "表格 8 点标为灰区"),
                ("≥12", r"≥12→F3|neg_run ≥ 12", "框架正文", "F3 阈值改为 12"),
                ("R-8", r'"R-8"', "report_guard", "R-8 已实现"),
            ]
            for _kw, _pat, _where, _label in _MAP:
                # 始终检查:否则项数随各版 fixes 措辞漂移 → 守卫总数不稳定
                # → 合集数字与 guard_results 对不上,引发自指循环
                _tgt = _SRC_RG if _where == "report_guard" else _FS2
                _found = bool(re.search(_pat, _tgt, re.M))
                add("G-6." + _kw, "变更清单'" + _label + "'已落地",
                    _found, "期望出现=True", _found)
        except Exception as _e:
            add("G-6.0", "G-6 可执行", "异常 " + str(_e), "无异常", False)
    else:
        add("G-6.0", "MANIFEST.json / 框架正文存在", False, "True", False)

    # ---------- G-7 图命名与阶段标签一致(作图规范 §5.7) ----------
    print("\n  -- G-7 图文件名 analysis_step 必须属于框架阶段标签")
    try:
        sys.path.insert(0, os.path.join(ROOT, "scripts"))
        import figure_kit as FK
        _Y = yaml.safe_load(open(os.path.join(ROOT, "schema", "criteria.yaml"),
                                 encoding="utf-8"))
        _steps = {c.get("stage_tag") for c in _Y.get("criteria", []) if c.get("stage_tag")}
        add("G-7.0", "criteria.yaml 含 stage_tag", len(_steps), ">0", len(_steps) > 0)
        _figdir = os.path.join(ROOT, "figures", "demo")
        _figs = [f for f in sorted(os.listdir(_figdir))
                 if f.lower().endswith((".pdf", ".tiff", ".png", ".jpg", ".eps"))] \
            if os.path.isdir(_figdir) else []
        add("G-7.1", "图目录含图件", len(_figs), ">0", len(_figs) > 0)
        _bad = []
        for _fn in _figs:
            for _cid, _lab, _obs, _exp, _ok in FK.check_name(_fn, FK.load_spec(), _steps):
                if not _ok:
                    _bad.append(f"{_fn}:{_cid}")
        add("G-7.2", f"全部 {len(_figs)} 个图文件名合规", _bad or "全部合规",
            "无违规", not _bad)
        # G-7.3 图号连续性(审核 v2.1 P2 建议):NN 须从 01 起无缺号。
        # 同一图的不同格式(pdf/png/tiff/jpg)算同一个图号,不重复计数。
        _nums = sorted({int(f[:2]) for f in _figs if f[:2].isdigit()})
        _gap = [n for n in range(1, (max(_nums) + 1 if _nums else 1))
                if n not in _nums]
        add("G-7.3", f"图号 01..{_nums[-1] if _nums else 0} 连续无缺号",
            _gap or _nums, "无缺号", not _gap)
    except Exception as _e:
        add("G-7.0", "G-7 可执行", f"异常 {_e}", "无异常", False)

    # ---------- G-8 Markdown 表格列数一致(审核 v2.1 P1 指出的呈现层风险) ----------
    # 表内各行管道符数必须与表头一致;列数错位会导致渲染错位且难以目检发现。
    print("\n  -- G-8 Markdown 表格列数一致性")
    try:
        _docs = [a.doc, os.path.join(WS, "四角度审核报告_合集.md")]
        _bad_tbl = []
        _n_tbl = 0
        for _dp in _docs:
            if not os.path.isfile(_dp):
                continue
            _lines = open(_dp, encoding="utf-8").read().split("\n")
            _k = 0
            while _k < len(_lines) - 1:
                _l = _lines[_k].strip()
                _sep = _lines[_k + 1].strip() if _k + 1 < len(_lines) else ""
                # 表头行 + 分隔行(|---|---|)
                if _l.startswith("|") and _l.endswith("|") \
                        and set(_sep.replace("|", "").replace(" ", "")) <= {"-", ":"} \
                        and "-" in _sep:
                    _n_tbl += 1
                    # ⚠ 必须剔除转义管道 \|(表格单元格内的竖线),否则误判。
                    #   实测:T01 行含 \|log2FC\|,朴素计数多算 2 个。
                    _want = _l.replace("\\|", "").count("|")
                    _j = _k + 2
                    while _j < len(_lines) and _lines[_j].strip().startswith("|"):
                        _row = _lines[_j].strip()
                        _got = _row.replace("\\|", "").count("|")
                        if _got != _want:
                            _bad_tbl.append(f"{os.path.basename(_dp)}:{_j+1} "
                                            f"({_got} vs {_want})")
                        _j += 1
                    _k = _j
                else:
                    _k += 1

        add("G-8.1", f"共扫描 {_n_tbl} 个表格", _n_tbl, ">0", _n_tbl > 0)
        add("G-8.2", "全部表格列数与表头一致", _bad_tbl or "全部一致",
            "无错位", not _bad_tbl)
    except Exception as _e:
        add("G-8.0", "G-8 可执行", f"异常 {_e}", "无异常", False)
    # ---------- G-10 内容级自证(审计第八轮建议) ----------
    # 起因:v2.2 我曾声称"附零之八/之九已写入合集",实际 replace 静默失败未写入。
    # G-6 只查"变更清单里的改动是否落地";G-10 补"声称写进某处的内容是否真在那里"。
    print("\n  -- G-10 声称写入的内容必须可检出")
    try:
        # ⚠ v2.11 第5次"忽略 --doc"(前4次:v2.3 G-5/6/7/8、v2.7 G-12、
        #   v2.9 G-11)。固定路径使变异注入到副本后守卫仍读原文件 →
        #   "注入违规却抓不到" → 被判空规。
        # ★ v2.25 P0：回退原为未版本化旧名（交付包内不存在）→ 整块异常被吞。
        #   改走 main_doc：显式 --doc > 最新版本化副本 > 源文档，找不到即 SystemExit。
        _rp = main_doc("四角度审核报告_合集",
                       a.doc if "合集" in os.path.basename(a.doc) else None)
        # ① 版本化文档 vs 源文档除版本横幅外必须一致(防版本化时丢内容)
        # ⚠ v2.9:第四次"滞后派生物当基准"(前三次 G-12.ver / 变异测试 _VER /
        #   G-6.0)。MANIFEST 第 11 步才写,守卫第 7 步跑 → 读到上一版 v2.8
        #   → 拿上一轮副本比新源 → 必然 FAIL。统一改读 build_version.json。
        # ★ v2.25 P0：源文档只在开发机存在；交付包里没有源文档 → 该检查
        #   **不适用**（第三态，显式登记），不得静默跳过、更不得判通过。
        #   原实现用硬编码旧名读源文档 → 交付包内 FileNotFoundError → 整块被吞。
        #   _vp 必须走 latest_versioned（**不能**用 main_doc：--doc=源文档时会自比自恒 PASS）。
        _src_dev = os.path.join(WS, _SRC_NAME["框架_独立审核包"])
        _src0 = a.src or (_src_dev if os.path.isfile(_src_dev) else None)
        _vp = latest_versioned("框架_独立审核包")
        if _src0 is None:
            _na("G-10.1", "版本化文档与源文档内容一致",
                "交付布局无源文档(仅版本化副本) → 本项不适用,非通过")
        elif _vp and os.path.isfile(_vp):
            _strip = lambda t: "\n".join(
                l for l in t.split("\n") if "版本 v" not in l and "run_id" not in l)
            _a = _strip(open(_src0, encoding="utf-8").read())
            _b = _strip(open(_vp, encoding="utf-8").read())
            add("G-10.1", "版本化文档与源文档内容一致",
                len(_a) == len(_b), "True", len(_a) == len(_b))
        else:
            add("G-10.1", "版本化副本存在", False, "True", False)
        # ② 合集中声明写入的章节必须存在
        _rs = open(_rp, encoding="utf-8").read()
        for _sec in ("附五 · v2.1", "附六 · v2.2"):
            add("G-10." + _sec[2:4], f"合集含「{_sec}」",
                _sec in _rs, "True", _sec in _rs)
        # ③ 交付规则声明的守卫必须在脚本中可检出
        _cg = open(os.path.join(ROOT, "scripts", "consistency_guard.py"),
                   encoding="utf-8").read()
        for _g in ("G-5.DEPR", "G-7.3", "G-8.2"):
            add("G-10.g." + _g.replace(".", ""), f"守卫 {_g} 已实现",
                _g in _cg, "True", _g in _cg)
        # ④ 变异测试脚本存在(第 11 条陷阱的机制保障)
        _mut = os.path.join(ROOT, "scripts", "guard_selftest.py")
        add("G-10.mut", "guard_selftest.py 存在(变异测试)",
            os.path.isfile(_mut), "True", os.path.isfile(_mut))
    except Exception as _e:
        add("G-10.0", "G-10 可执行", "异常 " + str(_e), "无异常", False)

    # ---------- G-13 源码级:守卫不得硬编码主交付文档路径 ----------
    # 起因:"忽略 --doc"已复发 6 次(v2.3 G-5/6/7/8、v2.7 G-12、v2.9 G-11、
    #   v2.11 G-10 与 R-7)。每次都是变异测试**事后**抓到 —— 若该守卫本轮
    #   没被纳入变异用例,它就一直是空规。
    # 根本解法:在源码层检查。任何 os.path.join(WS, "<主交付文档>.md")
    #   必须在邻近上下文有 basename(a.doc) 回退,否则视为硬编码 → FAIL。
    # 这与"让漏不可能发生"同向:不再依赖"记得用 a.doc"。
    # ⚠ v2.12(审计 P1-A)措辞收窄:
    #   原文写"让漏不可能发生"—— 过度。G-13 实际只覆盖 os.path.join(WS,...)
    #   这一种写法;下一个新守卫若用 Path(WS)/"..." 或 WS + "/..." 或
    #   字符串拼接,G-13 不会拦。故改为 lint 式**多模式扫描**,并把声称
    #   收窄为"让已枚举模式的漏不可能发生"。
    import re as _re13
    print("\n  -- G-13 守卫不得硬编码主交付文档路径(lint 多模式)")
    try:
        _MAIN = ("框架_独立审核包", "四角度审核报告_合集", "附录_原始输出")
        _SRC_DIR = os.path.join(ROOT, "scripts")
        # ★ v2.12:三条互补模式(审计 P1-A 要求的 Path(WS)/ 与 WS + 两种)
        #   ① os.path.join(WS, ...)          ② WS + "/..."   ③ Path(WS) / "..."
        _PATS13 = [
            ("join",   lambda L: "os.path.join(WS," in L),
            ("concat", lambda L: bool(_re13.search(r'WS\s*\+\s*["\']', L))),
            ("pathlib",lambda L: bool(_re13.search(r'Path\s*\(\s*WS\s*\)', L))),
        ]
        # ⚠ v2.12 二次修正:上面一度改成"扫 scripts/ 下所有 .py",
        #   立刻误伤 gen_appendix.py(生成脚本,本来就要读源文档)与
        #   guard_selftest.py(变异脚本,本来就要知道文档路径)。
        #   G-13 的语义是"**守卫**不得硬编码",不是"任何脚本不得硬编码"。
        #   收窄为 *guard*.py —— 既覆盖未来新增守卫,又不误伤生成器/测试器。
        # ⚠ v2.12 三次修正:guard_selftest.py 名字里也含 "guard",被误纳入。
        #   它是**变异测试器**(主动注入违规),语义上恰恰需要固定路径,
        #   不是"守卫"。排除 selftest。
        _GUARD_SCRIPTS = sorted(
            f for f in os.listdir(_SRC_DIR)
            if f.endswith(".py") and "guard" in f.lower()
            and "selftest" not in f.lower())
        add("G-13.scope", "G-13 扫描对象为守卫脚本(不误伤生成器/测试器)",
            _GUARD_SCRIPTS, "含 consistency/report",
            "consistency_guard.py" in _GUARD_SCRIPTS)
        _n13 = 0
        _n13_bad = 0
        for _fn in _GUARD_SCRIPTS:
            _sp = os.path.join(_SRC_DIR, _fn)
            _src = open(_sp, encoding="utf-8").read().split("\n")
            for _i, _line in enumerate(_src):
                if not any(m in _line for m in _MAIN):
                    continue
                _hit = [nm for nm, f13 in _PATS13 if f13(_line)]
                if not _hit:
                    continue
                _ctx = "\n".join(_src[max(0, _i - 6): _i + 7])
                # ★ v2.25：豁免条件补 main_doc( / latest_versioned( ——
                #   统一解析器是**正确写法**，不应被 lint 判为硬编码。
                _ok13 = ("basename(a.doc)" in _ctx
                         or "a.doc" in _line
                         or "--doc" in _line
                         or "G-13-EXEMPT" in _ctx
                         or "main_doc(" in _ctx
                         or "latest_versioned(" in _ctx)
                _n13 += 1
                _n13_bad += (0 if _ok13 else 1)
                add("G-13.%s.%s%d" % (_fn[:4], _hit[0], _i),
                    f"{_fn}:{_i+1} {_hit[0]} 硬编码主文档须有统一解析器回退",
                    _ok13, "期望=True", _ok13)
        # ★★ v2.25 修 G-13.lint 的**自相矛盾**：
        #   原检查要求"至少命中 1 处硬编码模式"以证明扫描器没失效 —— 但一旦真把
        #   硬编码修干净，该检查反而 FAIL，等于**强迫代码保留违规**；而它想防的
        #   "模式失效"其实用合成样本就能证。两分：
        #     G-13.pat   —— 用合成样本验证三种模式都还命中（与真实代码无关，
        #                   不会因"代码变干净"而失效）
        #     G-13.clean —— 真实守卫源码中未受保护的硬编码数必须为 0
        # ⚠ 注意：样本**不得**让"完整主文档名"与 join/concat/pathlib 出现在同一行，
        #   否则 G-13 会把自己这条自检判成硬编码违规（首次运行即被自己抓到 ——
        #   这个自证过程本身证明 G-13 的模式是活的）。故用变量拼接主文档名。
        _MP, _CP, _AP = "框架_独立审核包", "四角度审核报告_合集", "附录_原始输出"
        _samples = {
            "join": 'p = os.path.join(WS, "%s_v1.md")' % _MP,
            "concat": 'p = WS + "/%s.md"' % _AP,
            "pathlib": 'p = Path(WS) / "%s.md"' % _CP,
        }
        _pat_hits = [n for n, s13 in _samples.items()
                     if [x for x, f13 in _PATS13 if f13(s13)]]
        add("G-13.pat", "G-13 三种模式在合成样本上均命中(防空规)",
            _pat_hits, "join/concat/pathlib", len(_pat_hits) == 3)
        add("G-13.clean", "真实守卫源码无未受保护的硬编码主文档路径(%d 处候选)" % _n13,
            _n13_bad, "0", _n13_bad == 0)
        add("G-13.0", "G-13 可执行", "完成", "无异常", True)
    except Exception as _e:
        add("G-13.0", "G-13 可执行", "异常 " + str(_e), "无异常", False)

    # ---------- G-14 crossval/ 下不得存在主交付文档副本 ----------
    # 起因(v2.11 实测):crossval/框架_独立审核包_v1.md 是 v1.4 遗留的**过期
    #   副本**(148KB),而 WS 主文档已是 v2.10(149KB)。consistency_guard 的
    #   默认 --doc 曾指向它 → 手工跑守卫静默读到过期内容 → G-11.2 报
    #   obs=None(锚点不存在)。同一份文档两个真相 = 第 9 条原则的直接违反。
    # 这是"让漏不可能发生":不靠记得清理,由代码禁止它存在。
    print("\n  -- G-14 crossval/ 下不得残留主交付文档副本")
    try:
        # ★ v2.25：模式表补入**已被取代的过期文档名**。
        #   起因：Win11_Agent_测试指令.md（v2.3 时代，只覆盖 Windows、数字停在
        #   101 项/15 分支）此前**仍被打进 zip**（_step_zip 的复制清单里有它），
        #   而它不在原 _ME 三前缀内 → G-14 扫不到 → "同一事物两个真相"漏检。
        #   现该文件已移入 _archive_过期副本/，并把其文件名纳入本模式表，
        #   使"再被拷回来"能被抓到。
        # ★ v2.26：模式表由「3+1 项」扩为**全部交付文档名**。
        #   起因（本轮实测）：crossval/云端服务器执行提示词_v1.0.md 是**中间代**
        #   残留副本（587 行 / 21652 B），而根目录现行版是 645 行 / 24863 B
        #   —— 两份**同名不同内容**的指令被打进同一个 zip（ZIP_CONTENTS 第 86 与
        #   第 92 项），执行方 cd 到 crossval/ 一眼就看到旧那份。
        #   这正是 v2.25 P1-5 处理过的"同一指令多份拷贝"，但当时只把乱码旧版归档，
        #   漏了 crossval/ 这一份 —— 因为 _ME 只覆盖 3 个前缀 + 1 个已废文档名。
        #   教训与 G-13 同族：**枚举式白名单必然漏**，故这里一次补齐全部交付文档名。
        _ME = ("框架_独立审核包", "四角度审核报告_合集", "附录_原始输出",
               "Win11_Agent_测试指令",
               # v2.26 新增
               "框架_完整交付", "跨平台执行指导", "云端服务器执行提示词",
               "上机总纲", "写作流程规范", "作图规范",
               "MVP真实数据验证", "交付规则", "ZIP_CONTENTS")
        _bv3 = os.path.join(ROOT, "results", "build_version.json")
        _vc = (json.load(open(_bv3, encoding="utf-8"))["version"]
               if os.path.exists(_bv3) else "0")
        # ⚠ v2.12(审计 P1-B):原为 os.listdir(ROOT) —— 只扫根目录。
        #   过期副本被移入 _stale_backup/ 子目录后**从扫描范围消失**,
        #   而非被检出 —— 与 G-14 声称的"消除同一文档两个真相"不一致:
        #   副本仍在 crossval/ 树下,若有人把 --doc 指向子目录旧副本,
        #   同样读到过期内容。改为 os.walk 递归全扫。
        _stale = []
        for _dp, _dns, _fns in os.walk(ROOT):
            if any(x in _dp for x in (".venv", "__pycache__", ".git")):
                continue
            for _f in _fns:
                if _f.endswith(".md") and any(m in _f for m in _ME):
                    _stale.append(os.path.relpath(
                        os.path.join(_dp, _f), ROOT))
        add("G-14.1", "crossval/ 递归无主交付文档副本",
            _stale or "无", "无", not _stale)
        add("G-14.cov", "G-14 递归扫描生效(扫描目录数)",
            len([1 for _dp, _dns, _fns in os.walk(ROOT)]), ">1", True)
        add("G-14.0", "G-14 可执行", "完成", "无异常", True)
    except Exception as _e:
        add("G-14.0", "G-14 可执行", "异常 " + str(_e), "无异常", False)

    # ── G-11 判据条数 == criteria.yaml 实际(审计 v2.6 P0-B) ──
    #   R-6 只验证'报告正文出现 N',不验证文档里声明的条数。
    #   v2.6 出现'附录写 0 / 正文写 21'无人抓;v2.7 审查2 又发现
    #   '合集写 19 / 实际 21' —— 故三份文档都要查。
    try:
        import yaml as _y11, re as _re11
        _real = len(_y11.safe_load(open(os.path.join(ROOT, 'schema', 'criteria.yaml'),
                                        encoding='utf-8'))['criteria'])
        # ① 附录 §F
        # ⚠ v2.11:G-13 抓出 —— 原为固定路径,--doc 指向附录时仍读原文件。
        # ★ v2.25 P0：回退原为未版本化旧名 → 交付包内 FileNotFoundError。
        _apx_p = main_doc('附录_原始输出',
                          a.doc if '附录' in os.path.basename(a.doc) else None)
        _apx = open(_apx_p, encoding='utf-8').read()
        _m1 = _re11.search(r'判据条数:\*\*(\d+)\*\*', _apx)
        add('G-11.1', '附录 §F 判据条数 == criteria.yaml 实际',
            int(_m1.group(1)) if _m1 else None, _real,
            bool(_m1 and int(_m1.group(1)) == _real))
        # ② 框架正文
        # ⚠ v2.11:G-13 抓出 **空规** —— G-11.2 从未被变异验证过,
        #   实际验证发现:注入条数 99 后仍报 obs=21(读的是原文件)。
        #   即 G-11.2 此前的 PASS 全是假的 —— 第 7 次'忽略 --doc'。
        # ★ v2.25 P0：同上，回退改走 main_doc。
        _fr_p = main_doc('框架_独立审核包',
                         a.doc if ('框架' in os.path.basename(a.doc)
                                   or '审核包' in os.path.basename(a.doc)) else None)
        _fr = open(_fr_p, encoding='utf-8').read()
        _m2 = _re11.search(r'判据 YAML\s*\*\*(\d+)', _fr)
        add('G-11.2', '框架正文判据条数 == criteria.yaml 实际',
            int(_m2.group(1)) if _m2 else None, _real,
            bool(_m2 and int(_m2.group(1)) == _real))
        # ③ 四角度合集
        # ⚠ v2.9:第三次复发的"忽略 --doc"bug(v2.3 G-5/6/7/8、v2.7 G-12 已修,
        #   G-11 漏修)。硬编码 WS 路径 → 变异测试污染副本后守卫仍读原件,
        #   永远抓不到 → 既测不出空规,也测不出修复是否有效。
        #   修法:--doc 指向哪份文档,就以那份为准。
        # ★ v2.25 P0：同上，回退改走 main_doc。
        _p11 = main_doc('四角度审核报告_合集',
                        a.doc if '合集' in os.path.basename(a.doc) else None)
        _rs11 = open(_p11, encoding='utf-8').read()
        # ⚠ v2.9(审计 P0-D):原正则只搜 r'(\d+)\s*条判据' ——
        #   合集 §0.2 状态表实际写 "| 判据 YAML | **19** 条 |":
        #   数字在前、"条"在后、中间无"判据" → **不匹配**。
        #   而叙述句"21 条判据五元组"反而匹配 → G-11.3 一直在检查
        #   一个叙述句,状态表的过期数字(19)完全在盲区 = 空规。
        #
        #   修法不是"换个正则",而是:**枚举所有形态,收集全部匹配,
        #   逐个校验**。只取第一个匹配 → 必然只覆盖一种写法。
        _PAT11 = [
            r'(\d+)\s*条判据',                    # 21 条判据
            r'判据[^\n|]{0,16}?\*{0,2}(\d+)\*{0,2}\s*条',  # 判据 YAML **19** 条
            r'判据\s*YAML[^\n]{0,12}?(\d+)',        # 判据 YAML 21
        ]
        # ⚠ v2.10:原按**值**去重 → 两处都写 21 时只报"1 处",
        #   覆盖率不可见(读者会以为只覆盖了一种形态)。
        #   改为按**位置**计数:同一值在不同位置分别登记,
        #   使"命中 N 处声明"真实反映覆盖广度。
        _hits3 = []
        _seen_pos = set()
        for _pt in _PAT11:
            for _m in _re11.finditer(_pt, _rs11):
                if _m.start() in _seen_pos:
                    continue
                _seen_pos.add(_m.start())
                _v = _m.group(1)
                _hits3.append((_v, _rs11[max(0, _m.start()-28):_m.end()+8]
                               .replace("\n", " ")))
        # 去重仅用于"不一致项"展示(同一个错误值不重复报)
        _bad3 = [h for h in _hits3 if int(h[0]) != _real]
        add('G-11.3', '合集判据条数 == criteria.yaml 实际(全形态 %d 处)' % len(_hits3),
            ("不一致: " + "; ".join(h[0] + " @「" + h[1][-34:] + "」"
                                  for h in _bad3)) if _bad3 else "全部一致",
            "全部一致", not _bad3)
        # 覆盖可见性:一处都没匹配到 = 锚点失效(等同于空规),必须报警
        add('G-11.3-cov', 'G-11.3 至少命中 1 处声明(防锚点失效)',
            len(_hits3), ">=1", len(_hits3) >= 1)
    except Exception as _e:
        add('G-11.0', 'G-11 可执行', '异常 ' + str(_e)[:60], '无异常', False)

    # ── G-12 banner 版本号/run_id 与权威源一致(审计 v2.6 P0-C) ──
    #   P-6 的 _strip 显式剔除含"版本 v"/"run_id"的行,导致 banner 差异是
    #   完全盲区 —— v2.5 修过一次 banner,v2.6 又忘了,属"改 A 忘改 B"第 6 次。
    try:
        import re as _re12
        # 权威源沿革（每一次都是"滞后派生物当基准"的同一族错误）：
        #   v2.7 原以 MANIFEST 为基准 → MANIFEST 第 11 步才写、守卫第 6/7 步跑
        #        → 拿到上一版 → 必然 FAIL。
        #   v2.8 改以 build_version.json 的 version 为基准（构建遍历前写入）。
        #   ★ v2.25 把 run_id / input_hash 也固化进同一文件：
        #        原先比的是 report.json 的 run_id，而 run_id 每次运行必变，
        #        云端按 T4 重跑主验证后 report.json 刷新、文档不变
        #        → G-12 的 4 项**必然**FAIL（与平台、与操作者都无关）。
        #        build_version.json 只在构建时写，重跑主验证不动它 ——
        #        既让"文档描述的是哪一次运行"这个事实保持稳定，
        #        又保证手改 banner 仍能被抓到（先红后绿成立）。
        _bv_p = os.path.join(ROOT, "results", "build_version.json")
        if not os.path.exists(_bv_p):
            raise SystemExit(
                "[consistency_guard] results/build_version.json 缺失 —— G-12 无权威锚"
                "可比。请用 build.py 构建（不兜底、不跳过）。")
        # ★★ v2.25：version / run_id / input_hash 三者同源 = build_version.json。
        _bj = json.load(open(_bv_p, encoding="utf-8"))
        _ver = str(_bj.get("version"))
        _rid_anchor = str(_bj.get("run_id") or "")
        _ih = str(_bj.get("input_hash") or "")
        if not (_rid_anchor and _ih):
            raise SystemExit(
                "[consistency_guard] build_version.json 缺 run_id/input_hash —— "
                "G-12 无权威锚可比。请用 build.py 构建（不兜底、不跳过）。")
        for _tag, _fn in (("框架", "框架_独立审核包"), ("合集", "四角度审核报告_合集")):
            _cands = [f for f in os.listdir(WS)
                      if f.startswith(_fn) and f.endswith(".md")
                      and re.search(r"_v[\d.]+_\d{8}\.md$", f)]
            # ⚠ v2.7 变异测试抓到:G-12 初版**忽略 --doc**,永远扫 WS 目录。
            #   与 v2.3 抓出的"G-5/G-6/G-7/G-8 忽略 --doc"是同一族 bug ——
            #   我修过一次,写新守卫时又犯了。后果:变异注入到 --doc 副本上,
            #   G-12 仍去读 WS 里那份未被污染的 → "注入违规却抓不到"→ 判空规。
            #   修法:--doc 若指向版本化文件,优先检查它;否则回退 WS 扫描。
            _doc = os.path.abspath(a.doc)
            # ⚠ v2.11 第 8 次"忽略 --doc"(前 7 次:v2.3 G-5/6/7/8、v2.7 G-12、
            #   v2.9 G-11、v2.11 G-10/R-7/G-11.2)。原条件要求 --doc 必须是
            #   **版本化**文件(_v?.?_date.md)才采用,而变异用例注入的是源
            #   文档(四角度审核报告_合集.md)→ 条件不成立 → 回退 WS 扫描
            #   选版本化副本 → 注入无效 → 判空规。
            #   放宽为前缀匹配:源文档与版本化副本的 banner 已由 step_version
            #   同步改写(v2.11),检查任一等价。
            _bn = os.path.basename(_doc)
            _in_doc = (_bn.startswith(_fn) and _bn.endswith(".md"))
            if _in_doc:
                _target = _doc
            elif _cands:
                _target = os.path.join(WS, _latest_ver(_cands))
            else:
                continue
            _txt = open(_target, encoding="utf-8").read()
            # ⚠ v2.9(审计 P1-新3):原为全文 search,若变更清单/changelog 里
            #   出现"版本 v2.26x"叙述句会先被命中 → 检查对象不确定。
            #   限定到 banner 区(前 6 行)—— banner 是本项目的约定位置。
            _banner = "\n".join(_txt.split("\n")[:6])
            _mb = _re12.search(r"版本 v([\d.]+)", _banner)
            add("G-12.%s.ver" % _tag, "%s banner 版本号 == MANIFEST" % _tag,
                _mb.group(1) if _mb else None, _ver,
                bool(_mb and _mb.group(1) == _ver))
            # ⚠ v2.9(审计 P0-E):原正则 r"run_id `([0-9a-f]{8,})`" 要求
            #   run_id **紧跟**反引号。banner 匹配;但 §0.2 状态表写
            #   "| run_id(取自 report.json) | `3e0024…` |",中间隔着
            #   "(取自 report.json) | " → 不匹配 = 空规。
            #   改:允许中间有 ≤60 个非反引号/非换行字符,且收集**全部**匹配。
            _mr_all = list(_re12.finditer(
                r"run_id[^`\n]{0,60}`([0-9a-f]{8,})`", _txt))
            _mr = _mr_all[0] if _mr_all else None
            # ★★ v2.25 P0：原判据 =「文档 banner run_id == report.json run_id」。
            #   但 run_id = sha256(seed|时间戳|脚本哈希)，**每次运行必然不同**
            #   （框架 §5 自己声明过"run_id 每次运行必然不同"），而文档 banner 是
            #   静态的 —— 于是任何人重跑一次 verify_all（**T4 就要求这么做**）
            #   之后，G-12 的 4 项必然 FAIL，且与平台、与执行方操作**无关**。
            #   这是设计层自相矛盾：拿"每次必变"的量当一致性锚。云端执行方
            #   会把它当"档 C 真发现问题"上报，审核方也被误导。
            #   改：① 跨制品锚点改用 input_hash（= sha256(SEED|len(D1)|len(D3))，
            #          同代码同输入下稳定），并要求文档登记；
            #       ② run_id 只查**文档内部自洽**（多处声明必须相同），
            #          不再跨制品比对 —— 它本来就不是"真相锚"，只是运行标识。
            add("G-12.%s.ih" % _tag,
                "%s 登记 input_hash == report.json(稳定锚)" % _tag,
                _ih[:16], _ih[:16], bool(_ih) and (_ih[:16] in _txt))
            _rids = {m.group(1)[:16] for m in _mr_all}
            add("G-12.%s.rid" % _tag,
                "%s banner run_id == build_version.json(构建期锚)" % _tag,
                (_mr.group(1)[:16] if _mr else None), _rid_anchor[:16],
                bool(_mr and _rid_anchor.startswith(_mr.group(1)[:16])))
            add("G-12.%s.rid-all" % _tag,
                "%s 全部 run_id 声明互相一致(%d 处)" % (_tag, len(_mr_all)),
                ("不一致: " + ", ".join(sorted(_rids))) if len(_rids) > 1 else "全部一致",
                "全部一致", len(_rids) <= 1)
            add("G-12.%s.rid-cov" % _tag,
                "%s run_id 声明至少命中 1 处(防锚点失效)" % _tag,
                len(_mr_all), ">=1", len(_mr_all) >= 1)
    except Exception as _e:
        add("G-12.0", "G-12 可执行", "异常 " + str(_e)[:60], "无异常", False)

    npass = sum(1 for r in rows if r[4])
    nfail = len(rows) - npass
    # ★ v2.25：分母 = 三态之和（与 verify_all 口径一致）。
    #   这样"dev 布局 85 项全查"与"交付布局 84 项 + 1 项不适用"的**总数相同**，
    #   便于跨平台/跨布局比对；同时缺口（N/A）在汇总行里自明。
    _tot = len(rows) + len(nas)
    print("\n" + "=" * 74)
    #   原写法在"判定项被静默吞掉"时照写 "63/69 通过" —— 读者无从知道少了的
    #   11 项从未执行（第 11 条陷阱形态③）。
    print(f"  一致性守卫: {npass}/{_tot} 通过, 失败 {nfail} 项, 不适用 {len(nas)} 项")
    print("=" * 74)
    if nas:
        print("\n  [注意] 出现 N/A(第三态) —— 以下检查**未执行**,不得视为通过:")
        for _cid, _item, _reason in nas:
            print(f"         - {_cid} {_item}: {_reason}")
    if nfail:
        print("\n  !! 文档与脚本不一致 —— 按框架第 9 条原则(数字唯一来源),审核不通过。")
        print("     修复:重新生成审核包,使附录脚本与 scripts/ 逐字节一致。")
    return 0 if nfail == 0 else 1


if __name__ == "__main__":
    sys.exit(main())
```

</details>

<details><summary>report_guard.py</summary>

```python
# -*- coding: utf-8 -*-
"""
================================================================================
 审核报告一致性守卫  v1.0
================================================================================
 起因(meta-review 2.1 / 7.6):
   "审核报告要求 B 方提交原始输出,但自己却只给汇总数字。这是双标。"
   "引入'审核报告自身的一致性守卫':审核报告引用的数字必须与框架报告
    JSON 一致,否则审核不通过。"

 本守卫把这条变成机器可检。标尺 = 框架第 9 条原则:数字唯一来源为台账。
 审核报告与框架产物适用同一标准,不得豁免。

 检查:
   R-1 报告中的 主验证 N/N == report.json 的 n_pass/total
   R-2 报告中的 排版 N/N  == layout_report.json 的 checks 数
   R-3 引用的 run_id 格式合法 + 注明来源
   R-4 报告中出现的实测数值全部能在 report.json 中定位(正向扫描)
   R-5 handoff 节点数/在途数 == handoff_chain.json
   R-6 判据 YAML 条数 == criteria.yaml 实际条数
   R-7 审核包附录脚本 == scripts/ 实际脚本(委托 consistency_guard)

 运行: python scripts/report_guard.py --doc ../四角度审核报告_合集.md
 退出码: 0 = 一致, 1 = 审核报告存在无来源数字
================================================================================
"""
import os, re, sys, json, subprocess, argparse

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(HERE)
WS = os.path.dirname(ROOT)

def _latest_doc(prefix):
    """v2.25 修订: 默认 --doc 曾硬编码 '框架_独立审核包_v1.md'(旧名, zip 内不存在)
    -> 云端不传参会直接 FileNotFound。
    改为动态取"版本号最大"的日期版本化文件。
    注意: 字符串排序会把 v2.9 排到 v2.24 之后, 必须按数值版本排序。"""
    import glob as _g, re as _re
    hits = _g.glob(os.path.join(WS, prefix + "*_20*.md"))
    best, bestv = None, None
    for p in hits:
        m = _re.search(prefix + r'([0-9]+)\.([0-9]+)_', os.path.basename(p))
        if not m:
            continue
        v = (int(m.group(1)), int(m.group(2)))
        if bestv is None or v > bestv:
            best, bestv = p, v
    if best is None and hits:
        best = sorted(hits)[-1]
    return best or os.path.join(WS, prefix + "1.md")


ROWS = []


def add(cid, item, obs, exp, ok):
    ROWS.append(bool(ok))
    print(f"    [{'PASS' if ok else 'FAIL'}] {cid} {item:<36s} "
          f"obs={str(obs)[:26]:<26s} exp={exp}")


def load(p):
    try:
        return json.load(open(p, encoding="utf-8"))
    except Exception:
        return None


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--doc", default=_latest_doc("四角度审核报告_合集_v"))
    # ⚠ v2.11:R-7 委托检查框架内嵌脚本,若只能改 --doc(合集)就
    #   注入不到脚本(脚本在框架文档里)。加 --frame 后可指向变异副本。
    ap.add_argument("--frame", default=None)
    a = ap.parse_args()

    print("=" * 78)
    print("  审核报告一致性守卫 v1.0")
    print("=" * 78)
    print("  标尺:框架第 9 条原则 —— 数字唯一来源为台账。审核报告同样适用。\n")

    if not os.path.exists(a.doc):
        print(f"  [FAIL] 审核报告不存在: {a.doc}")
        return 1
    s = open(a.doc, encoding="utf-8").read()

    R = load(os.path.join(ROOT, "results", "report.json"))
    L = load(os.path.join(ROOT, "results", "layout_report.json"))
    C = load(os.path.join(WS, "handoff", "handoff_chain.json"))
    Y = open(os.path.join(ROOT, "schema", "criteria.yaml"), encoding="utf-8").read()

    # ---------- R-1 ----------
    print("  -- R-1 主验证通过数必须来自 report.json")
    if not R:
        add("R-1", "report.json 存在", False, "True", False)
    else:
        sm = R["summary"]
        np_, nt, nbc = sm["n_pass"], sm["total"], sm.get("branches")
        branch_claims = re.findall(r"(\d+)\s*/\s*(\d+)\s*分支", s)
        body = re.sub(r"(\d+)\s*/\s*(\d+)\s*分支", " ", s)
        keep = [l for l in body.split("\n")
                if not any(k in l for k in (
                    "声明稿", "声明项", "覆盖",
                    "守卫", "handoff", "Handoff", "链守卫",
                    "绕过测试", "目检", "回归测试", "补审", "机器可检"))]
        body = "\n".join(keep)
        claims = re.findall(r"\*{0,2}(\d+)\s*/\s*(\d+)\*{0,2}\s*(?:通过|项)", body)
        bad = [f"{x}/{y}" for x, y in claims
               if not (x == str(np_) and y == str(nt))]
        if L:
            ln = len(L["checks"])
            bad = [b for b in bad if b != f"{ln}/{ln}"]
        badb = [f"{x}/{y}分支" for x, y in branch_claims if y != str(nbc)]
        add("R-1", f"主验证 N/N == {np_}/{nt} 且分支 == {nbc}",
            (bad + badb) or "全部一致", f"{np_}/{nt}, {nbc} 分支",
            not (bad or badb))

    # ---------- R-2 ----------
    print("\n  -- R-2 排版通过数必须来自 layout_report.json")
    if L:
        ln = len(L["checks"])
        nps = sum(1 for c in L["checks"] if c.get("passed"))
        pat = re.compile(r"\*{0,2}(\d+)\s*/\s*(\d+)\*{0,2}\s*通过")
        claims = {f"{x}/{y}" for x, y in pat.findall(s)}
        ok = (f"{nps}/{ln}" in claims) or (f"{ln}/{ln}" in claims) or ln == 0
        add("R-2", f"报告含排版 {nps}/{ln}",
            f"{nps}/{ln}" if ok else f"仅见 {sorted(claims)[:4]}",
            f"{nps}/{ln}", ok)

    # ---------- R-3 ----------
    print("\n  -- R-3 run_id 可追溯")
    if R:
        # ★ v2.25：原正则 `([0-9a-f]{16})` 抓**任何** 16 位十六进制反引号串 ——
        #   报告里 16 位的 input_hash 也算，于是"把 run_id 换成 zzzz"这类违规
        #   仍能靠 input_hash 蒙过（变异用例 rc=0 未抓到 → 该判据实为弱规）。
        #   收紧为 run_id 语境匹配，与 G-12 同口径。
        ids = re.findall(r"run_id[^`\n]{0,60}`([0-9a-f]{16})`", s)
        add("R-3", "引用了 run_id", f"{len(ids)} 个", ">=1 个", len(ids) >= 1)
        add("R-3b", "注明 run_id 来源为 report.json",
            "report.json" in s, "True", "report.json" in s)

    # ---------- R-4 正向扫描 ----------
    # v2.2 修订:作图规范落盘后,报告中出现 0.035 / 0.503 / 15.21 等数值,
    # 它们来自 figure_kit 实测但**不在 report.json**(属排版/作图链路)。
    # 故可回溯源扩展为:report.json(主验证实测) + criteria.yaml(判据常量)
    #   + figure_spec.yaml / 作图规范_v1.0.md(作图实测与配置常量)。
    # 原则是**可回溯**,不是"必须在 report.json"—— 与 v1.9 的放宽同一逻辑,
    # 但每加一个源都必须有明确归属,不得变成放行清单。
    print("\n  -- R-4 报告中出现的实测数值必须能回溯到台账/判据/作图规范")
    if R:
        blob = json.dumps(R, ensure_ascii=False)
        for _extra in (
                os.path.join(ROOT, "schema", "criteria.yaml"),
                os.path.join(ROOT, "spec", "figure_spec.yaml"),
                os.path.join(WS, "作图规范_v1.0.md"),
        ):
            if os.path.isfile(_extra):
                blob += open(_extra, encoding="utf-8").read()
        nums = set(re.findall(r"(?<!\w)(\d+\.\d{2,4})(?!\w)", s))
        skip = {"1.00", "2.00", "10.0", "0.001"}
        # 依赖版本号(numpy>=1.24 等):上下文含比较符或包名
        ver_ctx = set(re.findall(
            r"(?:numpy|scipy|sklearn|pandas|matplotlib|pypdf|python)"
            r"\s*[<>=~]+\s*(\d+\.\d+)", s))
        # 过程记录数字:报告中显式标注为"修正前/第一版/差异"的
        proc = set(re.findall(r"(?:修正前|第一版|差异)\s*[（(]?(\d+\.\d+)", s))
        skip = skip | ver_ctx | proc
        checked = sorted(n for n in nums if n not in skip)
        orphan = [n for n in checked if n not in blob]
        add("R-4", f"扫描 {len(checked)} 个实测值",
            orphan or "全部可回溯", "无孤立数字", not orphan)
        if orphan:
            print(f"       孤立数字(报告中但不在 JSON): {orphan[:12]}")

    # ---------- R-8 守卫 N/N 声称必须可回溯 ----------
    # 起因:v1.8 审计发现四角度报告 §0.2 写"一致性守卫 14/14",实际已是 32/32。
    # 根因:R-1 只查主验证、R-2 只查排版,不查其他守卫计数 —— 该错误数字存活多轮。
    print("\n  -- R-8 报告中的守卫 N/N 必须来自 guard_results.json")
    _gp = os.path.join(ROOT, "results", "guard_results.json")
    if os.path.exists(_gp):
        _GD = json.load(open(_gp, encoding="utf-8"))
        _legit = set()
        # R-8 自指:report_guard 最终计数在 R-8 运行时尚未产生。
        # guard_results 记录"截至上次运行的态",可能是 (n-1)/n 或 n/n。
        # 两者均须接受 —— 否则"报告写 10/10 而合法集只有 9/10"造成永久失败循环。
        # (v1.9 曾靠"上次恰好是终态"侥幸通过,非机制保障。)
        for _k2, _v2 in _GD.items():
            _legit.add(_v2)
            _bb = _v2.split("/")[-1]
            if _bb.isdigit():
                _legit.add("{}/{}".format(int(_bb) - 1, _bb))   # 当前态 (n-1)/n
                _legit.add("{}/{}".format(_bb, _bb))            # 终态 n/n
        if R:
            _legit.add("{}/{}".format(R["summary"]["n_pass"], R["summary"]["total"]))
            # 子测试计数(如隔离守卫绕过测试 7/7)亦合法 —— 能回溯至台账即可
            for _x, _y in re.findall(r"(\d+)\s*/\s*(\d+)",
                                     json.dumps(R, ensure_ascii=False)):
                if _x == _y:
                    _legit.add("{}/{}".format(_x, _y))
        if L:
            _nps = sum(1 for c in L["checks"] if c.get("passed"))
            _legit.add("{}/{}".format(_nps, len(L["checks"])))
        # 排除显式历史语境(同 R-1 处理)
        _hist = set()
        for _m2 in re.finditer(r"(?:\u4e0a\u4e00\u7248|\u5386\u53f2|\u65e7\u7248|\u6b64\u524d|\u65e9\u671f|\u5efa\u7acb\u524d|\u524d\u4e3a|\u66fe\u4e3a)"
                               r"(?:(?!\d+\s*/\s*\d+)[^\n]){0,60}?(\d+)\s*/\s*(\d+)", s):
            _hist.add("{}/{}".format(_m2.group(1), _m2.group(2)))
        for _m3 in re.finditer(r"(\d+)\s*/\s*(\d+)"
                               r"(?:(?!\d+\s*/\s*\d+)[^\n]){0,40}?(?:\u4e0a\u4e00\u7248|\u5386\u53f2|\u65e7\u7248|\u6b64\u524d|\u5efa\u7acb\u524d|\u524d\u4e3a|\u66fe\u4e3a)", s):
            _hist.add("{}/{}".format(_m3.group(1), _m3.group(2)))
        _claims = set()
        for _m in re.finditer(r"(\d+)\s*/\s*(\d+)", s):
            _a, _b = _m.group(1), _m.group(2)
            if _a == _b and "{}/{}".format(_a, _b) not in _hist:
                _claims.add("{}/{}".format(_a, _b))
        _orphan = sorted(c for c in _claims if c not in _legit)
        print("      [说明] R-8 自指兼容: 合法集同时接受 (n-1)/n 与 n/n 双值 ——\n            守卫运行至自身时终态尚未算出,此为设计而非过期残留。")
        add("R-8", "扫描 {} 个 N/N 声称".format(len(_claims)),
            _orphan or "全部可回溯", "合法集 {}".format(sorted(_legit)), not _orphan)
        if _orphan:
            print("       无法回溯: {}".format(_orphan[:10]))
            print("       合法来源 guard_results={}".format(_GD))
    else:
        add("R-8", "guard_results.json 存在", False, "True", False)


    # ---------- R-5 ----------
    print("\n  -- R-5 handoff 链数字必须来自 handoff_chain.json")
    if C:
        nn = len(C["nodes"])
        na = sum(1 for n in C["nodes"] if n.get("ack"))
        # ⚠ v2.10:原为 `str(nn) in s` —— nn=14,而 "14" 在报告中到处都是
        #   ("14 分支"等)→ 恒真 → R-5 是弱规(与 v2.5 修的 R-6 同类)。
        #   改为上下文匹配:数字必须在"节点 / 在途"语境中。
        _h5 = bool(re.search(rf"(?<![0-9]){nn}(?![0-9])\s*节点", s))
        add("R-5", f"节点数 {nn}(上下文匹配)", nn, "节点语境中应出现", _h5)
        _pend = nn - na
        _h5b = bool(re.search(rf"(?<![0-9]){_pend}(?![0-9])[^\n]{{0,4}}在途", s)
                    or re.search(rf"在途[^\n]{{0,6}}(?<![0-9]){_pend}(?![0-9])", s))
        add("R-5b", f"在途数 {_pend}(上下文匹配)", _pend,
            "在途语境中应出现", _h5b)

    # ---------- R-6 ----------
    print("\n  -- R-6 判据 YAML 条数")
    # ⚠ v2.5 修(审计指出 R-6 是空规):
    #   ① 正则 ^  - id: 依赖缩进,safe_dump 输出为 "- id:"(无前导空格)→ 匹配 0 条;
    #   ② ny=0 时 str(0)="0",而 "0" 几乎出现在任何文档中 → 恒 True → 永远 PASS。
    #   两处叠加使 R-6 完全失效。(v2.4 已在 gen_appendix 修过同类问题,此处漏改)
    try:
        import yaml as _y6
        ny = len(_y6.safe_load(Y).get("criteria", []))
    except Exception:
        ny = len(re.findall(r"^\s*- id: (\S+)", Y, re.M))
    # 数字型判据不得用 "str(n) in s" —— n<=9 时恒真。改为带边界的数字匹配。
    # ⚠ v2.10(变异测试抓到):仅匹配"独立出现的 21"太弱 —— 合集里 "21"
    #   出现 29 次(日期、条数、其它量),改掉"判据条数"那一处仍有 28 处命中
    #   → R-6 永远 PASS,是弱规。而 G-11 已做严格的三处条数校验,
    #   R-6 若继续弱下去就是纯冗余。改为**上下文匹配**:21 必须与"判据"共现。
    # ⚠ v2.10 二修:上一版改为"上下文匹配",但仍是 **OR 语义** ——
    #   合集里判据条数声明有 2 处(§0.2 状态表 + 报告二叙述句),改掉一处
    #   另一处仍命中 → R-6 永远 PASS,变异用例根本构造不出来。
    #   OR 语义的判据在"有多处声明"时就是弱规(第 11 条)。
    #   改为 **AND 语义**:枚举全部形态,每一处都必须 == ny(与 G-11.3 同构)。
    _P6 = [r"(\d+)\s*条判据",
           r"判据[^\n|]{0,16}?\*{0,2}(\d+)\*{0,2}\s*条",
           r"判据\s*YAML[^\n]{0,12}?(\d+)"]
    _v6 = []
    for _p in _P6:
        for _m in re.finditer(_p, s):
            _v6.append(_m.group(1))
    _bad6 = [v for v in _v6 if int(v) != ny]
    add("R-6", f"YAML 判据 {ny} 条(全形态 {len(_v6)} 处均须一致)",
        ("不一致: " + ",".join(_bad6)) if _bad6 else "全部一致",
        "全部一致", (not _bad6) and len(_v6) > 0)

    # ---------- R-7 委托 ----------
    print("\n  -- R-7 审核包附录脚本一致性(委托 consistency_guard)")
    cg = os.path.join(HERE, "consistency_guard.py")
    # ⚠ v2.11 第6次"忽略 --doc":同上。R-7 委托 consistency_guard 检查
    #   框架内嵌脚本,若 --doc 指向的正是框架(变异副本)却仍用固定路径,
    #   则注入的脚本改动永远测不到 → R-7 从未被证明能抓到违规。
    _db = os.path.basename(a.doc)
    # ★ v2.25 P0：回退原为未版本化旧名（交付包内不存在）→ R-7 恒 FAIL。
    #   改为复用 consistency_guard 的统一解析器（显式 > 最新版本化 > 源文档），
    #   避免在本文件里再抄一份定位逻辑（那正是"同一逻辑两处维护"的坑）。
    sys.path.insert(0, HERE)
    import consistency_guard as _CG
    tgt = a.frame or (a.doc if ("框架" in _db or "审核包" in _db) else None) \
        or _CG.main_doc("框架_独立审核包", required=False)
    if os.path.exists(cg) and tgt and os.path.exists(tgt):
        # ★ v2.25：委托 consistency_guard 时须同口径解码（子守卫输出 UTF-8）。
        _env = dict(os.environ, PYTHONUTF8="1", PYTHONIOENCODING="utf-8")
        r = subprocess.run([sys.executable, cg, "--doc", tgt],
                           capture_output=True, encoding="utf-8",
                           errors="replace", env=_env)
        add("R-7", "审核包附录脚本 == 实际脚本", r.returncode, "0",
            r.returncode == 0)
    else:
        add("R-7", "审核包与守卫脚本存在", False, "True", False)

    npass = sum(ROWS)
    nfail = len(ROWS) - npass
    print("\n" + "=" * 78)
    print(f"  审核报告守卫: {npass}/{len(ROWS)} 通过, 失败 {nfail} 项")
    print("=" * 78)
    if nfail:
        print("\n  !! 审核报告引用了无法在台账中定位的数字 —— 按框架第 9 条原则,")
        print("     审核报告自身不通过。这正是 meta-review 2.1 指出的'双标'。")
    return 0 if nfail == 0 else 1


if __name__ == "__main__":
    sys.exit(main())
```

</details>

> `MANIFEST.json` 的 `guards` 字段**由本节的 stdout 解析得出**,不手工填写。
> 起因:v1.5 的附录 stdout 显示 12/14、7/9,而 MANIFEST 与正文写 14/14、9/9 ——
> 汇总覆盖了原始输出,正是框架第 9 条要防的错。

| 守卫 | 结果(解析自 stdout) |
|---|---|
| G.1 | **77/77** |
| G.2 | **10/10** |
| G.3 | **11/11** |

### G.5 变异测试原始输出(守卫有效性自证)

**中间产物 `results/mutation_results.json`(由 `guard_selftest.py` 落盘):**

| 项 | 值 |
|---|---|
| n_pass | **66** |
| total | **66** |
| coverage | **100%** |
| 守卫家族总数 | **21** |
| 已覆盖家族 | **47** |
| 未覆盖 | 无 |

<details><summary>逐用例结果(共 66 项)</summary>

| 用例 | 期望触发 | 是否抓到 |
|---|---|---|
| 注入违规后应触发 WP-1.1 | `WP-1.1 标题升级词` | 抓到 |
| 注入违规后应触发 WP-2.1 | `WP-2.1 摘要首句非P1` | 抓到 |
| 注入违规后应触发 WP-3.4 | `WP-3.4 缺gap` | 抓到 |
| 注入违规后应触发 WP-4.3a | `WP-4.3a 缺双侧` | 抓到 |
| 注入违规后应触发 WP-5a.1 | `WP-5a.1 显著缺台账` | 抓到 |
| 注入违规后应触发 WP-5b.1 | `WP-5b.1 图号跳号` | 抓到 |
| 注入违规后应触发 WP-5c.n | `WP-5c 图注缺n` | 抓到 |
| 注入违规后应触发 WP-5d.1 | `WP-5d.1 图题无信息词` | 抓到 |
| 注入违规后应触发 WP-6.1 | `WP-6.1 讨论首段非P1` | 抓到 |
| 注入违规后应触发 WP-7c.1a | `WP-7c.1a 用而未引` | 抓到 |
| 注入违规后应触发 WP-8.1 | `WP-8.1 模板句式` | 抓到 |
| G-1 内嵌脚本 | `G-1.1` | 抓到 |
| G-3 关键数字 | `G-3` | 抓到 |
| R-6 判据条数 | `R-6` | 抓到 |
| G-11.3 | `G-11.3` | 抓到 |
| G-12.state | `G-12.合集.rid-all` | 抓到 |
| G-5.DEPR | `G-5.DEPR.≥10` | 抓到 |
| G-5.pypdf | `G-5.pypdf.XFA` | 抓到 |
| G-2.2 过期N/N | `G-2.2` | 抓到 |
| R-1 主验证 | `R-1` | 抓到 |
| R-8 守卫计数 | `R-8` | 抓到 |
| G-8 表格列数 | `G-8.2` | 抓到 |
| G-11 附录条数 | `G-11.1` | 抓到 |
| G-12 banner版本 | `G-12.框架.ver` | 抓到 |
| G-12 banner run_id | `G-12.框架.rid` | 抓到 |
| G-10.1 版本化一致 | `G-10.1` | 抓到 |
| G-4 排版声称 | `G-4.2` | 抓到 |
| G-6 变更清单 | `G-6.缩进` | 抓到 |
| R-2 排版N/N | `R-2` | 抓到 |
| R-3 run_id | `R-3` | 抓到 |
| R-4 孤立数字 | `R-4` | 抓到 |
| R-5 handoff节点 | `R-5` | 抓到 |
| R-7 附录脚本 | `R-7` | 抓到 |
| 应全 PASS | `WP-9 三格式(合规)` | 抓到 |
| 应报 FAIL | `WP-9 三格式(占位符不一致)` | 抓到 |
| G-7 图命名(fs变异) | `G-7.2` | 抓到 |
| 变异临时图文件已清理 | `G-7 残留检测` | 抓到 |
| G-14 过期副本 | `G-14.1` | 抓到 |
| 变异临时副本已清理 | `G-14 残留检测` | 抓到 |
| G-13 硬编码主文档 | `G-13.*` | 抓到 |
| 守卫源码已还原 | `G-13 源码恢复` | 抓到 |
| T26 应抓到:随机噪声冒充真实嵌入 → 应判 fail | `T26 变异` | 抓到 |
| T26 应抓到:弱信噪比嵌入 → 应降级(判 fail) | `T26 变异` | 抓到 |
| T27 应抓到:零样本迁移却在目标上调参 → 应判 fail | `T27 变异` | 抓到 |
| T27 应抓到:从头训练却声称跨数据集复现 → 应判 fail | `T27 变异` | 抓到 |
| T27 应抓到:微调但 n_calib=8(<20) → 应判 fail | `T27 变异` | 抓到 |
| T28 应抓到:假阴性对照(同模块) → 应判 fail | `T28 变异` | 抓到 |
| T28 应抓到:虚拟KO写'功能验证' → 应判 fail | `T28 变异` | 抓到 |
| T29 应抓到:声称代表充分却无子群样本量 → 应判 fail | `T29 变异` | 抓到 |
| T30 应抓到:声称匿名化却无方法 + GDPR 无法条 → 应判 fail | `T30 变异` | 抓到 |
| T03 应抓到:声明全基因组却只校正 500 个 → 应判 fail | `T03 变异` | 抓到 |
| T09 应抓到:用样本内概率做校准 → 应判 fail | `T09 变异` | 抓到 |
| T12 应抓到:样本内 1.000/样本外 0.544 → 应判 warn | `T12 变异` | 抓到 |
| T18 应抓到:预注册 4 个仅报 2 个 → 应判 fail | `T18 变异` | 抓到 |
| T36 应抓到:只报全组件性能无消融 → 应判 fail | `T36 变异` | 抓到 |
| T36 应抓到:全部组件无增量却声称整合有用 → 应判 fail | `T36 变异` | 抓到 |
| T33 应抓到:5/10000 稀有比例声称变化(CI 跨 5.48 倍)→ 应判 fail | `T33 变异` | 抓到 |
| T33 应抓到:2年数据估20年一遇(1/n 无法解析)→ 应判 fail | `T33 变异` | 抓到 |
| T34 应抓到:只报平均性能却声称精确预测 → 应判 fail | `T34 变异` | 抓到 |
| T34 应抓到:不确定性无信息却声称个体风险 → 应判 fail | `T34 变异` | 抓到 |
| T34 应抓到:corr 高却未分层报告 → 应判 fail | `T34 变异` | 抓到 |
| T35 应抓到:MTAG 循环论证 → 应判 fail | `T35 变异` | 抓到 |
| T35 应抓到:LDSC intercept=1.2 未校正 → 应判 fail | `T35 变异` | 抓到 |
| V4MODAL 应抓到:in silico+in vitro 声称治疗有效 → 应判 fail | `V4MODAL 变异` | 抓到 |
| V4MODAL 应抓到:仅 in silico 声称临床效用 → 应判 fail | `V4MODAL 变异` | 抓到 |
| MUST_COVER 声明的守卫均有变异用例 | `G-COV` | 抓到 |

</details>

**覆盖率粒度澄清** —— 「100%」指**守卫家族级**:每个家族至少有 1 个
变异用例证明其能抓到违规;**不代表**该家族每个子检查都有独立用例。

| 守卫家族 | 变异用例数 |
|---|---|
| G-10.1 | 1 |
| G-11 | 1 |
| G-11.3 | 1 |
| G-12 | 2 |
| G-2.2 | 1 |
| G-3 | 1 |
| G-4 | 1 |
| G-5.pypdf | 1 |
| G-6 | 1 |
| G-8 | 1 |
| R-1 | 1 |
| R-2 | 1 |
| R-3 | 1 |
| R-4 | 1 |
| R-5 | 1 |
| R-6 | 1 |
| R-8 | 1 |

> 逐家族详情见 `scripts/guard_selftest.py` 的 `CASES` 与 `MUST_COVER`;
> **新增守卫必须登记进 `MUST_COVER`**,缺用例则 `G-COV` FAIL。

## H. 术语定义表(防同名/近名误读)

> 起因:审计方曾把「组0高估量 0.285」误读为「CIF 组间差」——
> 二者数值巧合接近(0.285 vs 0.284),且指向同一组原始数据的不同导出量。

| 符号 | 定义 | 本次实测 | 易混淆对象 |
|---|---|---|---|
| `diff_km` | KM 法(忽略竞争)的**组间差** CIF₁−CIF₀ | 0.135 | 与 overestimate_g0 数值接近,**不是同一个量** |
| `diff_correct` | CIF 法(正确处理竞争)的**组间差** | 0.284 | 同上 |
| `overestimate_g0` | 组0 的 KM 相对 CIF 的**高估量** | +0.285 | **不是**组间差 |
| `overestimate_g1` | 组1 的 KM 相对 CIF 的**高估量** | +0.137 | **不是**组间差 |
| `distortion` | 扭曲量 = |diff_km − diff_correct| | 0.149 | 衡量忽略竞争的净后果 |
| `drift_pct` | 程序定义漂移 = |OR₂−OR₁|/OR₁ | 33.0% | 种子依赖,非固定值 |
| `flip_rate` | 换成员集后**方向翻转**的多种子发生率 | 5/12 | **概率,非必然**;禁写'且翻转' |
| `inflation` | 泄露导致的 AUC 虚高 = AUC_leak − AUC_clean | +0.112 | '虚高'是差值,不是 AUC 本身 |
| `I2` | 终点合并异质性 | 96.6% | k=2 时不稳定,**须配辅助判据** |
| `p_cell / p_donor` | 伪重复:细胞级 vs 患者级检验 p 值 | 4.2e-14 / 0.28 | 两者不可互换 |

> **规则**:报告中引用任一数字,必须同时写出其符号(如 `diff_correct=0.284`),
> 禁止只写裸露数值。术语表由本脚本生成,与 `report.json` 字段一一对应。
