# 云端服务器执行提示词 v1.0

> **给谁用**：一台干净的 Linux 云端服务器上的 agent（下称"执行方"）。
> **自包含性**：执行方**不需要任何框架背景知识**。本文档含全部命令、路径、判定标准、返回格式。
> **配套**：`框架交叉验证包_v2.25_<日期>.zip`（文件清单见包内 `ZIP_CONTENTS.md`，
> 逐文件列出路径 / 字节 / sha256）。
> ⚠ 原此处写"77 个文件"（旧值），已改为**不写死数字** —— 以包内清单为准。
> **一句话目标**：**照镜子，不是修东西**。你负责把 Linux 云端跑出来的原始输出原样带回来。

---

## §0 AIM（目标）

### 0.1 要回答的问题

在一台**不属于开发环境**的 Linux 云端服务器上，用同一份代码、同一份数据、同一套判据，跑出一套完整结果，与开发侧基线比对。

具体三问：

| # | 问题 | 由哪份输出回答 |
|---|---|---|
| Q1 | 同一份代码在云端 Linux 上**能否跑通**？ | `verify_all.py` 的退出码 + stdout |
| Q2 | 结果数字与基线**是否一致**？ | `report.json` vs 基线数字 |
| Q3 | 守卫在云端**是否仍然生效**？ | `guard_selftest.py`（变异测试）stdout |

### 0.2 非目标（明确不做）

- ❌ **不修改任何代码**让它通过——改了代码，这次验证就失去意义
- ❌ 不重新生成图样本、不重跑合成数据"补齐"缺失项
- ❌ 不判断框架"好不好"——那是审核方的事
- ❌ 不优化、不重构、不顺手修 bug
- ❌ 不安装框架未声明的依赖（除非报 `ImportError`，见 GATEWAY G3）

### 0.3 一句话定位

> **你是来照镜子的。镜子里的样子可能不好看，但那正是要看的东西。**

---

## §1 TERM（术语表）

以下术语在本文档中固定含义，请严格按此理解：

| 术语 | 含义 |
|---|---|
| **基线** | 开发侧（Linux 沙箱）已跑出的参考结果。`n_pass=222`、`n_fail=0`、`n_na=0`、`total=222`、`branches=21`、`n_unmapped=0` |
| **判定项** | `verify_all.py` 输出的单条 PASS/FAIL 记录。基线共 **222** 项 |
| **分支** | 判定项的逻辑分组。基线共 **21** 个 |
| **守卫** | 检查"文档/脚本/台账是否互相一致"的程序。共 4 道：`consistency_guard` / `report_guard` / `handoff_guard` / `guard_selftest` |
| **变异测试** | `guard_selftest.py`。向文档注入**已知违规**，验证守卫**抓得到**。抓不到 = 守卫是空规 |
| **N/A** | "判不了"的第三态，与 PASS/FAIL 并列。**出现 N/A 不是失败** |
| **原始输出** | 脚本打印到终端的**完整文本**（含每一行 `[PASS]` / `[FAIL]`）。**不是 JSON、不是摘要、不是你的转述** |
| **台账** | `results/` 下的 JSON 文件，是所有数字的**唯一权威来源** |

---

## §2 TERM-TASKLIST（任务清单）

按顺序执行，共 **9 个任务**。全部完成才算跑完。

| # | 任务 | 命令（见 §3） | 产出 |
|---|---|---|---|
| T1 | 环境准备与目录建立 | — | `~/cvrun/` 结构 |
| T2 | 解压与完整性核对 | `unzip` | `pkg/` + 核对结果 |
| T3 | 依赖安装 | `pip install -r requirements.txt` | venv |
| T4 | 主验证 | `verify_all.py` | `report.json` + stdout |
| T5 | 排版检查 | `layout_check.py` | `layout_report.json` + stdout |
| T6 | 判据消融 | `ablation.py` | stdout（**JSON 到终端，不落盘**） |
| T7 | 变异测试 | `guard_selftest.py` | `mutation_results.json` + stdout |
| T8 | 三道守卫 + 环境快照 | 4 条命令 | `guard_results.json` + `platform_snapshot.json` |
| T9 | 单元测试 | `pytest` | `pytest_results.json` + stdout |
| **T10** | **写作流程守卫** | `writing_guard.py` | `writing_report.json` + stdout |
| **T11** | **三格式导出守卫** | `export_guard.py` | stdout（rc 即结论） |

> **T6 不可跳过**。它是 T37 判据（判据体系健康度）的**唯一出口**，跳过会导致该判据在云端永远缺失。

> **T10 不可跳过**。写作层 46 项判据（WP-1~9 + NC-1~7）**不在** `verify_all.py` 的 222 项里，
> 只有 `writing_guard.py` 会跑它们。跳过则写作层在云端**完全未验证**。

---

## §3 TERM-CHECKPOINT（执行步骤，含检查点）

### 前置约定

```bash
# 所有路径以此为根（可改，但改了要全程一致，并在返回时说明）
export CVROOT=~/cvrun
mkdir -p $CVROOT/pkg $CVROOT/out/copy
```

**目录结构**（执行完毕后应长这样）：

```
~/cvrun/
├─ pkg/                              # 解压目录
│   ├─ 框架_独立审核包_v<版本>_<日期>.md
│   ├─ 四角度审核报告_合集_v<版本>_<日期>.md
│   ├─ 附录_原始输出_v<版本>_<日期>.md
│   ├─ 框架_完整交付_v<版本>_<日期>.md
│   ├─ MANIFEST.json                 # ← 版本/基线/sha256
│   ├─ ZIP_CONTENTS.md               # ← 全部文件清单（逐项 字节 + sha256）
│   ├─ 跨平台执行指导.md
│   ├─ INVARIANTS.md (在 crossval/)
│   ├─ crossval/
│   │   ├─ README.md                 # 入门口径（v2.26 已与基线同步）
│   │   ├─ requirements.txt
│   │   ├─ scripts/                  # 25 个 .py（含 run_units.py 单元执行器）
│   │   ├─ schema/criteria.yaml
│   │   ├─ spec/                     # figure_spec.yaml / writing_spec.yaml
│   │   ├─ figures/{demo,samples}/
│   │   ├─ demo/
│   │   ├─ tests/
│   │   └─ results/                  # 输出落在这里
│   └─ handoff/
│       ├─ handoff_chain.json
│       └─ scripts/handoff_guard.py
└─ out/                              # ★ 你的产出集中放这里
    ├─ 01_verify_stdout.txt
    ├─ 02_layout_stdout.txt
    ├─ 03_ablation_stdout.json
    ├─ 04_mutation_stdout.txt
    ├─ 05_consistency_stdout.txt
    ├─ 06_report_guard_stdout.txt
    ├─ 07_platform_snapshot.json
    ├─ 08_pytest_stdout.txt
    ├─ 09_handoff_stdout.txt
    ├─ 10_writing_stdout.txt
    ├─ 11_export_stdout.txt
    └─ copy/                         # ★ 本地 agent 从这里读
        ├─ report.json
        ├─ layout_report.json
        ├─ mutation_results.json
        ├─ guard_results.json
        ├─ platform_snapshot.json
        └─ pytest_results.json
```

---

### T1 · 环境准备

```bash
python3 --version        # 需要 ≥ 3.9
which unzip || (apt-get update && apt-get install -y unzip)   # 或 yum/dnf
```

**检查点 CP-1**：`python3 --version` 有输出且 ≥3.9。不满足 → **STOP**，报告实际版本。

---

### T2 · 解压与核对

```bash
cd $CVROOT/pkg
unzip -q 框架交叉验证包_v2.25_<日期>.zip -d $CVROOT/pkg
# 若上传的是别的名字，替换上面文件名即可

ls -la | head -20
echo "--- 文件总数应为 101 ---"
find . -type f | wc -l
```

**检查点 CP-2**：以下 5 项**必须存在**，缺任一 → **STOP**。
注意：三个交付文档是**版本化文件名**（`_v<版本>_<日期>.md`），
**以包内 `ZIP_CONTENTS.md` / `MANIFEST.json` 的 `files` 字段为准，不要猜**：

```
MANIFEST.json
ZIP_CONTENTS.md
框架_独立审核包_v<版本>_<日期>.md
四角度审核报告_合集_v<版本>_<日期>.md
crossval/scripts/verify_all.py
```

```bash
# ★ 用通配符匹配版本化名，避免把版本号写死（v2.24 曾因此写死旧名）
for f in MANIFEST.json ZIP_CONTENTS.md crossval/scripts/verify_all.py \
         框架_独立审核包_v*.md 四角度审核报告_合集_v*.md; do
  [ -e "$f" ] && echo "OK   $f" || echo "MISS $f"
done
```

> ★ **v2.25 补充**：`ZIP_CONTENTS.md` 此前**实际不存在于包内**（v24 变更清单声称
> 已生成，但 `build.py` 里没有那一步），于是按本条 CP-2 的执行方**第一步就 STOP**。
> v2.25 已由构建代码生成，并新增后置校验 P-8 确保它随包交付。

> ★ **v2.26 补充**：`crossval/README.md` 此前长期停在 v1.x 口径（33/33、8 个分支、
> scripts/ 只列 1 个脚本），而它正是执行方 `cd crossval` 后**第一眼读到**的文档。
> 本文件原提醒"那里的数字过时，不要用以判定"。v2.26 已直接把它改对，
> 该提醒随之作废 —— 与其让执行方"忽略一份包内文档"，不如让那份文档正确。

---

### T3 · 依赖安装

```bash
cd $CVROOT/pkg/crossval
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip -q
pip install -r requirements.txt
```

`requirements.txt` 是依赖的**唯一来源**（v2.26 起）。请**不要**在别处另抄一份清单。
以包内实际文件为准，不要沿用本文档的转述 —— 本文档曾长期转述一份**过期且不全**的清单。

> ★ **v2.26 修正（重要，此前两次"修了却没修到实处"）**
>
> 原清单只有 6 个包（`numpy/scipy/scikit-learn/matplotlib/pandas/pypdf`），
> **缺 `pyyaml` 与 `pytest`** —— 而 §T6（ablation）、§T7（guard_selftest）、
> §T8（consistency_guard / layout_check / writing_guard）都 `import yaml`，§T9 需要 `pytest`。
> 按原清单 `pip install -r requirements.txt` 后必然**连续 ImportError → 触发 G3 网关 STOP**。
>
> v2.25 曾在本文件里"补上"这两个包 —— **但只改了文档，没改 `crossval/requirements.txt`**，
> 于是执行方按文件装仍然缺包。这正是本框架反复记录的"声称改了 vs 实际改了"。
> v2.26 已直接修好 `crossval/requirements.txt`，并把 `跨平台执行指导.md` 的 pip 行
> 也改成 `pip install -r requirements.txt`，使**依赖清单只有一处**。
>
> 另：原三处版本口径互不相同（本文档转述 `>=`、执行指导钉 `==2.2.6` 一套、
> 实测环境又是 `2.5.0` 一套）。现统一为 requirements.txt 中**跑出基线的那一套**（`==` 硬钉），
> 因为跨平台验证的前提是"同代码同输入"，版本漂移本身就是噪声。
>
> 若某版本在你的源/平台上不可用：**不要中断**，装最接近的可用版本，
> 并**在回报时写明实际版本号**（版本差异本身就是要观察的对象）。

**另请设置编码环境变量**（Windows 上必做；Linux 建议同设以求同口径）：

```bash
# Windows PowerShell
$env:PYTHONUTF8 = '1'; $env:PYTHONIOENCODING = 'utf-8'
# Linux
export PYTHONUTF8=1;  export PYTHONIOENCODING=utf-8
```

> 理由见 `跨平台执行指导.md` §3.1：中文 Windows(cp936) 下脚本输出的 `I²`/`✓`/`✗`
> 无法编码，会在接近终点处 `UnicodeEncodeError` 崩溃。

**检查点 CP-3**：`pip install` 退出码 0。若某包装不上 → **不要中断**，记录包名与错误，继续下一步（见 GATEWAY G3）。

**中文字体（可选）**：目检样本需要中文字体。云端通常没有 `WenQuanYi Micro Hei`，脚本会自动 fallback 到 DejaVu。**这不是错误**。若想装：

```bash
apt-get install -y fonts-wqy-microhei && rm -rf ~/.cache/matplotlib
```

---

### T4 · 主验证（核心）

```bash
cd $CVROOT/pkg/crossval
source .venv/bin/activate     # 若已激活可跳过

python3 scripts/verify_all.py 2>&1 | tee $CVROOT/out/01_verify_stdout.txt
echo "EXIT=${PIPESTATUS[0]}"
cp results/report.json      $CVROOT/out/copy/report.json
cp results/report.md        $CVROOT/out/copy/ 2>/dev/null
```

**耗时**：约 25 秒（基线）。若超过 5 分钟仍无输出，记录后中断并报告。

**检查点 CP-4**：stdout 末尾应出现汇总行，形如：

```
判定项 222/222 通过, 失败 0 项, 覆盖分支 21
```

**记录这四个数**：`n_pass` / `n_fail` / `total` / `branches`。

---

### T5 · 排版检查

```bash
python3 scripts/layout_check.py --figdir figures/demo 2>&1 | tee $CVROOT/out/02_layout_stdout.txt
cp results/layout_report.json $CVROOT/out/copy/
```

**检查点 CP-5**：基线条数 **70**。若你的数字不同 → 记录差异（见 §5 判定三档）。

---

### T6 · 判据消融（**不可跳过**）

```bash
python3 scripts/ablation.py 2>&1 | tee $CVROOT/out/03_ablation_stdout.json
```

> ⚠ **注意**：`ablation.py` **只输出到 stdout，不生成任何文件**。所以必须用 `tee` 保存。
> 输出是 JSON（含 A-1~A-7 与 C-1~C-4 用例结果）。

**检查点 CP-6**：输出中应含 `A-1` ~ `A-7`、`C-1` ~ `C-4` 共 11 个用例 ID，且全部 PASS。

自测（可选，验证消融器自身）：

```bash
python3 scripts/ablation.py --self-test 2>&1 | tail -20
```

---

### T7 · 变异测试（守卫有效性）

```bash
python3 scripts/guard_selftest.py 2>&1 | tee $CVROOT/out/04_mutation_stdout.txt
cp results/mutation_results.json $CVROOT/out/copy/
```

**检查点 CP-7**：基线 **66/66 通过，覆盖率 100%**。

> ⚠ **v2.26：本脚本的退出码有三态，必须先看码再读结论**
>
> | 码 | 含义 | 处置 |
> |---|---|---|
> | 0 | 全部守卫均证明过自己能抓到违规 | 正常，进 T8 |
> | 1 | 存在**空规** | 高价值发现，如实回报 |
> | **2** | **环境错误** —— 子守卫没跑起来，本轮结果无效 | **先修环境，不要当空规上报** |
>
> 若看到退出码 2：说明子守卫进程根本没产出任何判定行（解释器 / 依赖 / 路径错误），
> 此时脚本会打印"已发生的用例"清单。**它不是空规**。
> v2.26 之前，`python3` 被硬编码为子守卫解释器，而 Windows 的 venv 不生成
> `python3.exe` → 落到另一个没装依赖的解释器 → **18 个守卫家族被误报为"空规"**。
> 若你仍观察到这种现象，请连同 `python -c "import yaml,numpy"` 的结果一起回报。

> **这是本次验证最重要的一项**。它回答"守卫是不是空规"。
> 若这里出现**退出码 1** 的 FAIL，说明某个守卫在云端**抓不到它本该抓到的违规**——这是高价值发现。

---

### T8 · 三道守卫 + 环境快照

```bash
# 8a 一致性守卫（框架正文 vs 脚本/台账）
python3 scripts/consistency_guard.py \
    --doc ../框架_独立审核包_v<版本>_<日期>.md 2>&1 \
    | tee $CVROOT/out/05_consistency_stdout.txt

# 8b 审核报告守卫（四角度合集 vs 台账）
python3 scripts/report_guard.py \
    --doc ../四角度审核报告_合集_v<版本>_<日期>.md 2>&1 \
    | tee $CVROOT/out/06_report_guard_stdout.txt

# 8c 环境快照（用于解释跨平台差异）
python3 scripts/platform_check.py 2>&1 | tail -5
cp results/platform_snapshot.json $CVROOT/out/copy/ 2>/dev/null

# 8d handoff 链守卫
python3 ../handoff/scripts/handoff_guard.py 2>&1 \
    | tee $CVROOT/out/09_handoff_stdout.txt

# 8e 汇总守卫计数
cp results/guard_results.json $CVROOT/out/copy/
```

**检查点 CP-8**：基线 `consistency_guard` **76/77 通过 + 1 项不适用**、
`report_guard` 10/10、`handoff_guard` 11/11。

> ★ **v2.25 基准变更说明（必读，否则你会误判为"真发现问题"）**
>
> | 布局 | 一致性守卫结果 | 为什么 | 
> |---|---|---|
> | 开发机（源文档 + 版本化副本并存） | **77/77 通过, 不适用 0** | G-10.1 可执行 |
> | **交付包（只有版本化副本）** | **76/77 通过, 不适用 1** | G-10.1「版本化副本 vs 源文档」**不适用** |
>
> 两者**总数都是 77**，差额恰为 1 项 N/A，且 stdout 会**明确列出**是哪一项、
> 以及为什么。**"不适用"不是失败，也不算通过** —— 它表示该项没有执行。
> 附录 §G.1 记录的 77/77 来自开发布局，与你在交付布局看到的 76/77 不矛盾。
>
> ⚠ v2.24 及以前，交付布局下这个守卫会直接**崩掉一整块检查**
> （判定项从 80 静默降到 69，汇总照写「63/69 通过」），现已修复。

> **必须显式传 `--doc`**，路径指**版本化文件名**（`_v<版本>_<日期>`），不是裸 `v1.md`。

---

### T9 · 单元测试

```bash
python3 -m pytest tests/ -q 2>&1 | tee $CVROOT/out/08_pytest_stdout.txt
cp results/pytest_results.json $CVROOT/out/copy/ 2>/dev/null
```

**检查点 CP-9**：基线 **37 项通过**。

---

### T10 · 写作流程守卫（★ 独立模块，不在主验证内）

```bash
# ① 先从稿件提取真实 P1（核心发现一句话），不得凭空编造
#    稿件中形如「P1：……」或摘要首句 / 讨论首段首句
P1="<把提取到的 P1 原文粘贴在此>"

python3 scripts/writing_guard.py \
  --doc demo/manuscript_fixed.md \
  --p1 "$P1" \
  --skill demo/research_survey_skill_demo.json \
  2>&1 | tee $CVROOT/out/10_writing_stdout.txt
cp results/writing_report.json $CVROOT/out/copy/ 2>/dev/null
```

**检查点 CP-10**：基线 **43 项通过，不适用 0**（`n_pass=43, n_na=0`）。

> **⚠ `--p1` 必须传真实 P1，`--skill` 必须提供，且必须报告 `n_na`。**
> 实测三种误用都会产生**假绿或假红**（下表为 v2.25 在交付布局上的实测值）：
>
> | 输入 | 结果 | 为什么是假的 |
> |---|---|---|
> | 不传 `--p1`（给 `--skill`） | `40/43 通过, 不适用 3` | WP-CORE / WP-2.1 / WP-6.1 三项**根本没检查** |
> | 不传 `--p1` 且不给 `--skill` | `38/42 通过, 失败 1, 不适用 3` | 上条 + WP-3.1 FAIL（v2.25 起不再静默 N/A） |
> | 传一个编造的 P1 | `41/43 通过, 失败 2` | WP-2.1 / WP-6.1 两项假失败，与稿件真实质量无关 |
>
> 这与框架反复出现的"汇总掩盖原始输出"同族：**不提供输入 → 检查自动通过**。
> 你报告时必须同时给出 `n_pass` 与 `n_na`，只报通过数视为不完整。

> ★ **v2.25 修正（重要）**：`--skill` 未提供时，WP-3.1 现在判 **FAIL**，不再判 N/A。
> 原因：原实现无法区分「调用了但返 0 篇」与「压根没调用」——两者都走 N/A，
> 而 **N/A 不算失败** → 一个从不调用检索 skill 的执行方永久拿到 N/A（默认放行）。
> 这与本框架自己批判的「不提供输入 → 检查自动通过」同构，故已硬化。
>
> 包内已提供 demo 夹具 `crossval/demo/research_survey_skill_demo.json`，可直接使用：
>
> ```bash
> python3 scripts/writing_guard.py \
>   --doc demo/manuscript_fixed.md --p1 "$P1" \
>   --skill demo/research_survey_skill_demo.json
> ```
>
> 想验证该判据**不是空规**（先红后绿）：去掉 `--skill` 再跑一次 → 应出现
> `[FAIL] WP-3.1`。这正是"守卫能抓到违规"的唯一证据形式。

---

### T11 · 三格式导出守卫

```bash
python3 scripts/export_guard.py \
  --a demo/export/docA.txt \
  --b demo/export/docB.txt \
  --p demo/export/docP.txt \
  2>&1 | tee $CVROOT/out/11_export_stdout.txt
echo "rc=$?"
```

**检查点 CP-11**：基线 **10/10 通过，rc=0**（实测值，勿信旧文档里的 8/8）。

---

## §4 TERM-GATEWAY（网关，不通过则 STOP）

以下任一命中，**立即停止并报告**，不要自行处置：

| # | 网关 | 触发条件 | 动作 |
|---|---|---|---|
| **G1** | Python 版本 | `< 3.9` | STOP，报告实际版本 |
| **G2** | 解压完整性 | §3 CP-2 五个文件缺任一 | STOP，列出缺失文件 |
| **G3** | 依赖失败 | `pip install` 失败**且**后续报 `ImportError` | STOP，报告缺失包 |
| **G4** | 主验证崩溃 | `verify_all.py` 抛异常（非 FAIL） | STOP，贴完整 traceback |
| **G5** | 结果目录不可写 | `results/` 无法创建文件 | STOP |

> **区分**：单个判定项 `[FAIL]` **不是** G4。G4 只指**程序崩溃**。
> 判定项 FAIL 是有效结果，继续跑完。

---

## §5 RECHECK-POINT（复查点与判定三档）

拿到结果后，按这三档判定。**三档，不是二值**。

### 档 A · 通过

| 条件 |
|---|
| `n_pass=222`、`n_fail=0`、`branches=21`、`n_unmapped=0` |
| 所有数值与基线差 **≤ 1e-3** |
| 守卫/变异测试/pytest 计数与基线一致 |

→ 记：**跨平台一致性成立**。

### 档 B · 通过但有差异

| 条件 |
|---|
| 计数一致（仍是 222/21） |
| 但具体数值差 **> 1e-3** |

→ 记：**需归因**。把 `platform_snapshot.json` 一起带回，重点看：
- numpy / scipy / scikit-learn 版本
- 中文字体实际选中哪个
- 时区偏移

### 档 C · 真发现问题

| 条件 | 含义 |
|---|---|
| `n_pass` / `branches` 计数**不一致** | 云端触发或跳过了某些分支 |
| 某分支 **PASS → FAIL** | **最有价值的结果** |
| 变异测试某用例 FAIL | 某个守卫在云端**是空规** |

→ **档 C 不是失败，是本次验证最有价值的产出。**
它说明开发侧的"全绿"可能依赖了特定环境特征。

### 已知正常差异（**不判为问题**）

| 项 | 说明 |
|---|---|
| 中文字体 | 云端无 WenQuanYi → fallback DejaVu。目检样本 PDF 元数据可能不同 |
| PDF 元数据 | 生成时间/字体名不同，不构成 FAIL |
| `elapsed` 耗时 | 云端机器性能不同，正常 |
| `run_id` | **每次运行必然不同**（由 seed+时间戳+脚本哈希导出）。这是预期行为 |
| `parent_run_id` | 基线下为 `null`，正常 |

---

## §6 HOOK（告警钩子）

以下情况**自动触发告警**，必须在返回时**单独列出**：

| # | 钩子 | 触发条件 | 为什么重要 |
|---|---|---|---|
| **H-ALL-PASS** | 全绿告警 | 所有项 100% PASS 且与基线完全一致 | 需警惕：可能分支未真正激活，或恰好复制了开发环境 |
| **H-GUARD-ALLPASS** | 守卫全绿 | 4 道守卫全部 PASS | 与 H-ALL-PASS 同理——**一直通过的守卫可能是空规** |
| **H-MUTATION-FAIL** | 变异失败 | `guard_selftest.py` 出现 FAIL | **最高优先级**：证明某守卫在云端抓不到违规 |
| **H-NA-APPEAR** | 出现 N/A | `n_na > 0` | 不是失败，但必须列出哪些项判了"判不了"及理由 |
| **H-NEWFAIL** | 新失败 | 某分支 PASS→FAIL | **最高价值**：记录分支号 + 实测值 |
| **H-COUNT-DIFF** | 计数漂移 | 判定项数 ≠ 222 或分支 ≠ 21 | 记录实际数字 |

> **H-ALL-PASS 特别说明**：
> 框架自身第 11 条陷阱就是"守卫一直通过 = 可能是空规"。
> 所以**全绿不等于没问题**——如果你看到全绿，请在返回中写一句：
> "确认各分支确实执行（stdout 中有对应分支的判定行），而非静默跳过。"

---

## §7 LOOP（重试策略）

| 情况 | 是否重试 | 怎么重试 |
|---|---|---|
| 依赖装不上 | 重试 1 次 | 换镜像源：`pip install -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 某命令超时 | 重试 1 次 | 用 `--quick` 跳过耗时项，并注明 |
| 判定项 FAIL | **不重试** | FAIL 是结果，不是故障 |
| 程序崩溃（G4） | 重试 1 次 | 若仍崩，STOP 并贴 traceback |
| 字体缺失警告 | **不重试** | 已知正常差异 |

**原则**：**不为了让数字变绿而重试**。固定了随机种子（20260910），理论上同代码同平台结果应完全一致；重跑只会浪费时间，且掩盖真实差异。

---

## §8 HANDOFF-TICK（交接刻度）

每完成一个任务，在 `$CVROOT/out/PROGRESS.md` 追加一行：

```bash
echo "T4 主验证 | n_pass=222 n_fail=0 branches=21 | $(date '+%F %T')" >> $CVROOT/out/PROGRESS.md
```

**已完成的任务打 ✅，跳过的写 SKIP + 理由。** 不允许静默跳过。

最终 `PROGRESS.md` 应覆盖 T1–T9 全部 9 行。

---

## §9 WORKFLOW-ALLPOINT-CHECKLIST（全流程检查清单）

返回前逐条自检：

```
[ ] T1 Python ≥3.9                        已确认
[ ] T2 五个关键文件齐全                    已确认
[ ] T3 依赖安装（或已记录失败）             已确认
[ ] T4 主验证跑完，记录 4 个数字            已确认
[ ] T5 排版检查跑完（基线 70）              已确认
[ ] T6 消融跑完（★ 易漏，且必须 tee）       已确认
[ ] T7 变异测试跑完（基线 66/66）           已确认
[ ] T8 三道守卫 + 环境快照 + handoff        已确认
[ ] T9 pytest 跑完（基线 37）               已确认
[ ] 所有 stdout 已存到 $CVROOT/out/         已确认
[ ] JSON 已复制到 $CVROOT/out/copy/         已确认
[ ] PROGRESS.md 覆盖 T1–T9                 已确认
[ ] 未修改任何框架代码                      已确认
[ ] 触发的 HOOK 已单独列出                  已确认
```

---

## §10 ENDCHECKMARK（收尾标记）

在返回内容**最开头**放这个标记块：

```
=== ENDCHECKMARK ===
platform        : Linux <发行版> <内核版本>
python          : <版本>
判定项          : <n_pass>/<total>
分支            : <branches>
失败            : <n_fail>
判不了(N/A)     : <n_na>
排版            : <实际>/70
变异测试        : <实际>/66
一致性守卫      : <实际>
报告守卫        : <实际>
handoff 链      : <实际>
pytest          : <实际>
档位            : A / B / C
code_modified   : NO
触发的 HOOK     : <列出，无则写"无">
=== END ===
```

> **`code_modified: YES` → 本次结果作废。**
> 这是整份提示词最硬的一条。改了代码就不再是"同一份代码"的验证。

---

## §11 END-CONFIRM（最终确认与返回清单）

### 11.1 必须返回的文件（按优先级）

| # | 文件 | 为什么必要 |
|---|---|---|
| 1 | `out/01_verify_stdout.txt` | **完整 stdout**，含每一行 PASS/FAIL |
| 1b | `out/10_writing_stdout.txt` | 写作层 stdout（**须含 n_pass 与 n_na 两个数**） |
| 1c | `out/11_export_stdout.txt` | 导出层 stdout + rc |
| 2 | `out/copy/report.json` | 主验证台账（数字唯一来源） |
| 3 | `out/04_mutation_stdout.txt` | 变异测试 stdout（证明守卫非空规） |
| 4 | `out/03_ablation_stdout.json` | 消融输出（T37 唯一出口） |
| 5 | `out/05_consistency_stdout.txt` | 一致性守卫 stdout |
| 6 | `out/06_report_guard_stdout.txt` | 报告守卫 stdout |
| 7 | `out/copy/platform_snapshot.json` | 环境比对 |
| 8 | `out/02_layout_stdout.txt` | 排版 stdout |
| 9 | `out/08_pytest_stdout.txt` | pytest stdout |
| 10 | `out/09_handoff_stdout.txt` | handoff 链 stdout |
| 11 | `out/PROGRESS.md` | 9 行进度 |

### 11.2 返回纪律（三条硬要求）

1. **必须贴完整 stdout，不能只给 JSON，不能给摘要。**
   > 理由：本框架曾出现"守卫实际 FAIL 但汇总写成 PASS"的事故。只看 JSON 会重蹈覆辙。

2. **每个数字标注来源文件**（如 `n_pass=222 ← report.json summary`）。

3. **不许改写、不许精简、不许"整理后呈现"。** 原样贴。

### 11.3 观察项（可选但欢迎）

回答这几个问题：

- Q1：有没有哪个分支在云端**没有执行**？（stdout 里找不到该分支的判定行）
- Q2：数值差异（若有）集中在哪类计算？（浮点 / 随机采样 / 字体度量）
- Q3：中文字体实际 fallback 到了哪个？
- Q4：有没有任何 WARNING 是基线没有的？

---

## §12 给执行方的一句话

> **不要试图让它通过。**
> 如果云端跑出 FAIL，那不是你的失误——那是这次验证唯一真正的产出。
> 如果云端跑出全绿，反而要警惕：确认各分支真的执行了，而不是被静默跳过。
