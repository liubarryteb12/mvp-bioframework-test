# 交叉验证包依赖 —— 单一来源
# ─────────────────────────────────────────────────────────────────────
# ★ v2.26 修正两处：
#
# ① 缺 pyyaml / pytest。按本文件装完依赖后，下列命令会 ImportError：
#      ablation.py（T6，import yaml）
#      guard_selftest.py（T7，间接）
#      consistency_guard.py / layout_check.py / writing_guard.py（yaml）
#      pytest tests/（T9）
#    → 云端执行方会连续撞 ImportError，最终触发网关 G3 STOP。
#    与审查报告 P1-5/P1-7 是**同一缺口的第三次复发**：前两次只改了执行指导
#    的 pip 行，没改本文件；本次连执行指导里的 pip 行一并改为
#    `pip install -r requirements.txt`，杜绝"两处各写一份清单"。
#
# ② 版本口径统一。此前有三种写法且互相冲突：
#      本文件      numpy>=1.24（下限，无上界）
#      跨平台执行指导 numpy==2.2.6 等（另一套，且与实测环境不符）
#      platform_snapshot.json  numpy 2.5.0 等（**实际跑出基线的版本**）
#    现以**跑出基线的那一套**为准（下方 == 硬钉），因为跨平台验证的目的是
#    "同代码同输入是否得同结果"，版本漂移本身就是噪声源。
#
# 若某个版本在你的 pip 源/平台上不可用：**不要中断**，装上最接近的可用版本，
# 并在回报时**明确说明实际安装的版本号**（版本差异本身就是要观察的对象）。
# 回报格式见《跨平台执行指导》§5。
# ─────────────────────────────────────────────────────────────────────

# 基线实测版本（Windows 11 / CPython，见 results/platform_snapshot.json）
numpy==2.5.0
scipy==1.18.1
scikit-learn==1.9.1
matplotlib==3.11.0
pandas==3.0.5
pypdf==6.18.0

# 判据消融 / 守卫 / 排版检查用（v2.26 补入，此前缺失）
pyyaml==6.0.3

# 单元测试用（v2.26 补入，此前缺失）
pytest==9.1.1
