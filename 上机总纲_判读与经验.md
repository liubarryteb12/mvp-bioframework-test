# 上机总纲 · 判读标准与经验教训

> 版本 v2.25 · 2026-09-12
> 用途：**上机实测时的判读手册**——拿到结果后怎么判断"这是真发现还是环境噪音"
> 定位：与 `框架_完整交付_v2.25_<日期>.md`（完整框架）、`跨平台执行指导.md`（命令）配套
>
> ⚠ **v2.25 同步**：本文原为 v2.13 当轮值（101 项 / 15 分支 / 29 用例 / 1e-6 容差），
> 已按现行值更正为 **222 项 / 21 分支 / 66 用例 / 1e-3 容差**。
> 这正是本框架反复栽的"改了 A 忘改 B"——**文档分叉**。请以
> `MANIFEST.json` + `ZIP_CONTENTS.md` + 各组件的 stdout 为准，本文只作判读口径。

---

## §1 上机前 60 秒

### 1.1 环境准备

```bash
# Linux 沙箱 / WSL
cd <解压目录>/crossval
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# Windows 11 + PowerShell 7
cd <解压目录>\crossval
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

若执行策略拦截：`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`

### 1.2 三条核心命令（按序）

```bash
python3 scripts/platform_check.py      # ⓪ 环境快照（先建立基线）
python3 scripts/verify_all.py          # ① 主验证 → results/report.json
python3 scripts/guard_selftest.py      # ② 变异测试（确认守卫仍生效）
```

**第 ② 条不要省。** 它验证"守卫在**你这台机器**上仍能抓到违规"——如果它在你的环境里全 PASS 但从未 FAIL 过，说明守卫在你这没生效，后面所有 PASS 都不可信。

### 1.3 必须返回的内容

| # | 内容 | 为什么 |
|---|---|---|
| 1 | `results/report.json` **全文** | 数字唯一来源（台账） |
| 2 | `verify_all.py` **完整 stdout** | 含每行 `[PASS]/[FAIL]` |
| 3 | `guard_selftest.py` **完整 stdout** | 证明守卫在你这生效 |
| 4 | `results/platform_snapshot.json` | 环境基线，用于归因差异 |

**必须原样贴全文，不要摘要。** 摘要是本框架一直反对的东西——汇总会撒谎，原始输出不会。

---

## §2 判读标准（三档，不是二值）

拿到结果后，按下表分档。**不要只看"跑通没跑通"。**

| 档 | 判据 | 含义 | 处置 |
|---|---|---|---|
| **A 一致** | n_pass=222、branches=21、判定项数=222；数值与基线差 ≤1e-3 | 跨平台一致性成立 | 记录，进入真实数据验证 |
| **B 有差异** | 计数一致，但具体数值差 >1e-3 | 需归因（numpy/scipy/sklearn 版本差） | 用 `platform_check` 输出比对版本 |
| **C 真发现** | 某分支 PASS→FAIL，或判定项数变了 | **最有价值的结果** | 不要修它，如实记录 |

### 2.1 关于 C 档——这是你要找的东西

> **真实数据/新环境上 FAIL 不是框架的失败，是框架在真实场景下被发现不足。**

合成数据验证的是"判据能不能跑通"，真实环境验证的是"判据拦不拦得住"。

**如果所有判定项仍然全 PASS，反而需要警惕**——可能是：
- 框架对合成数据过拟合；
- 或守卫锚点只对合成数据形态生效（第 11 条陷阱形态①）；
- 或真实数据触发的分支太少（见 §4）。

### 2.2 已知正常差异（**不是 FAIL**）

| 现象 | 说明 |
|---|---|
| 中文字体 fallback | Linux 上 `WenQuanYi Micro Hei` 可能不存在 → 回退 `DejaVu Sans`；Windows 回退 `Microsoft YaHei`。PDF 元数据不同属正常 |
| PDF 元数据字节数不同 | 字体回退导致，不构成目检 FAIL |
| 路径分隔符 | 脚本已处理（`\` → `/`），隔离守卫 v2 已针对 Windows 加固 |
| 时间戳 | 一律 UTC+8，若你的机器时区不同，**数值不受影响** |

### 2.3 真正的报警信号

- 判定项数 ≠ 222
- 某分支从 PASS→FAIL
- 判定项出现 `N/A`（第三态，见 §4）
- `guard_selftest.py` 报某守卫"未生效"

---

## §3 经验教训（13 轮迭代踩过的坑）

### 3.1 高频复发类型：**改了 A 忘改 B**（复发 5+ 次）

| 失配面 | 实例 |
|---|---|
| §9 ↔ §3 | 依据改了，主文没改 |
| criteria ↔ 框架正文 | 判据改了，正文没改 |
| 变更清单 ↔ 实际改动 | 声称改了，源文件没改 |
| MANIFEST ↔ stdout | 汇总手工填，与原始输出不符 |
| 图号 ↔ 分析产物 | 文件名连续 ≠ 流程真的执行过 |

**现有防线**：G-5 / G-5.DEPR / G-6 / R-8 / G-11 / G-12 / G-13 / G-14

### 3.2 自指悖论（复发 3+ 次）

- handoff 链登记自身 sha256 → 写回后必然失效
- MANIFEST 在文件头写自身 sha256 → 同样失效
- 守卫结果计入自身合法集 → 永久失败循环

**规则**：**任何文件不登记自身的校验和；任何守卫的结果不计入自身扫描范围。**

### 3.3 静默失败（最危险）

| 形态 | 后果 |
|---|---|
| `except Exception: pass` | 吞掉真错误，判"通过" |
| 正则不匹配 → 计数 0 → `"0" in s` 恒真 | 空规 |
| 路径错 → `NameError` 被吞 → 返回 `"unknown"` | 看起来合法、内容无意义 |

**规则**：框架明令禁止静默 except。遇到读不到的东西，`raise SystemExit`，不许降级成看起来正常的字符串。

### 3.4 数字唯一来源

```
report.json / guard_results.json / criteria.yaml  (台账)
      ↓
附录 stdout（原始输出，机器生成）
      ↓
MANIFEST.json（派生自 stdout）
      ↓
报告 / 正文（引用台账数字）
      ↓
守卫核查
```

**任何一环手工填或转述，整条链断。** v1.5「MANIFEST 手工填 guards」就是实例。

---

## §4 判断经验（方法论）

### 4.1 第 11 条陷阱：守卫的通过状态可以被构造（**最根本**）

| 形态 | 含义 |
|---|---|
| ① 从未 FAIL | 空规——规则没跑到过应触发的输入 |
| ② 先 FAIL 后 PASS | **唯一能证明守卫有效的证据** |
| ③ FAIL 被写成 PASS | 比①更隐蔽——连"先红后绿"的机会都被掩盖 |

> **一个从未 FAIL 过的守卫，和没有守卫，在证明力上完全等价。**

**推论**：审核方必须直接读 stdout，不能读汇总。汇总本身可能撒谎——即使撒谎是无意的。

### 4.2 "先红后绿"是唯一自证

`guard_selftest.py` 每次**现场注入违规**，当场验证守卫抓得到。当前 **66/66 生效、家族覆盖 100%（21/21）**。
（v2.25 实测：开发布局 66/66；交付布局在**先重跑主验证**之后同样 66/66 —— 锚点已改为
取 `build_version.json` 而非易变的 `report.json`。）

**这是本框架走到现在、唯一能自我校验的方法论。**

### 4.3 覆盖率粒度

注意：100% 是**守卫家族级**，不是子检查级。某个家族若有 5 个子检查但只有 1 个用例，家族级 100% 仍是粗粒度。附录 §G.5 给了「家族 → 用例」映射表。

### 4.4 判据成立 ≠ 判据通过

判据成立需满足两条：
1. 该判据**在应触发的输入上确实触发**（先红后绿验证）
2. 该判据的**触发条件与它宣称要防的错一一对应**

---

## §5 真实数据专项（GSE250167）

### 5.1 预期：覆盖面会比你想的窄

预演结果：**9 条判据中 8 条不适用，仅 T01 适用**。

原因：无通路注释、无临床结局、非单细胞、n=8 < 阈值（15/30）。

**这意味着 222 项里大部分分支不会被真正激活。**

### 5.2 第三态「不适用 + 理由」

v2.13 新增 `add_na()` + `applicability_gate.py`。规则：
- 理由为空 → 直接 `raise`（禁止空理由的"不适用"）
- 「不适用」**计入台账**，不得静默删除
- 与 PASS/FAIL 并列，是第三态

### 5.3 关键测试点

> 拟南芥小 RNA 的"基因"是 sRNA 序列，不是 mRNA。
> 若框架能按「不适用 + 理由」正确降级 → 自适应机制有效；
> 若强行套用并给出 PASS/FAIL → 暴露设计假设缺陷。

**已确认**：框架中**没有**硬编码 `|log2FC| ≥ 0.585`（grep 零结果），T01 查的是"尺度声明"，数据类型无关。真正的问题不是阈值不适用，而是框架此前**没有表达"不适用"的能力**——v2.13 已补。

### 5.4 运行方式

```bash
wget "https://ftp.ncbi.nlm.nih.gov/geo/series/GSE250nnn/GSE250167/suppl/GSE250167_RAW.tar"
tar -xvf GSE250167_RAW.tar -C ~/mvp_test/data/
cd crossval
python3 scripts/verify_all.py --datasource real --datadir ~/mvp_test/data
```

适配器**宁停勿猜**：文件名无法解析分组 → 报错退出，不猜测。

### 5.5 建议：加一个人类临床数据集

若目的是"最大化暴露框架问题"，GSE250167 太特殊。加一个带临床结局的人类数据集（TCGA 或 GEO 生存数据集）能激活 **T06/T09/T13** 等分支，覆盖面会好很多。

---

## §6 上机时的三条铁律

1. **不要改代码让它通过** —— 你是来照镜子的，不是来修东西的
2. **报错不要中断** —— 继续跑下一条，记录全部
3. **必须贴完整 stdout** —— 不能只给 JSON。本项目出过「FAIL 被汇总写成 PASS」，只给汇总等于重蹈覆辙

---

## §7 未完成项（如实列）

### 可自闭环：0 项（已全部完成）

### 外部依赖（2 项，**只有这两项能证明"这套东西到底有没有用"**）

| # | 项 | 谁做 |
|---|---|---|
| 1 | **跨平台实测**（Win11 + Linux 沙箱） | 你 |
| 2 | **真实数据验证**（GSE250167 / 人类临床集） | 你 |

### 已知边界（记录在案，未解决）

- 变异测试用例覆盖的是**已枚举的注入形态**，新写法仍可能逃逸
- 覆盖率 100% 是家族级，非子检查级
- `MUST_COVER` 列表本身无历史基线，若有人删项不会被检出
- Nature 官方措辞类条目（图注 ≤250 字口径等）需人工核对原文
- handoff 链仍有 9/14 交接在途未确认（C 宕机遗留）

---

> 一句话：**框架侧能自闭环的已全部做完，每个守卫都证明过自己能抓到违规。剩下的两项不是"做得更规范"，是"到底有没有用"——只有上机能回答。**
