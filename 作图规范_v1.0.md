# 作图规范 v1.0（Nature 标准）

> 版本 v1.0 · 依据：Nature Research Figure Guide + Nature Formatting Requirements + FigureGuild 2026
> 定位：主框架 **§5.6 图内容规范**。与 §5 排版层并行，默认休眠（同 V5-P/V5-C 机制）。
> 配套：`spec/figure_spec.yaml`（机器可检配置）+ `scripts/figure_kit.py`（导出与检查）

---

## §0 本版的实测发现（先用这三条，再看条文）

这三条是落盘时实跑发现的，**条文原文未提及**，且其中两条会改变你的执行方式：

### §0.1 Wong 色板不保证三色盲安全

实测 Wong 2011 前 6 色在三种色盲模拟下的最小 CIELAB ΔE：

| 模拟 | 最小 ΔE | 判定 |
|---|---|---|
| Protanopia | 14.53 | 强制阈值 ≥10，**通过** |
| Deuteranopia | 15.99 | 强制阈值 ≥10，**通过** |
| **Tritanopia** | **8.65** | 建议阈值 ≥10，**未达** |

**结论**：Wong 色板针对红绿色盲（protan/deutan）设计，对三色盲（tritanopia）不保证。
冲突来源主要是橙色 `#E69F00` 与黄色 `#F0E442` 在三色盲下接近。

**处理**：本规范把 protan/deutan 设为**强制项**、tritanopia 设为**建议项**（阈值 8.0）。
若目标人群需覆盖三色盲，避免使用橙+黄的相邻组合。

### §0.2 ΔE 抓不到**等亮度**红绿对比 —— 纯红纯绿反而是安全的

直觉相反，但实测成立：

| 色对 | 相对亮度 Y | ΔY | 模拟后 ΔE | 判定 |
|---|---|---|---|---|
| `#FF0000` vs `#00FF00` | 0.213 / 0.715 | **0.503** | 15.21 | **可区分** |
| `#A93226` vs `#1E8449` | 0.109 / 0.173 | **0.064** | — | **风险** |

**原因**：纯红与纯绿的相对亮度差异极大（ΔY=0.50），红绿色盲主要**靠亮度**区分二者 ——
这说明 ΔE 在**非等亮度**红绿上是有效的（它捕获了 L 通道差异）。
真正不可区分、且 ΔE 会漏判的，是**等亮度红绿**。

**处理**：单靠 ΔE 会漏判等亮度红绿，故另设 **C-6.c3 等亮度红绿检测**（色相一红一绿 + ΔY<0.15）。

### §0.3 色板本身含等亮度红绿对，规则必须限定适用范围

实测 Wong 色板内 `#D55E00`（朱红，Y=0.222）与 `#009E73`（绿，Y=0.257），ΔY=**0.035**——
若把 C-6.c3 无条件应用于所有颜色，**色板自身会被判违规**，造成误伤。

**处理（MVP 降级）**：C-6.c3 **仅检查色板外的自选色**，且为**建议级**（不阻断）。
色板内的颜色安全性由 C-6.c2（ΔE 强制项）验证，不重复判定。

> ⚠ **这是 MVP 阶段的降级，不是最终规则。** 代价是削弱了对自选色的约束力。
> **正式使用时应改为**：色板内等亮度对在**同一图的类别调色板中同时出现**时判违规
> —— 即豁免条件从"属于色板"收紧为"属于色板 **且不在同一图中并用**"。
> 改法：在 `figure_spec.yaml` 的 `redgreen` 下增设 `scope: same_figure`，
> 并把 `advisory: true` 改为 `false`。

---

## §A 图件本身

### A-1 分辨率

| 图类型 | 最低 | Nature 推荐 |
|---|---|---|
| 照片/连续色调（显微、WB、影像） | **≥300 dpi** | 300–600 dpi；在线校样上限 450 dpi |
| 线条图（矢量导出光栅） | **≥600 dpi** | 1000–1200 dpi |
| 混合图 | **≥600 dpi** | — |
| 灰阶图 | ≥300 dpi | — |

**禁止**：在制图软件中人为提高分辨率（Nature 明文）。

### A-2 色彩模式

| 内容类型 | 模式 |
|---|---|
| 原创研究 | **RGB** |
| 综述/透视/进展 | CMYK |

打印时自动转 CMYK，**作者无需手动转换**。

### A-3 格式

| 类型 | 主图（生产文件） | Extended Data |
|---|---|---|
| 矢量（线图、通路图、示意图） | AI / EPS / PDF（可编辑图层） | EPS / PDF |
| 光栅（显微、WB、照片） | TIFF / PNG（≥300 dpi） | JPEG 首选 / TIFF / EPS（≤10 MB） |
| 混合 | 分层 PSD / AI + 嵌入 TIFF | 同上 |

**Nature 主图禁止**：JPEG（压缩伪影）、PowerPoint/Word 截图。

### A-4 矢量/光栅混用

- 主图矢量优先；光栅部分嵌入为分层 TIFF，**不扁平化**
- **禁止**把矢量图导出为高 dpi 光栅"充数"

### A-5 单图规范（本框架默认单图，非组合图）

| 项 | 规范 |
|---|---|
| 尺寸 | 单栏 ≤89 mm；双栏 ≤183 mm |
| 高度 | ≤170 mm |
| 线宽 | 0.25–1 pt（最终尺寸） |
| 图内字号 | 5–7 pt；面板标签 8 pt |
| 图注 | ≤250 字（旗舰 Nature） |

### A-6 线条与填充

必须有：坐标轴线 + 刻度 + 单位标签
必须避免：背景网格线、投影/阴影、纹理/图案、装饰性图标

### A-7 色盲友好

**推荐色板（Wong 2011，Nature Methods 8:441）**：

```
#000000 黑    #E69F00 橙    #56B4E9 天蓝  #009E73 绿
#F0E442 黄    #0072B2 蓝    #D55E00 朱红  #CC79A7 紫红
```

连续数据：viridis / cividis / plasma / magma / inferno

**禁止**：红绿对比、rainbow/jet 色标、灰度转换后不可区分

**判据分级**（依 §0.1）：
- Protanopia / Deuteranopia：**强制**，ΔE ≥10
- Tritanopia：**建议**，ΔE ≥8
- 色板外等亮度红绿：**建议**，须规避

### A-8 面板标签（单图不适用）

若必须组合：**8 pt bold 正体小写 a, b, c**，左上角。
注意 Nature 用**小写**（Science/Cell 用大写）。

---

## §B 统计图内容规范

### B-1 误差棒定义（强制）
图注必须写明：误差棒 = SD / SEM / 95% CI **之一**；中心度量 = mean / median **之一**。
示例：*"Data are presented as mean values ± SEM."*

### B-2 n 值（强制）
- 必须为**精确整数**（n=5，不是 n=3–6）
- 必须定义重复单位：biologically independent samples / independent experiments / animals / donor
- **禁止**用技术重复冒充生物重复；**禁止** n<3 推导统计量

### B-3 显著性标记
- 图内 `*`/`**`/`***` 必须在图注给出对应 p 值阈值，或直接标注精确 p 值
- 必须说明检验类型、**单侧/双侧**、多重比较校正

### B-4 散点图 vs 柱状图
- **连续数据禁用纯柱状图**
- 推荐 dot plot / box-and-whisker
- 用柱状图时必须 overlay 数据点

### B-5 箱线图定义（强制）
图注须写明：minima、maxima、centre、bounds of box、whiskers、percentile。**缺任一项不通过**。

### B-6 生存曲线
须含：删失标记、at-risk table、中位随访、检验方法（log-rank/Wilcoxon）、HR 与 95% CI。

### B-7 火山图 / 热图 / 曼哈顿图

| 图 | 阈值线 | 颜色标尺 |
|---|---|---|
| 火山图 | FDR 与 \|log2FC\| 阈值线 | 上调/下调/不显著三色 |
| 热图 | 聚类方法（行/列） | z-score / log2FC / 原始值，须明确 |
| 曼哈顿图 | 全基因组显著性阈值 | 染色体交替色 |

### B-8 伪重复可视化
同一 donor/患者的多点必须用连线或同色标记；**禁止**细胞级与 donor 级散点混层不加区分（与框架 T16 一致）。

---

## §C 图像类图

### C-1 Western Blot
- 必须提供 **unprocessed 原始图像**（Nature 强制）
- 裁剪拼接必须画切割线
- 亮度/对比度线性调整须声明；**非线性调整严格禁止**（selective enhancement = misconduct）
- ≥3 次独立实验，图注写明 n

### C-2 显微图
- 必须有 **scale bar**（非放大倍数）
- scale bar 与文字须在**独立可编辑层**，不扁平化
- 图注写明物镜倍数、染色方法、每通道颜色

### C-3 图像诚信
禁止：非线性调整（gamma、非均匀对比度）、选择性增强、未声明的拼接与复用。

---

## §D 图注与正文配套

### D-1 自包含
图注须使图**脱离正文可理解**。

### D-2 结构顺序
1. 简要标题句 → 2. 内容描述 → 3. n 值（精确） → 4. 误差棒 + 中心度量 →
5. 统计检验 + 单/双侧 + 多重校正 → 6. p 值展示 → 7. 缩写定义 → 8. 第三方材料权限声明

### D-3 字数
旗舰 Nature **≤250 字**；子刊 ≤300 字。

### D-4 禁止
图注**不写方法细节**（放 Methods）、**不写结果叙述**（放 Results）。

### D-5 数字一致
图注中的 n、p、统计量必须与正文一致 → 不一致即 **R4 数字纪律违反**。

### D-6 统计方法
图注须标注图内所用统计方法（如 two-sided Wilcoxon rank-sum test）。

---

## §E 命名规范（与分析流程对齐）

```
{NN}_{analysis_step}_{description}.{ext}
```

| 段 | 含义 | 示例 |
|---|---|---|
| `NN` | 两位图号 | `01`, `02` |
| `analysis_step` | 分析步骤，**须属框架阶段标签** | `qc`, `deg`, `enrichment`, `survival`, `dca` |
| `description` | snake_case 简述 | `volcano_deg`, `km_os`, `calibration_curve` |

示例：`01_qc_violin_mito.pdf`、`02_deg_volcano.pdf`、`05_dca_net_benefit.pdf`

**与框架对齐**：`analysis_step` 必须 ∈ `criteria.yaml` 的 `stage_tag` 集合，由 **G-7** 机器校验。
当前标签集：`batch, dca, deg, enrichment, imputation, performance, perturbation, prediction, qc, replication, sensitivity, survival`

---

## §F 多格式导出

用户要求：**出图都出单图，jpg / png / tiff / pdf 四格式齐全**。

| 格式 | 用途 | 参数 |
|---|---|---|
| **PDF** | 矢量主图 | fonttype=42（TrueType）；不压缩；≤50 MB |
| **TIFF** | 光栅主图（显微/WB） | 600 dpi；LZW；RGB |
| **PNG** | 预览 / 投稿审查 | 300 dpi；无损；RGB |
| **JPEG** | Extended Data / 预览 | 300 dpi；quality≥90；RGB；≤10 MB |

**注意**：JPEG 不得作主图生产文件；PNG 仅作草稿/预览。

```python
matplotlib.rcParams['pdf.fonttype'] = 42
matplotlib.rcParams['ps.fonttype'] = 42
fig.savefig('fig.pdf',  bbox_inches='tight')
fig.savefig('fig.tiff', dpi=600, bbox_inches='tight')
fig.savefig('fig.png',  dpi=300, bbox_inches='tight')
fig.savefig('fig.jpg',  dpi=300, bbox_inches='tight',
            pil_kwargs={'quality': 95})     # ⚠ Agg 后端不支持 quality= 直接传参
```

> ⚠ 实测：`FigureCanvasAgg.print_jpg()` 不接受 `quality` 关键字，
> 必须走 `pil_kwargs={"quality": 95}`，否则导出即抛 TypeError。

---

## §G 机器可检清单（C-6 / G-7）

| 检查 | 来源 | 实现 |
|---|---|---|
| C-6.c1 颜色取自 Wong 色板 | A-7 | `figure_kit.check_colors` |
| C-6.c2 红绿色盲 ΔE ≥10（强制） | A-7 + §0.1 | 同上 |
| C-6.c2.tritan ΔE ≥8（建议） | §0.1 | 同上 |
| C-6.c3 无等亮度红绿（色板外） | §0.2/§0.3 | `check_redgreen` |
| C-6.f1–f5 体积/dpi/RGB/宽度/字体 | A-1/A-2/A-3 | `check_file` |
| C-6.l1–l10 图注必含项 | B-1/B-2/B-3/B-5/D | `check_legend` |
| C-6.n1–n2 命名与阶段标签 | §E | `check_name` |
| **G-7** 图名 `analysis_step` ∈ stage_tag | §E | `consistency_guard` |

**条目性质区分**：本规范只把**可机械判定**的项纳入 `figure_spec.yaml`。
Nature 官方措辞类要求（如"避免装饰元素"、图注≤250字的具体口径）需在提交前人工核对官方最新原文。

---

## §H 提交前 17 项检查清单

**先分清两类**——机器可检项由 `figure_kit.py` 自动判定，人工核对项机器无法替代：

| 类别 | 项数 | 依据 |
|---|---|---|
| **机器可检（C-6 / G-7）** | 12 项 | `spec/figure_spec.yaml` 配置 + 脚本判定 |
| **人工核对（Nature 官方措辞类）** | 5 项 | 需对照目标期刊官方最新原文，脚本无法判定 |

### §H.1 机器可检（12 项）

| # | 检查项 | 判据 | 实现 |
|---|---|---|---|
| 1 | 分辨率 | 照片 ≥300；线图 ≥600 | `check_file` C-6.f2 |
| 2 | 色彩模式 RGB | mode ∈ RGB/RGBA | C-6.f3 |
| 3 | 字体类型合规 | TrueType/Type0（禁 Type3） | C-6.f5 |
| 4 | 图宽度 ≤183mm | 双栏上限 | C-6.f4 |
| 5 | 体积 | 主图 ≤50MB / ED ≤10MB | C-6.f1 |
| 6 | 色板取自 Wong | 全部命中 | C-6.c1 |
| 7 | 红绿色盲 ΔE ≥10（强制） | protan/deutan | C-6.c2 |
| 8 | 三色盲 ΔE ≥8（建议） | tritanopia | C-6.c2.tritan |
| 9 | 无等亮度红绿（色板外，建议） | ΔY<0.15 | C-6.c3 |
| 10 | 图注必含项 | 误差棒/中心/n/单位/检验/单双侧 | C-6.l1~l7 |
| 11 | 箱线图六项定义 | min/max/center/bounds/whiskers/percentile | C-6.l8 |
| 12 | 命名与阶段标签 | `{NN}_{step}_{desc}` 且 step ∈ stage_tag | C-6.n1/n2 + G-7 |

### §H.2 人工核对（5 项，不可机械判定）

| # | 检查项 | 说明 |
|---|---|---|
| 13 | 字体族 Arial/Helvetica 且 5–7pt | 脚本可读字体名与 Subtype，但**无法判定最终渲染字号是否落在 5–7pt**（需结合图缩放比） |
| 14 | 无背景网格/阴影/图案/装饰图标 | 属视觉判断，需人眼或专业审图 |
| 15 | 连续数据非纯柱状图（须 overlay 数据点） | 需理解图的内容语义 |
| 16 | 图注自包含、≤250 字、**不写方法与结果** | "是否写了方法/结果"是语义判断，脚本无法区分 |
| 17 | 显微图有 scale bar 且位于独立可编辑层 | "独立图层"需检查源文件（PSD/AI），非导出件 |

> Nature 官方措辞类条目（如"避免装饰元素""图注 ≤250 字"的具体口径）**必须对照目标期刊
> 官方最新原文核对**，本规范不代替官方指南。
