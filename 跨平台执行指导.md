# 跨平台执行指导（Windows 11 / Linux 通用）

> 版本 v1.0 · 2026-09-11
> 用途：让 **Windows 11 + PowerShell 7** 或 **Linux** 环境下的执行者（人或 Agent）
> 在同一份代码、同一份数据上重跑，并把结果原样返回。
>
> **本指令自包含**，执行方无需了解框架背景。
> **一份文档覆盖两个平台** —— 刻意不做成两份，因为两份文档必然不同步
> （本项目已因此栽过 5 次，见文末"为什么不分两个文件"）。

---

## 一、给你的任务

有一个生信分析防错框架的代码包，已在开发机（Linux）跑通并留下基线结果。

**你的任务：换一个环境重跑，把结果原样返回。**

- 目的**不是**"跑通"，而是检验**同一份代码在不同环境下是否得到一致结果**。
- **如果结果不一致，那不是你的错，反而是最有价值的发现。**
- **不要修改代码让它通过** —— 那正是这次测试要找的东西。

> 一句话：**你是来照镜子的，不是来修东西的。**

---

## 二、先选你的平台

| 你的环境 | 跳到 |
|---|---|
| **Windows 11 + PowerShell 7** | [§3 Windows 命令](#三-windows-11--powershell-7) |
| **Linux（含 WSL、容器、沙箱）** | [§4 Linux 命令](#四-linux) |

两平台的**判定标准、返回要求、已知正常差异完全相同**，见 §5–§7。

---

## 三、Windows 11 / PowerShell 7

### 3.1 环境准备

```powershell
# 进入解压后的目录
cd <解压目录>\crossval

# 建立虚拟环境（推荐，避免污染全局）
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# ★ 强制 Python 用 UTF-8 输出（v2.25 新增，Windows 上**必做**）
#   中文 Windows 的默认控制台代码页是 cp936(GBK)，而脚本会输出 I² / ✓ / ✗ 等
#   GBK 无法编码的字符 → 抛 UnicodeEncodeError。实测（v2.24）verify_all.py 在
#   **已输出 8294 行、最接近终点的"分支 8C"处**崩溃，exit=3、report.json 未落盘，
#   整轮证据作废。这不是"字体差异"，是崩溃。
#   设以下两行即可彻底消除；脚本本身也已内建降级保护（不可编码字符替换为 '?'），
#   但设了这两行才能拿到**完全精确**的输出。
$env:PYTHONUTF8 = '1'
$env:PYTHONIOENCODING = 'utf-8'
```

若报 **"禁止运行脚本"**，先执行：
```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

### 3.2 安装依赖

```powershell
pip install -r requirements.txt
```

> ★ **v2.26：不再在本文件重复列依赖清单。**
> 此前这里写死了 `numpy==2.2.6 / scipy==1.15.3 / …`，而 `crossval/requirements.txt`
> 写的是 `numpy>=1.24 / …`，实跑环境又是 `numpy 2.5.0 / …` —— **同一件事三份数字**，
> 且本文件那份**与实测环境不符**。更糟的是 `requirements.txt` 当时**缺 pyyaml 与 pytest**，
> 按它 `pip install -r` 之后 T6/T7/T9 会连续 ImportError → 网关 G3 STOP。
> 现依赖清单**只有一处**：`crossval/requirements.txt`（钉实测版本）。
>
> 若某版本在你的源/平台上不可用：**不要中断**，装最接近的可用版本，
> 并在回报时**明确说明实际安装的版本号** —— 版本差异本身就是要观察的对象。

### 3.3 执行（按顺序，每条都要）

> **文档路径**：下面的 `<版本>_<日期>` 请替换为你包里实际的版本化文件名，
> 例如 `框架_独立审核包_v2.6_20260911.md`。
> 权威文件名清单见 `MANIFEST.json` 的 `files` 字段 —— **从那里取，不要猜**。
> （v2.6 审计指出：此处原写死了 `v1` 旧名，与版本化命名冲突。）

```powershell
cd <解压目录>\crossval

# ⓪ 环境快照（新增，用于差异归因）
python scripts\platform_check.py

# ① 主验证（约 10–60 秒）
python scripts\verify_all.py

# ② 排版与作图检查
python scripts\layout_check.py --figdir figures\demo

# ③ 单元测试
python -m pytest tests\ -q

# ④ 守卫有效性变异测试（验证守卫不是空规）
python scripts\guard_selftest.py

# ⑤ 一致性守卫
python scripts\consistency_guard.py --doc ..\框架_独立审核包_<版本>_<日期>.md

# ⑥ 跨文档报告守卫
python scripts\report_guard.py     --doc ..\四角度审核报告_合集_<版本>_<日期>.md

# ⑦ 工作证据链守卫（脚本在 handoff 目录下）
python ..\handoff\scripts\handoff_guard.py
```

### 3.4 与基线比对

```powershell
# 结果比对
python scripts\compare_runs.py results\report.json

# 环境比对（解释数值差异从哪来）
python scripts\compare_platforms.py results\platform_snapshot.json
```

---

## 四、Linux

### 4.1 环境准备

```bash
# 进入解压后的目录
cd <解压目录>/crossval

# 建立虚拟环境
python3 -m venv .venv
source .venv/bin/activate

# ★ 强制 Python 用 UTF-8 输出（v2.25 新增）
#   Linux 默认多为 UTF-8，一般已满足；显式设置是为了与 Windows 侧**同口径**，
#   使"同一份代码、同一份输出编码"成为前提而不是巧合（否则比对 stdout 时会误判）。
export PYTHONUTF8=1
export PYTHONIOENCODING=utf-8
```

**中文字体（可选但推荐）**：基线开发机装有 `WenQuanYi Micro Hei`。
纯净 Linux 可能没有中文字体 —— 脚本会自动回退到 `DejaVu Sans`，
**不会报错**，但生成的 PDF 元数据会不同（属已知正常差异，见 §7）。

若你想消除这个差异：

```bash
# Debian / Ubuntu
sudo apt-get install -y fonts-wqy-microhei
# RHEL / CentOS / Fedora
sudo yum install -y wqy-microhei-fonts
# Arch
sudo pacman -S wqy-microhei
```

装完清一次 matplotlib 缓存，否则新字体不会被识别：
```bash
rm -rf ~/.cache/matplotlib
```

### 4.2 安装依赖

```bash
pip install -r requirements.txt
```

> 依赖清单**只有一处**：`crossval/requirements.txt`（理由见 §3.2 的 v2.26 说明）。
> 版本不可用时同上：装最接近的，**记录实际版本号**。

### 4.3 执行（按顺序，每条都要）

> **文档路径**：同 §3.3 —— 从 `MANIFEST.json` 的 `files` 字段取实际文件名，
> 不要沿用任何硬编码的旧名。

```bash
cd <解压目录>/crossval

# ⓪ 环境快照（新增，用于差异归因）
python3 scripts/platform_check.py

# ① 主验证（约 10–60 秒）
python3 scripts/verify_all.py

# ② 排版与作图检查
python3 scripts/layout_check.py --figdir figures/demo

# ③ 单元测试
python3 -m pytest tests/ -q

# ④ 守卫有效性变异测试
python3 scripts/guard_selftest.py

# ⑤ 一致性守卫
python3 scripts/consistency_guard.py --doc ../框架_独立审核包_<版本>_<日期>.md

# ⑥ 跨文档报告守卫
python3 scripts/report_guard.py --doc ../四角度审核报告_合集_<版本>_<日期>.md

# ⑦ 工作证据链守卫
python3 ../handoff/scripts/handoff_guard.py
```

### 4.4 与基线比对

```bash
python3 scripts/compare_runs.py results/report.json
python3 scripts/compare_platforms.py results/platform_snapshot.json
```

### 4.5 （可选）完整重建

若你希望从零重跑整套构建流程（13 步，含版本化与打包）：

```bash
cd <解压目录>
python3 build.py <版本> <日期>          # 如 python3 build.py 2.6 20260911
python3 build.py <版本> <日期> --check  # 只做后置校验，不重跑
```

`build.py` 是平台无关的，Win/Linux 通用。

---

## 五、把结果发回（**最重要的一步**）

请把以下内容的**完整原文**返回，**不要摘要、不要转述、不要挑选**：

| # | 内容 | 位置 |
|---|---|---|
| 1 | 主验证台账 | `crossval/results/report.json` —— **全文** |
| 2 | 排版台账 | `crossval/results/layout_report.json` —— **全文** |
| 3 | 守卫计数 | `crossval/results/guard_results.json` —— **全文** |
| 4 | 环境快照 | `crossval/results/platform_snapshot.json` —— **全文** |
| 5 | 各命令的控制台输出 | 每条命令的 stdout，**原样** |
| 5b | **一致性守卫 stdout（关键）** | `consistency_guard.py` 完整输出 —— **必须贴，不能只给 JSON** |
| 6 | 实际安装的依赖版本 | `pip freeze` 输出 |
| 7 | 系统信息 | Windows：`python -V` + `$PSVersionTable.PSVersion`<br>Linux：`python3 -V` + `uname -a` |

**如果文件太大无法全部贴上，优先贴 1、3、4、5b，并说明省略了什么。**

> **为什么 5b 强制**：本项目曾出现过"守卫实际 FAIL，但汇总 JSON 显示 PASS"的事故。
> 只看 JSON 会漏掉真问题。**stdout 才是原始输出，JSON 是派生汇总。**

---

## 六、结果怎么判（你不需要判断，但请了解）

| 档 | 判据 | 含义 |
|---|---|---|
| **通过** | 判定项数 = 222、分支 = 21；数值与基线差 ≤ 1e-3 | 跨平台一致 |
| **通过但有差异** | 计数一致，数值差 > 1e-3 | 需人工核查差异来源 |
| **真发现问题** | 计数不一致，或某分支 PASS→FAIL | **最有价值，不是失败** |

第三种情况请**如实报告**。若 Windows 暴露了 Linux 上没有的问题，
说明"同代码不同平台结果应完全一致"这个期望本身需要修订 —— **这正是要找的东西**。

---

## 七、已知的正常差异（**这些不算失败，不要修**）

1. **中文字体**：Windows 无 `WenQuanYi Micro Hei`（回退 `Microsoft YaHei`/`SimHei`）；
   纯净 Linux 可能也没有（回退 `DejaVu Sans`）。**属预期。**
2. **字体渲染 / PDF 元数据**：不同系统生成的 PDF 元数据可能不同。**属预期。**
3. **路径分隔符**：代码已处理 `\` 与 `/`。若发现**新的**路径错误，**请报告**，这是真问题。
4. **文件系统大小写**：Windows 不敏感、Linux 敏感。隔离守卫已按此加固。**属预期。**
5. **matplotlib 后端**：保持 `Agg`，**不要**改成 `TkAgg`。
6. **时间戳**：取决于时区，`platform_check.py` 会记录 UTC 偏移。
7. **`run_id`**：每次运行必然不同（由 seed + 时间戳 + 脚本哈希导出）。**这是预期行为**，
   不构成差异。v2.25 起一致性守卫已改为用 `input_hash`（同代码同输入下稳定）
   做跨制品锚，不再拿 `run_id` 比对 —— 所以重跑主验证后**不会**再产生
   4 项必然 FAIL。
8. **控制台编码**：**不属于"已知正常差异"**。中文 Windows(cp936) 下若未设
   `PYTHONUTF8=1`，脚本可能输出被替换的字符（如 `?` 代替 `✓`）。此为脚本的
   降级保护，不是环境噪音；设了环境变量即为精确输出。若出现
   `UnicodeEncodeError` 崩溃 → **那是真问题，请报告**。
9. **`guard_selftest.py` 的退出码有三态**（v2.26 新增第 3 态），**务必分清**：

   | 码 | 含义 | 处置 |
   |---|---|---|
   | 0 | 全部守卫均证明过自己能抓到违规 | 正常 |
   | 1 | 存在**空规**（规则从没跑到过应触发的输入） | 真发现，回报 |
   | **2** | **环境错误** —— 子守卫没跑起来，本轮结果无效 | **先修环境，不要当空规上报** |

   > **为什么单列第 3 态**：子守卫原以硬编码 `python3` 启动，而 Windows 的 venv
   > **不生成 `python3.exe`**（只有 `python.exe`）→ `python3` 落到 PATH 上另一个
   > 没装依赖的解释器 → ImportError → 输出里一个判定行都没有 →
   > **18 个守卫家族被误报为"空规"**。
   > 这是把**环境故障**误报成**规则失效**（第 11 条陷阱的镜像形态），
   > 会把审核方引去查根本不存在的空规。v2.26 起改用 `sys.executable`，
   > 并把两者分开报。若你看到退出码 2，请贴出它给出的"已发生用例"清单。

---

## 八、若某条命令报错

请**原样贴出完整错误信息**（含 traceback），然后**继续跑下一条**，
不要因为一条失败就中断整个流程。

**报错本身是有价值的结果** —— 它说明这套代码在该平台上存在可移植性问题。

---

## 九、为什么不分两个文件

本项目在 v1.4–v2.5 期间，"改了 A 忘了改 B"复发 **5 次**：
§9↔§3、criteria↔正文、变更清单↔实际改动、MANIFEST↔stdout、图号↔分析产物。

若把执行指导拆成 Windows 版和 Linux 版，第六处不同步几乎必然发生：
改了判定项数却只改一个文件、加了新脚本却只写进一份……

**所以这份文档刻意合二为一**：通用部分（§1、§5–§9）只有一处，
平台差异被隔离在 §3、§4 两个命令块里，且两块的**步骤编号一一对应**
（都是 ⓪①②③④⑤⑥⑦），便于肉眼核对是否同步。

**若你发现 §3 与 §4 步骤数量不一致，请报告** —— 那就是文档本身的不同步。
